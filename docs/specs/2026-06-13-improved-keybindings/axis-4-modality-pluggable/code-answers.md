[← Back to index](./README.md)

## Axis 4 — Modality as a first-class, pluggable choice

### A4-Q4 — CONFIRMED · confidence: high

**Q.** Entity carries one associated type Event (entity.rs:39-40); VimModel Event is VimEvent (vim.rs:1837), subscribe_to_model is typed on that event (model/context.rs:67). Should all engines share Entity Event = EditorCommand so ONE subscription + ONE blanket ModalEditExecutor impl (replacing vim.rs:1849) serves every mode -- and in that one executor, can Helix's new ExtendSelection(Motion) REUSE the 11-way vim navigate fan-out (vim.rs:1865-1883) or need a parallel fan-out? Or keep per-engine event types requiring N subscriptions in a ModalEngineModel enum?

**A.** Verified against code. The framework mechanically supports a shared-Event design: Entity::Event is a free associated type (entity.rs:39-40) and subscribe_to_model downcasts to <S as Entity>::Event monomorphically (context.rs:67-86), so one Entity emitting EditorCommand = exactly one subscription + one blanket ModalEditExecutor impl, generalizing today's single blanket `impl<T> VimSubscriber for T where T: VimHandler` (vim.rs:1849-1942). A per-engine-Entity enum does NOT collapse N: each variant is a distinct Entity with its own Event type, each needing its own subscribe_to_model. Helix ExtendSelection(Motion) CAN reuse the existing motion dispatch instead of a duplicated 11-way fan-out, because the model primitives already carry a keep_selection:bool extend/collapse flag (model.rs:2315-2321, tail logic 2410-2413) and the operator path already drives motions with extend semantics (vim_handler.rs:223-231). The only real cost is threading a keep_selection/extend parameter through the navigate trait methods (which today hardcode false). None of ModalEditExecutor/EditorCommand/ModalEngine/ExtendSelection exist yet (grep returns 0) -- all are design proposals.

I independently opened every cited file plus one location the answer flagged as unverified (the terminal navigate path). Every evidence claim holds at the cited line ranges.

CODE FACTS (re-verified):
1. Entity has exactly one associated type: `pub trait Entity: 'static { type Event; }` (entity.rs:39-40). No second event slot. CONFIRMED.
2. subscribe_to_model<S: Entity, F> binds the callback to S::Event and at dispatch downcasts the payload to `<S as Entity>::Event` (context.rs:67-86, downcast at 79-81). One call = one subscription bound to one concrete Event type; N distinct Entity/Event types => N subscriptions. CONFIRMED.
3. VimModel fixes Event=VimEvent: `impl Entity for VimModel { type Event = VimEvent; }` (vim.rs:1836-1838). VimEvent struct at vim.rs:524, VimEventType enum at vim.rs:540. CONFIRMED.
4. A single blanket dispatch impl already exists today: `impl<T> VimSubscriber for T where T: VimHandler` (vim.rs:1849-1942) is one big match over VimEventType. This "one subscription + one blanket impl per view" is the CURRENT shape, just nailed to VimEvent/VimHandler. CONFIRMED.
5. Exactly TWO concrete VimHandler impls: CodeEditorView (vim_handler.rs:23) and terminal EditorView (mod.rs:1959). Grep `impl VimHandler for` returns exactly these two. CONFIRMED.
6. The navigate fan-out (vim.rs:1865-1883) matches all 11 VimMotion variants and dispatches to navigate_char/word/line/... trait methods. VimMotion enum (vim.rs:302-319) has exactly 11 variants: Character, Word, Line, FirstNonWhitespace, FindChar, JumpToFirstLine, JumpToLastLine, JumpToLine, JumpToMatchingBracket, JumpToUnmatchedBracket, Paragraph. CONFIRMED.
7. NONE of the proposed types exist: grep -rE 'ExtendSelection|extend_selection_by_motion|ModalEditExecutor|ModalEngine|EditorCommand' over crates/ and app/ returns 0 hits. CONFIRMED.

SHARED-EVENT vs PER-ENGINE (code-verifiable portion):
- A shared Event is mechanically available; nothing constrains Entity::Event. A single Entity (e.g. a ModalEngineModel emitting EditorCommand) sets type Event = EditorCommand, is subscribed once, and dispatched by one blanket ModalEditExecutor impl -- a direct generalization of vim.rs:1849.
- The adversarial angle is sound: an enum of distinct per-engine Entities does NOT reduce N, because each variant is its own Entity with its own Event associated type needing its own subscribe_to_model. Only a single Entity with a shared Event collapses to one subscription.

EXTENDSELECTION REUSE OF THE FAN-OUT (strongly supported):
- The extend-vs-collapse switch already exists one layer below the fan-out: vim_move_horizontal_by_offset(.., keep_selection, ..) (model.rs:2315-2321) builds `SelectionOffsets { head, tail: if keep_selection { selection.tail } else { head } }` (model.rs:2410-2413). keep_selection=false = pure move; keep_selection=true = extend -- exactly Helix's "every motion extends".
- navigate_* impls hardcode keep_selection=false (vim_handler.rs:40,49). The VimHandler trait navigate methods (vim.rs:1949+) take (count, motion, ctx) with NO keep_selection param, so reuse requires adding that bool to the fan-out's trait-method signatures (or having the executor pass it). The model-layer selection algorithm itself is already shared.
- The operator-pending path already drives motions with extend semantics: vim_handler.rs:223-231 calls `model.vim_find_char(true /* keep_selection */, ..)`. There is a second, parallel motion fan-out (operator-select) calling distinct vim_select_for_char_motion (model.rs:2601), vim_select_for_word_motion (2665), vim_select_for_line_motion (2744), vim_find_char (3102). ExtendSelection slots into one of the two existing dispatch tables (most naturally navigate with keep_selection=true), not a brand-new third table.

VIEW WIRING: each view does one add_model + one subscribe_to_model for the vim model (view.rs:359-360; mod.rs:3015,3017). CONFIRMED.

INDEPENDENT CHECK BEYOND THE CITED LINES (terminal path, which the answer flagged as not line-verified): terminal EditorView::navigate_char (mod.rs:2032) calls `move_cursors_by_offset(count, &Direction::Backward, /* keep_selection */ false, /* stop_at_line_boundary */ true, ctx)`. This is a DIFFERENT method name than the code editor's vim_move_horizontal_by_offset, but it carries the SAME keep_selection extend/collapse flag and hardcodes false in navigate. This corroborates (does not refute) the answer's "model primitives carry keep_selection" claim across both editors -- the flag concept is shared even though the two editors use distinct model methods.

NITPICK (non-material): detailed_answer point 3 cites VimEventType at "vim.rs:522-599"; actual lines are struct VimEvent at 524 and enum VimEventType at 540. The evidence array correctly uses 524-599. No bearing on any conclusion.

_Evidence:_
- Entity has exactly one associated type Event: `pub trait Entity: 'static { type Event; }` — `crates/warpui_core/src/core/entity.rs:39-40` _(both)_
- subscribe_to_model<S: Entity, F> binds callback to S::Event and downcasts payload to <S as Entity>::Event -- one subscription per (model, concrete Event type) — `crates/warpui_core/src/core/model/context.rs:67-86 (downcast at 79-81)` _(code)_
- VimModel fixes Entity::Event = VimEvent; VimEvent struct at line 524, VimEventType enum at line 540 — `crates/vim/src/vim.rs:1836-1838 (and struct 524 / enum 540)` _(code)_
- A single blanket dispatch impl already exists: impl<T> VimSubscriber for T where T: VimHandler, one match over VimEventType — `crates/vim/src/vim.rs:1849-1942` _(both)_
- The navigate fan-out matches all 11 VimMotion variants and dispatches to navigate_* trait methods — `crates/vim/src/vim.rs:1865-1883` _(code)_
- VimMotion has exactly 11 variants (Character, Word, Line, FirstNonWhitespace, FindChar, JumpToFirstLine, JumpToLastLine, JumpToLine, JumpToMatchingBracket, JumpToUnmatchedBracket, Paragraph) — `crates/vim/src/vim.rs:302-319` _(code)_
- Exactly two concrete VimHandler impls exist: CodeEditorView and terminal EditorView (grep `impl VimHandler for` returns exactly these two) — `app/src/code/editor/view/vim_handler.rs:23 and app/src/editor/view/mod.rs:1959` _(both)_
- Motion model primitive carries keep_selection:bool; tail collapses to head when false, stays when true -- the extend/collapse switch — `app/src/code/editor/model.rs:2315-2321,2410-2413` _(code)_
- navigate_char impl hardcodes keep_selection=false (pure move); VimHandler trait navigate methods take (count, motion, ctx) with no keep_selection param — `app/src/code/editor/view/vim_handler.rs:40,49 and crates/vim/src/vim.rs:1949-1973` _(code)_
- Operator path already drives motions with extend semantics: vim_find_char(true /* keep_selection */) — `app/src/code/editor/view/vim_handler.rs:223-231` _(code)_
- A second, parallel operator-select motion fan-out exists via distinct model methods vim_select_for_char/word/line_motion and vim_find_char — `app/src/code/editor/model.rs:2601,2665,2744,3102` _(code)_
- Each view wires exactly one add_model + one subscribe_to_model for the vim model — `app/src/code/editor/view.rs:359-360 and app/src/editor/view/mod.rs:3015,3017` _(code)_
- ModalEditExecutor, EditorCommand, ModalEngine, ExtendSelection, extend_selection_by_motion do not exist in code (grep over crates/ and app/ returns 0 hits) — `grep -rE 'ExtendSelection|extend_selection_by_motion|ModalEditExecutor|ModalEngine|EditorCommand' crates/ app/ => 0` _(code)_
- Terminal EditorView::navigate_char uses move_cursors_by_offset(..,/* keep_selection */ false,..) -- a different method name than code editor's vim_move_horizontal_by_offset but the same keep_selection flag (corroborates shared selection-switch concept across both editors) — `app/src/editor/view/mod.rs:2032-2071` _(code)_

- _Unblocks:_ Lets the design doc commit to: (1) a single shared Entity emitting a mode-neutral EditorCommand with ONE subscribe_to_model and ONE blanket ModalEditExecutor impl (generalizing vim.rs:1849), explicitly rejecting a per-engine-Entity enum as not reducing N subscriptions; and (2) modeling Helix ExtendSelection as a keep_selection=true flag threaded through the existing motion dispatch (adding one bool to the navigate trait methods) rather than a duplicated 11-way fan-out -- bounding executor-trait growth to ~a flag/one method instead of a full parallel motion table.
- _Residual / needs decision:_ The actual product/architecture choice (single shared Entity emitting mode-neutral EditorCommand + one boxed dyn ModalEngine vs an enum of per-engine Entities) is a design decision, not a code fact -- code only shows the shared-Event path is mechanically supported and strictly cheaper in subscription count. Whether ExtendSelection threads keep_selection through the navigate fan-out vs the operator-select fan-out is an open design choice; both share the model-layer keep_selection switch but are two distinct dispatch tables today, and the navigate trait methods would need a new keep_selection/extend parameter to be reused. Helix motion semantics beyond simple extend (anchored/directional selection, multi-selection extension) were not exhaustively mapped against vim_move_*/vim_select_for_* behavior. Confirmed the terminal editor also carries a keep_selection flag, but via a distinct method (move_cursors_by_offset, mod.rs:2032) rather than the code editor's vim_move_horizontal_by_offset -- so the extend primitive is conceptually shared but not a single shared implementation across the two editors; a unified executor would still need both editors to expose the same extend-capable trait surface.
- _Verification:_ I opened all 13 cited locations and re-ran the grep myself; every line reference is accurate and says what the answer asserts. Specifically re-verified: entity.rs:39-40 (one associated type), context.rs:67-86 (S::Event downcast at 79-81), vim.rs:1836-1838 (Entity for VimModel), vim.rs:1849-1942 (blanket VimSubscriber match), vim.rs:1865-1883 (11-way navigate fan-out), vim.rs:302-319 (exactly 11 VimMotion variants -- counted), vim_handler.rs:23 + mod.rs:1959 (exactly 2 VimHandler impls, grep-confirmed), model.rs:2315-2321/2410-2413 (keep_selection tail logic), vim_handler.rs:40,49 (false), vim_handler.rs:223-231 (vim_find_char true), model.rs:2601/2665/2744/3102 (vim_select_for_* + vim_find_char), view.rs:359-360 + mod.rs:3015/3017 (one add_model + one subscribe each). Grep for the five proposed type names returns 0 hits across crates/ and app/. I also checked the location the answer left unverified (terminal navigate path, mod.rs:2032): it calls move_cursors_by_offset with a keep_selection bool (false) -- a different method than the code editor's vim_move_horizontal_by_offset, but it confirms (not contradicts) that both editors thread a keep_selection extend/collapse flag. No app/-vs-crates scope confusion, no overgeneralization, no off-by-N counts in the evidence array. Only nitpick: detailed_answer prose cites VimEventType at 522-599 while struct VimEvent is at 524 / enum at 540 -- non-material, and the evidence array already uses 524-599.

### A4-Q5 — CONFIRMED · confidence: high

**Q.** The reusable buffer algorithms take vim-namespaced value enums (vim_inner_word(.., WordType), vim_find_matching_bracket(.., BracketChar), vim_word_iterator_from_offset(.., WordType, WordBound)) and vim_find_char is NOT generic (concrete &str, find_char.rs:28). Does the mode-agnostic EditorCommand IR carry these vim enums directly (coupling Helix/Kakoune to vim vocabulary), or define neutral motion/operator types with per-engine translation shims into the vim enums at the executor boundary?

**A.** CONFIRMED. Today and as prescribed by the report, the IR carries the vim enums DIRECTLY — the proposed EditorCommand is explicitly "a crate-level superset of VimEventType" (report line 210) and the buffer algorithms are "reused as-is" (report line 221). No neutral motion/operator type and no translation shim exists anywhere in code (grep for EditorCommand/ModalEditExecutor returns 0 hits in app/ and crates/); the executor unpacks the IR's vim enums straight into the algorithms (model.rs:2898-2933, verbatim pass-through). The enum VALUES are mode-neutral editing primitives that Helix's analogous commands map onto without new variants, but the NAMING/vocabulary is vim, so "mode-agnostic" is partly a naming fiction — exactly the caveat the report flags at line 191.

I independently opened every cited file and re-ran the greps. The answer is accurate on all line numbers, enum bodies, signatures, executor behavior, grep counts, and report citations. Nothing material is wrong.

VERIFIED CODE FACTS:

1) IR = VimEventType, crates/vim/src/vim.rs:540 (enum). It embeds vim value enums directly via Navigate(VimMotion) (vim.rs:542), Operation{operator:VimOperator, operand:VimOperand,...} (vim.rs:566-567), and VisualTextObject(VimTextObject) (vim.rs:589). All confirmed at the exact lines claimed.

2) The vim-namespaced value enums (all in crates/vim/src/vim.rs): Direction:58, WordBound:74, WordType:83 (BigWord doc literally says the Vim docs call these "WORD" in all caps), WordMotion struct:111 {direction:Direction, bound:WordBound, word_type:WordType}, FindCharMotion:164 {direction:Direction, destination:FindCharDestination, is_repetition:bool, c:char}, FindCharDestination:231 {AtChar, BeforeChar}, VimMotion:302 (Word(WordMotion), FindChar(FindCharMotion), JumpToUnmatchedBracket(BracketChar):316), MotionType:325, TextObjectInclusion:337 {Around, Inner}, VimTextObject struct:347, QuoteType:367 {Single, Double, Backtick}, BracketEnd:396 {Opening, Closing}, BracketType:403 {Parenthesis, CurlyBrace, SquareBracket}, BracketChar struct:422 {end:BracketEnd, kind:BracketType}, TextObjectType:480 (Word(WordType)/Paragraph/Quote(QuoteType)/Block(BracketType)), VimOperand:696 (Motion{MotionType, VimMotion}/TextObject(VimTextObject)/Line). Every line matches.

3) Algorithm signatures: vim_word_iterator_from_offset (word_iterator.rs:52) requires Direction(55), WordBound(56), WordType(57) — all generic over T:TextBuffer+?Sized. vim_find_matching_bracket (matching_brackets.rs:9) takes &BracketChar and dispatches on BracketEnd. vim_inner_word/vim_a_word (word.rs:15/52) take WordType. vim_inner_quote/vim_a_quote (quote.rs:15/34) take QuoteType. vim_inner_block/vim_a_block (block.rs:27/78) take BracketType. All generic over T:TextBuffer+?Sized. vim_find_char_on_line (find_char.rs:28) is NOT generic — first param line:&str (line 29), and it already carries keep_selection:bool (line 33, used at line 88). All confirmed exactly.

4) Executor does NO translation: ModelEditor::calculate_text_object_range (model.rs:2889) destructures TextObjectType::Word(word_type)→vim_inner_word/vim_a_word, Quote(quote_type)→vim_inner_quote/vim_a_quote, Block(bracket_type)→vim_inner_block/vim_a_block (model.rs:2898-2933), passing the vim enums verbatim. No intermediate neutral type. Confirmed.

5) Grep for EditorCommand / ModalEditExecutor over app/ and crates/ (excluding .md) returns 0 hits. Grep for "translation shim"/"neutral motion"/"neutral operator" returns 0. These are design-doc-only names.

6) find-char is single-line by nature: at model.rs:3121 the caller extracts one line via buffer.text_in_range(line_start..line_end).into_string() and passes &line_text. Same pattern in the terminal editor at view/model/mod.rs:1884. Both editor views (code + terminal) call the same single-line function — the answer correctly cited both, no app/crates or code/terminal scope confusion. Notably the caller iterates per-selection via current_selections.mapped(...), so per-selection iteration already exists.

7) Report citations all accurate: line 191 — "...all of these take vim-namespaced value enums like WordType/BracketChar/FindCharMotion, so reuse pulls in vim's domain types even though it's independent of the keymap"; line 210 — Step B "Extract a crate-level superset of VimEventType (vim.rs:540)"; line 221 — "The text objects, word iterators, and bracket matcher ... are reused as-is ... no new buffer algorithm is required". The report's Step B adds only ExtendSelection(Motion) and ApplyOperator(Operator) as the two NEW primitives and maps vim events 1:1 — i.e. Option A (carry vim enums directly), not neutral types + shims.

NET: The answer's thesis holds. The current IR carries vim enums directly, the report keeps it that way, no neutral/shim layer exists, the enum values generalize to Helix without new variants, and "mode-agnostic" is value-neutral-but-vim-named. The A-vs-B choice remains a genuine design decision because EditorCommand does not yet exist in code — the answer correctly hedges this in residual_unknowns.

_Evidence:_
- IR VimEventType (enum at line 540) embeds vim value enums via Navigate(VimMotion):542, Operation{operator:VimOperator,operand:VimOperand}:566-567, VisualTextObject(VimTextObject):589 — `crates/vim/src/vim.rs:540-599` _(code)_
- VimMotion carries Word(WordMotion{Direction,WordBound,WordType}), FindChar(FindCharMotion), JumpToUnmatchedBracket(BracketChar) — `crates/vim/src/vim.rs:302-316,111-115` _(code)_
- TextObjectType = Word(WordType)/Quote(QuoteType)/Block(BracketType); VimOperand::TextObject carries VimTextObject — `crates/vim/src/vim.rs:480-489,696-703` _(code)_
- vim_word_iterator_from_offset is generic over T:TextBuffer+?Sized and requires Direction, WordBound, WordType — `crates/vim/src/word_iterator.rs:52-60` _(code)_
- vim_find_matching_bracket takes &BracketChar and dispatches on BracketEnd — `crates/vim/src/matching_brackets.rs:9-22` _(code)_
- vim_inner_word/vim_a_word take WordType; vim_inner_quote/vim_a_quote take QuoteType; vim_inner_block/vim_a_block take BracketType — `crates/vim/src/text_objects/word.rs:15,52 + quote.rs:15,34 + block.rs:27,78` _(code)_
- vim_find_char_on_line is NOT generic (line:&str at line 29) but already carries keep_selection:bool (line 33, used line 88) — `crates/vim/src/find_char.rs:28-33,88` _(code)_
- Executor passes IR vim enums verbatim into algorithms with no neutral type/shim (Word(word_type)->vim_inner_word, Quote(quote_type)->vim_a_quote, Block(bracket_type)->vim_a_block) — `app/src/code/editor/model.rs:2889-2935` _(code)_
- find_char is single-line: caller extracts one line via text_in_range(line_start..line_end) and passes &line_text; same in terminal editor; iterates per-selection via .mapped — `app/src/code/editor/model.rs:3113-3127 + app/src/editor/view/model/mod.rs:1884` _(code)_
- Grep for EditorCommand/ModalEditExecutor over app/ and crates/ (excluding .md) returns 0 hits; translation shim/neutral motion/operator also 0 — `app/ + crates/ grep (re-run, 0 hits)` _(both)_
- Report flags the caveat that reuse pulls in vim's domain types because algorithms take vim-namespaced value enums (WordType/BracketChar/FindCharMotion) — `.understand-anything/keybindings-architecture-report.md:191` _(code)_
- Report Step B prescribes EditorCommand as 'a crate-level superset of VimEventType (vim.rs:540)', adding only ExtendSelection/ApplyOperator; vim events map 1:1 — `.understand-anything/keybindings-architecture-report.md:210` _(code)_
- Report states text objects/word iterators/bracket matcher are 'reused as-is ... no new buffer algorithm is required' — `.understand-anything/keybindings-architecture-report.md:221` _(code)_
- BracketType {Parenthesis,CurlyBrace,SquareBracket} and QuoteType {Single,Double,Backtick} are the only bracket/quote variants, covering Helix mi(/ma( — `crates/vim/src/vim.rs:403-407,367-371` _(code)_

- _Unblocks:_ Lets the design doc decide the central Axis-4 artifact: either accept VimEventType-as-superset (cheapest, but inherits vim vocabulary into the 'neutral' IR — and document that mode-agnostic means value-neutral-but-vim-named), or invest in renamed neutral motion/operator types with thin per-engine translation shims at the executor boundary. The code shows the values generalize without new variants, so the decision is primarily about naming honesty + future-engine ergonomics, not about missing algorithm capability — with the lone concrete generalization task being whether vim_find_char_on_line's single-line &str signature is acceptable (it is, plus it already has keep_selection) for select-first engines.
- _Residual / needs decision:_ The EditorCommand IR does not yet exist, so the A-vs-B choice (carry vim enums directly vs neutral types + per-engine shims) is a genuine design DECISION, not a code fact. What IS settled in code: the current IR carries vim enums directly, no neutral/shim layer exists, and the report's plan keeps it that way (superset of VimEventType, algorithms reused as-is). Open judgment calls: (1) whether to rename vim-namespaced types (WordType->WordKind, BracketChar->Bracket, FindCharMotion->...) into neutral vocabulary to make 'mode-agnostic' honest, at the cost of a translation layer; (2) whether Helix's whitespace-inclusive word-boundary conventions actually land on the SAME CharOffsets vim_word_iterator_from_offset emits — the WordType enum has the needed variants, but per-motion boundary parity for Helix w/e/b was not verified against Helix's exact stop set and would need keystroke-trace fixtures; (3) Kakoune's word/object model was not analyzed and may stress the enum set differently. One ambiguity in the report itself: Step B's ExtendSelection(Motion)/ApplyOperator(Operator) do not state whether Motion/Operator are the vim types or new neutral types — context ('superset of VimEventType', 'map 1:1', 'reused as-is') strongly implies the vim types, but a design doc could still go either way.
- _Verification:_ I opened the spec doc (question block at line 642, text matches), crates/vim/src/vim.rs (every cited enum/struct: 58,74,83,111,164,231,302,316,325,337,347,367,396,403,422,480,540,542,566-567,589,696 — all match), the four algorithm files (word_iterator.rs:52, matching_brackets.rs:9, text_objects/word.rs:15/52, find_char.rs:28), and crates/vim/src/text_objects/quote.rs and block.rs (signatures NOT deeply cited by the answer but I checked them: quote.rs:15/34 take QuoteType, block.rs:27/78 take BracketType — both confirmed). I read the executor calculate_text_object_range (model.rs:2889-2935) and confirmed verbatim vim-enum pass-through with no shim. I re-ran the EditorCommand/ModalEditExecutor grep over app/ and crates/ myself: 0 hits (excluding .md). I also grepped 'translation shim'/'neutral motion/operator': 0 hits. I opened a location the answer did not deeply cite — the find_char CALL SITE at model.rs:3113-3127 — confirming it extracts a single line (text_in_range(line_start..line_end)) and iterates per-selection via current_selections.mapped(...), which both supports the single-line claim and corroborates the per-selection-iteration anchor. I verified all three report citations (191, 210, 221) verbatim. No shifted line refs, no overgeneralization (find_char correctly flagged as the lone non-generic), no app/crates scope confusion (answer correctly cites both the code editor model.rs:3121 and terminal editor view/model/mod.rs:1884). The answer's interpretation that the report chooses Option A is the strongest-supported reading: 'crate-level superset of VimEventType' + 'reused as-is' + vim events 'map 1:1'. The one inherent soft spot — whether the report's ExtendSelection(Motion)/ApplyOperator(Operator) use vim or neutral Motion/Operator types — is genuinely undecided in a design doc, and the answer correctly defers it to residual_unknowns rather than overstating.

### A4-Q6 — CONFIRMED · confidence: high

**Q.** Verified the editor ALREADY applies operators across all selections (update_selections_internal iterates selections.iter().zip(...).map(...); vim operators map over Vec1 of SelectionOffsets) -- so per-selection apply is NOT the Helix gap. Which specific Helix selection-management commands (s regex-split, S, x line-split, % whole-file, keep/collapse, rotate-primary) require NEW model primitives, and does the IR/ModalEditExecutor own them or do they stay app-level?

**A.** Confirmed: the per-selection APPLY path is already fully multi-selection (selection.rs:642 returns Vec1; update_selections_internal merges then maps over the Vec1 at selection.rs:683,694-697; vim operators map over selection_offsets() e.g. vim_handler.rs:237,364-365 and write back via vim_set_selections -> BufferSelectAction::SetSelectionOffsets), so per-selection apply is NOT the gap. The real gap is selection-set CREATION/MANAGEMENT: of the Helix commands only `%` (SelectAll) has a primitive; `s`/`S`/`x`/keep/remove/`;`/`,`/rotate-primary have ZERO primitives, and there is no "primary selection" pointer (model exposes only first/last of the Vec1). These new primitives must land in the crates/editor selection model (they need buffer bytes + the regex DFA, both unreachable from the IR/executor); the IR/ModalEditExecutor can only carry intent and dispatch.

VERIFIED by opening every cited file plus the trait-default sites the answer did not pin precisely.

PART 1 — Per-selection APPLY is genuinely already multi-selection (the draft "operators assume a single primary selection" framing is false):
- `Selection::selections()` returns `Vec1<SelectionOffsets>` at crates/editor/src/selection.rs:642 (confirmed; answer's :642 exact).
- `update_selections_internal` first calls `selection_model.merge_overlapping_selections()` (selection.rs:683), then `selections.iter().zip(goal_xs.iter()).map(|(selection, old_goal_x)| selection_update(...)).unzip()` at selection.rs:694-697 (answer said 693-697 — off by one on the start, immaterial). Movement/extension flows through it: extend_selection:729, move_selection:764, navigate_character:822, navigate_word:835, navigate_line:868, navigate_line_boundary:922 (answer's "navigate_* 800-922" — first navigate is actually 822, not 800; immaterial range slip).
- Vim operators map over the whole set: vim_handler.rs:237 `current_selections.mapped(|selection| ...)` (JumpToLine, full block 232-250 confirmed), and the text-object path `model.buffer_selection_model().as_ref(ctx).selection_offsets().mapped(|selection| { let start = head.min(tail); ... })` at vim_handler.rs:364-365 then `model.vim_set_selections(starts, ...)` at :372. Write path `vim_set_selections` issues `BufferSelectAction::SetSelectionOffsets { selections }` at app/src/code/editor/model.rs:587/595. `add_cursor` exists at selection.rs:499; `add_cursor_at` exists as a trait method at crates/editor/src/model.rs:633 (NOTE: the answer's bare "model.rs:633" resolves to crates/editor/src/model.rs, not app/src/code/editor/model.rs whose :633 is the unrelated nav_diff_up — minor citation ambiguity, the method genuinely exists). Net: operator application over N selections is reusable today.

PART 2 — Selection-set CREATION/MANAGEMENT primitives: the true missing layer.
- The complete write substrate is `BufferSelectAction` (crates/editor/src/content/buffer.rs:164-197): MoveLeft/Right, ExtendLeft/Right, SelectAll, SetLastHead, SetLastSelection, AddCursorAt, AddSelection, UpdateSelectionOffsets, SetSelectionOffsets, plus `merge_overlapping_selections` (selection_model.rs:156). All low-level "write raw offsets". No pattern-compute command, no primary pointer.
- Reproducible grep RE-RUN by me across crates/editor crates/vim crates/warpui_core app/src for split_selection|split_on_regex|select_regex|select_all_matches|keep_selections|keep_matching|remove_selections|rotate_primary|keep_primary|remove_primary|collapse_selection|trim_selection|align_selection => EXACTLY 2 hits, both `collapse_selections_to_cursors` (app/src/notebooks/editor/model.rs:1221 + view.rs:2026). I read the impl: it takes `selection.selections(ctx).first()` and writes a single `vec1![SelectionOffsets{ head: first_selection.head, tail: first_selection.head }]` via SetSelectionOffsets — collapses ALL selections to ONE cursor at the first head, which is neither Helix `;` (collapse each) nor `,` (keep primary). Zero hits in crates/editor, crates/vim, or the code-editor/vim path.
- No "primary selection" concept: the model only exposes first/last of the Vec1; buffer.rs:1981 "non-primary selection models" is the shared-buffer-across-views primary model, and terminal `rotate_selection` (app/src/terminal/model/alt_screen.rs:341) is scroll — neither is the Helix multi-cursor primary pointer.
- Regex/search is single-match only: search.rs select_next_from_cursor:174, select_next_result:199, select_match_at_offset:245 each select ONE match; no "select all regex matches as multi-selection" and no split/keep-by-regex.
- The only multi-cursor CREATION features (`select_next_occurrence` app/src/editor/view/mod.rs:6981; add_cursor_above/add_cursor_below bindings mod.rs:938/945, impl `add_cursor` mod.rs:7099) live ONLY in the TERMINAL editor and are built on the DisplayMap/DisplayPoint model (verified at 7099: uses `editor_model.display_map(ctx)` and `to_display_point`), so they are NOT reusable by the code editor / vim path. grep confirms app/src/code/editor has none.

PART 3 — Helix command -> existing vs missing primitive:
- `%` whole file -> EXISTS: BufferSelectAction::SelectAll (buffer.rs:169, impl :1653), CodeEditorViewAction::SelectAll (actions.rs:639/914) -> trait default model.select_all (crates/editor/src/model.rs:545).
- `s`/`S`/keep/remove (regex select/split/keep) -> MISSING; need buffer text + regex DFA.
- `x` whole-lines/line-split -> only partial substrate (selected_lines selection_model.rs:89, navigate_line selection.rs:868); no `x` primitive.
- `;`/`,` collapse-each / keep-primary -> only the notebooks "collapse to one cursor" hack; no shared/correct primitive; no primary pointer.
- rotate selections/contents, remove-primary, `_` trim, `&` align, copy-selection-next/prev -> MISSING.

PART 4 — Ownership: the COMPUTE side of every missing command needs buffer bytes and/or the regex DFA, both living in crates/editor (find.rs/search.rs, buffer.rs) and unreachable from the IR (EditorCommand) or a ModalEditExecutor. So new primitives MUST be new crates/editor selection-model operations (new BufferSelectAction variants + Selection/SelectionModel methods: SelectRegexWithin, SplitOnRegex, KeepMatching/RemoveMatching, CollapseEach, RotatePrimary, plus new "primary index" model state since Vec1 has none). The IR/executor own only intent + dispatch. This corrects the report's "~90% shared" framing: per-selection apply is reusable; selection-set management is net-new model surface.

_Evidence:_
- selections() returns Vec1<SelectionOffsets>; update_selections_internal merges overlapping then iterates/zips/maps/unzips over the Vec1 (per-selection apply is built-in) — `crates/editor/src/selection.rs:642,683,694-697` _(code)_
- vim operators map over the whole selection set, not a primary: JumpToLine current_selections.mapped (:237, block 232-250) and text-object selection_offsets().mapped (:364-365) then vim_set_selections (:372) — `app/src/code/editor/view/vim_handler.rs:237,364-372` _(code)_
- vim_set_selections write path dispatches BufferSelectAction::SetSelectionOffsets — `app/src/code/editor/model.rs:587,595` _(code)_
- add_cursor exists (selection.rs:499) and add_cursor_at is a trait method (crates/editor/src/model.rs:633) — NOT app/src/code/editor/model.rs:633 which is nav_diff_up — `crates/editor/src/selection.rs:499; crates/editor/src/model.rs:633` _(code)_
- BufferSelectAction is the full write substrate: only raw-offset ops + SelectAll; no pattern-compute, no primary pointer — `crates/editor/src/content/buffer.rs:164-197` _(both)_
- Re-ran the Helix-primitive grep across crates/editor crates/vim crates/warpui_core app/src: exactly 2 hits, both collapse_selections_to_cursors (notebooks), which collapses ALL selections to ONE cursor at the first head — not Helix ; or , — `app/src/notebooks/editor/model.rs:1221-1234; app/src/notebooks/editor/view.rs:2026` _(both)_
- % (SelectAll) is the only Helix command with an existing primitive: BufferSelectAction::SelectAll variant+impl, CodeEditorViewAction::SelectAll, trait-default select_all — `crates/editor/src/content/buffer.rs:169,1653; app/src/code/editor/view/actions.rs:639,914; crates/editor/src/model.rs:545` _(code)_
- Search/find selects a SINGLE match only; no multi-selection regex creation — `crates/editor/src/search.rs:174,199,245` _(code)_
- Multi-cursor CREATION exists ONLY in the terminal editor on the DisplayMap/DisplayPoint model (select_next_occurrence; add_cursor_above/below bindings + impl); code editor has none — `app/src/editor/view/mod.rs:6981,938,945,7099` _(both)_
- No Helix primary pointer: buffer.rs:1981 'non-primary selection models' is the shared-buffer primary model; terminal rotate_selection is scroll — `crates/editor/src/content/buffer.rs:1981; app/src/terminal/model/alt_screen.rs:341` _(code)_

- _Unblocks:_ Lets the design doc state, with evidence, that a single Helix executor reuses the existing per-selection apply path but must add a NEW selection-set-management layer in crates/editor (regex split/select/keep + collapse/rotate + a primary-selection pointer), dispatched by the IR/executor as intent only — sizing the true cost of "one executor for Helix" and correcting the report's ~90%-shared framing.
- _Residual / needs decision:_ The exact Helix command-to-keybinding inventory and which subset the team commits to is a product decision, not code-derivable. Whether a 'primary selection index' is added as new SelectionModel state vs. encoded by convention (e.g. last element) is a design choice. Whether new selection-set ops are new BufferSelectAction variants vs. free functions computing a Vec1 then calling existing SetSelectionOffsets is an implementation choice (both viable on today's substrate). The precise IR shape (one parametric SelectRegex command vs. several) is undecided.
- _Verification:_ I opened every cited file and re-ran the load-bearing grep. All material claims hold. Per-selection apply: selection.rs:642 (selections->Vec1) confirmed exact; update_selections_internal merge at :683 and iter/zip/map/unzip at :694-697 confirmed (answer's :693-697 off by 1 on start, immaterial). navigate_* functions are at 822-922 (answer's '800-922' first fn is actually 822 — range slip, immaterial). Vim mapping (vim_handler.rs:237,364-372), vim_set_selections->SetSelectionOffsets (model.rs:587/595), add_cursor (selection.rs:499) all confirmed. CITATION NUANCE (not an error): the answer's 'add_cursor_at (model.rs:633)' resolves to crates/editor/src/model.rs:633 (a real trait method calling selection.add_cursor), NOT app/src/code/editor/model.rs:633 (which is nav_diff_up); the prose's preceding sentence used the app path, so the bare 'model.rs' is ambiguous — but the method genuinely exists, so no conclusion changes. The Helix-primitive grep returned EXACTLY 2 hits as claimed, both notebooks collapse_selections_to_cursors; I read the impl and confirmed it collapses to a SINGLE cursor at the first selection head (not Helix ;/,). SelectAll end-to-end wiring confirmed at all four cited sites. Search single-match (174/199/245) confirmed. Terminal-only multi-cursor creation confirmed: select_next_occurrence at 6981, add_cursor_above/below bindings at 938/945, impl at 7099 using display_map/to_display_point (DisplayMap model), and zero hits in app/src/code/editor. buffer.rs:1981 non-primary and alt_screen.rs:341 rotate_selection false-positives confirmed. No overgeneralization, no app/-vs-crates scope confusion in the conclusions, count is correct. Verdict: confirmed; only cosmetic line/file-attribution slips that do not affect any finding.

### A4-Q7 — CONFIRMED · confidence: high

**Q.** VimModel is built once (ctx.add_model) and subscribed once at view.rs:359-360; the AppEditorSettings subscription at view.rs:312 has an empty body; mode is only re-read live via vim_mode_enabled() (view.rs:1953). When a user changes editing_mode mid-session (Vim to Helix), what happens to in-flight FSA state (pending_action, pending_action_count, pending_visual_object, register, dot_repeat_event, current submode) and the existing single subscription, and is the engine Entity swapped or reset in place?

**A.** Confirmed. In-flight FSA state is untouched and the engine is neither swapped nor reset.

VERDICT confirmed. Code editor view.rs builds the VimModel Entity once at 359 and subscribes once at 360. The field at 271 is a concrete ModelHandle, no enum or boxed dyn, with supports_vim_mode captured once at 357. The only enable normalization is a one-time escape keypress during construction at 363-368 (brace 369), gated on supports_vim_mode and vim_mode_enabled, never re-run on a settings change. The AppEditorSettings observer at 312-314 is literally ctx.notify, no rebuild, reset, or re-subscription. The terminal editor mod.rs wires the model identically, add_model 3015 and subscribe 3017, and its settings handler 3380-3408 matches only cursor and autocomplete and autosuggestion events with a catch-all arm at 3406 swallowing any mode toggle. Mode is read live: vim_mode_enabled 1952-1953 is supports_vim_mode and the live AppEditorSettings boolean. keymap_context 2388-2405 inserts the Vim flags only when vim_mode is Some, gating the id-Vim and not-id-Vim forks in actions.rs, 16 occurrences across 54-175 plus 593, that route via vim_keystroke and vim_user_insert 2081-2102, calling vim_model.update with only an is_self_focused guard and no vim_mode_enabled guard. So toggling vim_mode off stops dispatch but never resets the single VimFSA in VimModel, vim.rs 1800-1802, and the fields at vim.rs 38-54 (mode, showcmd, pending_action, pending_action_count, pending_operand_count, pending_visual_object, last_find_motion, register, dot_repeat_event) persist and resume stale. The engine is neither swapped, a concrete ModelHandle with no dyn, nor reset in place: force_insert_mode and interrupt exist around vim.rs 1825 and 1831 but only terminal insert-mode actions call them, never on a settings change. No editing_mode, Helix, or Kakoune engine exists, only the boolean vim_mode at settings/editor.rs 202, default false, read at 285-286. Nit: escape brace at 369 not 368.

_Evidence:_
- VimModel created once and subscribed once at construction, and the field is a concrete ModelHandle, not an enum or dyn. — `app/src/code/editor/view.rs:359-360` _(code)_
- AppEditorSettings observer body is just ctx.notify, with no rebuild or reset or re-subscribe. — `app/src/code/editor/view.rs:312-314` _(code)_
- Terminal settings handler has a catch-all arm that swallows a mode toggle. — `app/src/editor/view/mod.rs:3380-3408` _(code)_
- Routing is gated only by the Vim keymap context inserted when vim_mode is Some, and the forward path has only an is_self_focused guard. — `app/src/code/editor/view.rs:2081-2102, 2388-2405` _(code)_
- In-flight FSA fields persist across a toggle, and VimModel is a concrete VimFSA wrapper that is not swapped or reset. — `crates/vim/src/vim.rs:37-54, 1800-1833` _(code)_
- No editing_mode or Helix or Kakoune engine exists, only the boolean vim_mode, default false. — `app/src/settings/editor.rs:202, 285-286` _(code)_

- _Unblocks:_ Lets the design doc commit to explicit runtime mode-change semantics for the pluggable-engine model: decide whether editing_mode change drops+re-adds the engine Entity and re-subscribes, or swaps an inner boxed engine and resets it in place; and mandate discarding in-flight FSA state (pending operator/count/register/visual-object/dot-repeat) plus re-normalizing to the engine's default mode on switch — replacing today's wire-once, never-react behavior.
- _Residual / needs decision:_ Net-new and unspecified in code: swap-versus-reset of the engine Entity, the in-flight FSA discard policy on switch, and re-normalization to the default mode. Both editors must change symmetrically.
- _Verification:_ Opened all cited files and several uncited ones. Confirmed line-for-line: view.rs 312-314 (ctx.notify only), 359-360 (add_model plus subscribe once), 363-369 (one-time escape, gated), 1942 and 1947-1949 and 1952-1953 (live reads), 2081-2102 (only an is_self_focused guard, no vim_mode_enabled guard), 2388-2405 (Vim context only when vim_mode is Some). actions.rs grep counted 16 id-Vim forks across 54-175 and 593. vim.rs VimFSA fields at 38-54 as listed, and VimModel is a concrete struct holding fsa VimFSA at 1800-1802, no enum or dyn. editor.rs 202 vim_mode bool default false, 285-286 accessor. Terminal mod.rs 3015-3023 plus 3380-3408 with catch-all arm at 3406. Independent uncited checks: grepped all VimModel new and add_model and subscribe on vim_model across app/src, exactly two of each (one per editor), no reset or swap or re-subscribe path. Grepped interrupt and force_insert_mode callers, only the terminal editor for insert-mode actions (mod.rs 3738, 3986, 4086, 5388-5397, 5426, 5709, 5837, 5993), never a settings change, and the code editor never calls them. Two trivial non-material nits: escape block closing brace at 369 not 368, and the grep also hits mod_tests.rs. Neither affects any conclusion.

### A4-Q8 — CONFIRMED · confidence: high

**Q.** The report says registers "remain an app concern and generalize cleanly," but the FSA stores a vim-specific register: char (vim.rs:53), there is a vim-only app/src/vim_registers.rs, and a vim-only setting vim_unnamed_system_clipboard (editor.rs:211-220). Does the shared EditorCommand IR plus executor carry a single register model, and does vim_unnamed_system_clipboard become a generalized modal_* setting, stay Vim-only, or get a per-engine analog?

**A.** There is no "EditorCommand IR" today; the de-facto IR (VimEventType) and its executor carry one register model that is vim-specific to the bone: char-named registers all the way down (register_name: char on the events), a char-keyed HashMap store, vim's MotionType inside RegisterContent, and a single app-global VimRegisters singleton shared by both the code editor and the terminal command editor. vim_unnamed_system_clipboard is strictly Vim-only in code (name, gating, and a single functional consumer); whether it becomes modal_*, stays Vim-only, or gets a per-engine analog is an unmade design decision with no code basis yet.

I independently opened every cited file and re-ran the greps. The answer is accurate in all material respects.

1) No multi-engine IR. `grep -rn "EditorCommand" crates app/src` returns NOTHING — the type does not exist. There is no `modal_mode`/`editing_mode` editing-mode setting (the only `modal_` hits are `one_time_modal_model`, an unrelated launch-modal UI feature). `grep -rin "helix|kakoune"` over crates+app/src hits only non-engine code: external-editor command generation (app/src/util/file/external_editor/mod.rs:267-268 plus its tests), a BERT tokenizer vocab json, and a tree-sitter grammar README URL — none are modal-editing engines. So no Helix/Kakoune engine exists. The de-facto IR being generalized is VimEventType (crates/vim/src/vim.rs:539-599), executed by the VimHandler dispatch (~vim.rs:1981+). CONFIRMED.

2) Single, vim-specific register model — verified at every layer:
- FSA state `register: char` at vim.rs:52 (comment "Vim has char-named registers" at lines 50-51). Defaulted to the unnamed register '"' at construction (vim.rs:717) and reset to '"' in clear() (vim.rs:728); register chosen via the `"` command only if valid_register_name(c) (vim.rs:1132, 1608). CONFIRMED. (Note: the spec doc's own question cites vim.rs:53, off by one; the answer correctly uses 52.)
- IR vocabulary: `register_name: char` is hardcoded on the events — Paste (552), Operation (568), VisualOperator (582), VisualPaste read+write (586-587), and again in the handler dispatch (1986, 2014, 2020, 2021, 2034). `grep -c "register_name: char"` = 10. The "10 sites" count is EXACT.
- valid_register_name / BLACK_HOLE_REGISTER live in the shared vim crate (crates/vim/src/register.rs): black hole '_' at line 3, valid_register_name at lines 6-8 accepting only `a-zA-Z + * "` — pure vim vocabulary. CONFIRMED.
- Storage: one app-global VimRegisters SingletonEntity (app/src/vim_registers.rs): `registers: HashMap<char, RegisterContent>` (line 25), `RegisterContent { text: String, motion_type: vim::vim::MotionType }` (lines 12-15) — content payload keyed on a vim enum. CONFIRMED.
- Created once at app startup: `ctx.add_singleton_model(|_| VimRegisters::new());` at app/src/lib.rs:1707 (exact). Not per-engine/per-editor. CONFIRMED.
- Shared across BOTH editors: code editor app/src/code/editor/view/vim_handler.rs writes at 310/349/530/613 and reads at 593/692; terminal command editor reads at app/src/editor/view/mod.rs:2476/2731 and writes via app/src/editor/view/model/mod.rs:2806 — all on the SAME `VimRegisters::handle(ctx)` singleton. CONFIRMED.

Net: registers are NOT mode-neutral today; the IR carries vim register names (char) and vim paste semantics (MotionType). The report's "generalizes cleanly" is currently untested/contradicted by the char-typed IR fields.

3) vim_unnamed_system_clipboard is strictly Vim-only in code:
- Setting def app/src/settings/editor.rs:211-218: bool, default false, sync_to_cloud Globally, toml_path "text_editing.vim_unnamed_system_clipboard", description "Whether the Vim unnamed register uses the system clipboard." CONFIRMED.
- Functional consumption is a SINGLE site: the setting field `.vim_unnamed_system_clipboard` is read only inside VimRegisters::points_to_system_clipboard at app/src/vim_registers.rs:39 (that fn is the lone gate: '*'/'+' always hit the system clipboard; '"' only when the flag is on). Every other reference is UI plumbing (features_page toggle render/telemetry/dispatch) or the keymap flag. CONFIRMED.
- Surfaced as a UI toggle gated on VIM_MODE_CONTEXT_FLAG (app/src/settings_view/features_page.rs:506-519, builds context `... & id!(flags::VIM_MODE_CONTEXT_FLAG)`), and published as keymap context flag VIM_UNNAMED_SYSTEM_CLIPBOARD nested inside the `if *vim_mode.value()` block (app/src/workspace/view.rs:22107-22111); flag string "Vim_Unnamed_System_Clipboard" at app/src/settings_view/mod.rs:456. CONFIRMED.

So: the executor carries one vim-shaped register model, and vim_unnamed_system_clipboard is today 100% Vim-scoped (name, gating flag, single functional consumer). Whether the setting evolves to modal_*, stays Vim-only, or gets per-engine analogs, and whether register_name is neutralized in a future IR, are forward-looking design decisions with no current code basis.

_Evidence:_
- register: char field at vim.rs:52 (comment 'Vim has char-named registers' at 50-51); defaulted to '"' at 717, reset to '"' at 728; register chosen via " only if valid_register_name at 1132 and 1608 — `crates/vim/src/vim.rs:52,717,728,1132,1608` _(code)_
- VimEventType hardcodes register_name: char on Paste(552)/Operation(568)/VisualOperator(582) and read/write register names on VisualPaste(586-587); plus handler dispatch 1986,2014,2020,2021,2034; grep -c 'register_name: char' = exactly 10 — `crates/vim/src/vim.rs:552,568,582,586-587,1986,2014,2020,2021,2034` _(both)_
- valid_register_name accepts only a-zA-Z + * " and BLACK_HOLE_REGISTER='_' live in the shared vim crate — `crates/vim/src/register.rs:3,6-8` _(both)_
- VimRegisters is HashMap<char, RegisterContent> where RegisterContent { text: String, motion_type: vim::vim::MotionType } — `app/src/vim_registers.rs:4,12-15,25` _(code)_
- VimRegisters singleton created once at app startup via add_singleton_model (not per engine/editor) — `app/src/lib.rs:1707` _(code)_
- The SAME VimRegisters singleton is written/read by the code editor (write 310/349/530/613, read 593/692) and the terminal command editor (read mod.rs:2476/2731, write model/mod.rs:2806) — `app/src/code/editor/view/vim_handler.rs:309-316,348-355,529-530,592-593,612-614,690-692; app/src/editor/view/mod.rs:2474-2476,2730-2731; app/src/editor/view/model/mod.rs:2805-2806` _(code)_
- vim_unnamed_system_clipboard is a Vim-named bool setting (default false, cloud-synced, toml 'text_editing.vim_unnamed_system_clipboard') — `app/src/settings/editor.rs:211-218` _(code)_
- The setting field is functionally read at exactly one site, VimRegisters::points_to_system_clipboard, which decides whether '"' routes to the system clipboard — `app/src/vim_registers.rs:37-44 (setting read at :39)` _(code)_
- Setting surfaced as a UI toggle gated on VIM_MODE_CONTEXT_FLAG and published as keymap context flag VIM_UNNAMED_SYSTEM_CLIPBOARD nested under the vim_mode block — `app/src/settings_view/features_page.rs:506-519; app/src/workspace/view.rs:22107-22111; app/src/settings_view/mod.rs:456` _(code)_
- No EditorCommand type and no modal_/editing_mode editing-mode setting exist; helix/kakoune appear only in non-engine code (external_editor command gen, tokenizer vocab, grammar README) — `app/src/util/file/external_editor/mod.rs:267-268 (grep -rn EditorCommand returns nothing)` _(code)_

- _Unblocks:_ Lets the design doc decide the register seam: today registers are a single vim-shaped model (char names in the IR, char-keyed singleton store, vim MotionType payload) shared across both editors, and vim_unnamed_system_clipboard is strictly Vim-scoped. The doc must therefore explicitly choose (1) whether to neutralize register_name in the IR vs keep it vim-typed with shims, and (2) the fate of the clipboard setting (rename to modal_*, keep Vim-gated, or per-engine), rather than assuming registers "generalize cleanly" as-is.
- _Residual / needs decision:_ The product/architecture decisions are not settleable from code because the multi-engine layer does not exist: (a) whether a future shared IR keeps register_name: char or introduces a neutral RegisterRef with per-engine translation; (b) whether the store generalizes beyond HashMap<char, RegisterContent> to express Kakoune's per-selection register arrays and yank-register-on-delete semantics; (c) whether vim_unnamed_system_clipboard is renamed to a generalized modal_* setting, kept Vim-gated, or given per-engine analogs. RegisterContent already couples content to vim's MotionType, which would need neutralizing for non-vim paste models. These are unmade design choices, not current code facts.
- _Verification:_ I opened all cited files directly (crates/vim/src/vim.rs, crates/vim/src/register.rs, app/src/vim_registers.rs, app/src/lib.rs, app/src/settings/editor.rs, app/src/settings_view/features_page.rs, app/src/workspace/view.rs, app/src/settings_view/mod.rs) plus locations the answer did not cite (app/src/editor/view/model/mod.rs, and a full grep for every reader of the .vim_unnamed_system_clipboard field). Re-ran the load-bearing greps myself: `grep -c "register_name: char"` = 10 (EXACT match to the claim); `grep -rn "EditorCommand"` = no results (confirms no IR type); `grep -rin "helix|kakoune"` confirms no modal engine. Every line ref the answer gives lands on the asserted content. The construction default and clear() reset to '"' are at 717/728 as claimed; valid_register_name gating at 1132/1608 as claimed. The singleton at lib.rs:1707 is exact. The cross-editor sharing is confirmed: both the code editor's vim_handler.rs and the terminal editor's mod.rs/model/mod.rs operate on the same VimRegisters::handle(ctx) singleton. The setting is read functionally only inside points_to_system_clipboard (vim_registers.rs:39); all other references are UI/telemetry/keymap-flag plumbing. Minor imprecisions that do NOT affect the conclusion: (a) editor.rs block is 211-218 not 211-219 (closing brace at 218); (b) register.rs content spans lines 1-8, the answer's 3-7 captures it; (c) the helix/kakoune grep also surfaces a BERT tokenizer vocab json and a grammar README URL beyond external_editor, but none are modal-editing engines, so the substantive 'no Helix/Kakoune engine' claim stands. The spec doc's own question text cites vim.rs:53 for register: char; the actual line is 52, and the answer correctly uses 52.

### A4-Q9 — CONFIRMED · confidence: high

**Q.** The IR is already polluted with vim repeat semantics: VimEventType::Operation carries a replacement_text field kept empty except for Change when dot-repeated (vim.rs:565-572), and the FSA holds dot_repeat_event: Option<VimEvent> driven by for_dot_repeat (vim.rs:53-54). Vim dot, Helix dot, and Kakoune macros differ. Does the shared EditorCommand IR carry repeat/dot-repeat state, or does each engine own its repeat mechanism with the IR staying stateless?

**A.** Confirmed. There is no "EditorCommand" IR anywhere in the repo; the de-facto IR is VimEventType emitted by VimFSA into the VimHandler trait. The dot-repeat STATE MACHINE is fully per-engine FSA-internal state (dot_repeat_event + for_dot_repeat + insert tracking), never crossing the handler boundary. But the emitted IR is NOT repeat-neutral: Operation.replacement_text and InsertText.text are dot-repeat-derived payloads baked into the shared event/trait surface (VimHandler::operation even takes replacement_text: &str, threaded through BOTH the code-editor and terminal handlers). No macro (q/Q) recording exists anywhere, so Kakoune's model has zero representation. Every cited file:line was independently opened and verified; the only line-ref discrepancy (the spec's own 56-57 for dot_repeat_event) was already corrected by the answer to the accurate 53-54.

VERDICT: CONFIRMED. I opened every cited location plus uncited ones (full VimHandler trait, all InsertText producers, repo-wide EditorCommand/macro greps). All 14 evidence claims hold.

CURRENT STATE OF THE "SHARED IR":
- No type named EditorCommand exists. Repo-wide grep across BOTH crates/ and app/ returned zero hits (stronger than the answer's "graphed crates" qualifier). The "shared EditorCommand IR" is purely a report-proposed abstraction. The actual IR boundary: VimFSA emits VimEvent/VimEventType (struct at vim.rs:524-536, enum at vim.rs:538-599); the blanket impl<T: VimHandler> VimSubscriber for T (vim.rs:1849) dispatches those events into the VimHandler trait (vim.rs:1945-2064), the closest thing to a shared/abstract executor. App-level handlers implement VimHandler: app/src/code/editor/view/vim_handler.rs (code editor) and app/src/editor/view/mod.rs (terminal duplicate).

REPEAT STATE IS PER-ENGINE FSA-INTERNAL (stateless w.r.t. the handler boundary):
- dot_repeat_event: Option<VimEvent> is a private field of VimFSA (vim.rs:53-54 — NOTE the spec question itself mis-cited this as 56-57; the answer correctly uses 53-54), owned inside VimModel { fsa: VimFSA } (vim.rs:1800-1801).
- Set in typed_character via for_dot_repeat() (vim.rs:767-770); the mapping table for_dot_repeat (vim.rs:607-661) decides which events repeat and how (ChangeMode->Insert maps to InsertText at 627; ChangeMode->Replace maps to ReplaceChar at 638). NOTE: both Change AND Delete operators are dot-repeatable Operations (615-616), but only Change carries replacement_text — fully consistent with the answer's scoping.
- The '.' command is resolved entirely inside the FSA: handle_normal_nothing_pending clones dot_repeat_event.event_type and re-emits it (vim.rs:1038-1044). Dot count semantics live in compute_event_count (vim.rs:850-857).
- Insert-mode char accumulation for repeat is FSA-internal: dot_repeat_text_mut / handle_insert_char / handle_insert_mode_backspace / handle_insert_mode_delete (vim.rs:1683-1728), and count>1 insert replay in exit_insert_mode (vim.rs:865-892).
- None of this machinery is a handler parameter; it never leaves the FSA. The repeat MECHANISM is already per-engine and could stay so for Helix/Kakoune without touching the executor.

BUT THE EMITTED IR / EXECUTOR TRAIT IS NOT REPEAT-NEUTRAL (the report's "1:1, repeat-neutral" claim is wrong):
- VimEventType::Operation { operator, operand, register_name, replacement_text: String } (vim.rs:565-572). Doc comment states replacement_text is "kept empty for all operators except Change when it is dot-repeated." Produced empty by create_operation (vim.rs:1656-1663), back-filled only on the Change branch by dot_repeat_text_mut (vim.rs:1689-1697), then passed across the boundary into VimHandler::operation(..., replacement_text: &str, ...) (signature at vim.rs:1981-1989, param at 1987; emitted at vim.rs:1884-1896) and consumed in the handler at vim_handler.rs:325-326 (gated on operator==Change && !replacement_text.is_empty(), then model.insert(replacement_text,...)) with the terminal duplicate at app/src/editor/view/mod.rs:2199 (param) and 2325 (editor_model.insert(replacement_text, None, ctx)). So a vim-dot-repeat-only field rides inside the shared event AND the executor trait signature in BOTH handlers.
- VimEventType::InsertText { text, position } (vim.rs:574-577): I verified the ONLY construction sites that emit a fresh InsertText are for_dot_repeat's ChangeMode->Insert mapping (627) and exit_insert_mode's count>1 replay (886); normal typing emits InsertChar(c) per character (1707). So "this variant exists specifically to materialize dot-repeat/count-insert as a single replayed event" is accurate. Replayed via the handler at vim.rs:1903-1905.

MACRO MODEL (Kakoune q/Q): No macro recording exists at all. Grep for macro/record/replay/record_macro in crates/vim found nothing. vim.rs's only "register" is the char-named clipboard register field (vim.rs:50-52), set on the `"` prefix command (self.register = c at 1133 and 1609) and used for yank/paste, not macro recording. Kakoune's q/Q macro model has ZERO representation in the current IR or FSA.

BOTTOM LINE: The repeat machinery is already cleanly per-engine FSA state and can stay stateless at the IR/handler boundary EXCEPT that two emitted variants (Operation.replacement_text, InsertText.text) currently smuggle vim-dot-repeat payload through the shared event + the VimHandler::operation signature (threaded through both the code-editor and terminal handlers). Making the IR truly repeat-neutral requires extracting replacement_text off Operation (have the FSA emit a delete+InsertText pair, as it already does for the InsertText repeat path) so Helix/Kakoune don't inherit a Change-only field. Macro recording is a net-new surface with no existing plumbing.

_Evidence:_
- No EditorCommand type exists anywhere in the repo (crates AND app, not just graphed crates); the IR is VimEventType/VimEvent emitted by VimFSA into VimHandler — `crates/vim/src/vim.rs:524-599` _(both)_
- dot_repeat_event is a private FSA field (answer correctly uses 53-54; the spec question mis-cited 56-57), not a handler parameter — `crates/vim/src/vim.rs:53-54` _(code)_
- VimFSA is owned inside VimModel { fsa: VimFSA }, keeping repeat state engine-local — `crates/vim/src/vim.rs:1800-1801` _(code)_
- for_dot_repeat() is the FSA-internal mapping deciding repeatability and event remapping (ChangeMode->Insert->InsertText at 627, ChangeMode->Replace->ReplaceChar at 638; both Change and Delete repeatable as Operation at 615-616) — `crates/vim/src/vim.rs:607-661` _(code)_
- dot_repeat_event set in typed_character via for_dot_repeat; never exposed to handler — `crates/vim/src/vim.rs:767-770` _(code)_
- '.' is resolved inside the FSA by cloning dot_repeat_event.event_type and re-emitting — `crates/vim/src/vim.rs:1038-1044` _(code)_
- Operation.replacement_text is a dot-repeat-only field documented as empty except Change when dot-repeated — `crates/vim/src/vim.rs:565-572` _(code)_
- replacement_text produced empty by create_operation, back-filled only on the Change branch of dot_repeat_text_mut — `crates/vim/src/vim.rs:1656-1663,1689-1697` _(code)_
- replacement_text crosses the executor boundary: VimHandler::operation takes replacement_text:&str (param at 1987); emitted at 1884-1896 — `crates/vim/src/vim.rs:1981-1989` _(code)_
- Code-editor handler consumes the dot-repeat replacement_text by inserting it, gated on Change && non-empty — `app/src/code/editor/view/vim_handler.rs:325-326` _(code)_
- Terminal duplicate handler also threads replacement_text through operation() (param 2199) and inserts it (2325) — `app/src/editor/view/mod.rs:2193-2325` _(code)_
- InsertText.text is accumulated via dot_repeat_text_mut/handle_insert_char; the only fresh InsertText producers are for_dot_repeat (627) and exit_insert_mode (886) — normal typing emits InsertChar per char (1707) — `crates/vim/src/vim.rs:1683-1716` _(code)_
- count>1 insert replay materializes InsertText from dot_repeat_event in exit_insert_mode; replayed via handler at 1903-1905 — `crates/vim/src/vim.rs:865-892` _(code)_
- No macro (q/Q) recording mechanism exists in the vim crate; only the char-named clipboard register (set on " prefix at 1133/1609), used for yank/paste — `crates/vim/src/vim.rs:50-52` _(both)_

- _Unblocks:_ Lets the design doc decide the repeat/macro ownership boundary: keep repeat as per-engine FSA state (cheap, already true) while extracting the two vim-dot-repeat payload fields (Operation.replacement_text, InsertText.text) out of the shared event/executor trait so Helix/Kakoune don't inherit vim-shaped repeat fields, and scope macro recording (Kakoune q/Q) as net-new per-engine state rather than an IR concern.
- _Residual / needs decision:_ Whether the future shared IR SHOULD carry repeat state or stay stateless is a design decision, not code-verifiable. The code shows the choice is open: the repeat machinery (dot_repeat_event, for_dot_repeat, insert tracking) is already per-engine FSA-internal and can remain so, but achieving a truly repeat-neutral IR requires removing replacement_text from VimEventType::Operation (and from the VimHandler::operation trait signature in BOTH handlers) and likely emitting a delete+InsertText pair instead — confirming the report's "VimEventType maps 1:1, repeat-neutral" is inaccurate on these two variants. Kakoune's q/Q macro model has no existing plumbing to extend; it is net-new. How Helix's "repeat last insert" (.) maps versus vim's broader dot-repeat (operations + inserts) is a semantic design choice the code does not settle.
- _Verification:_ I independently opened all cited locations and several uncited ones. Confirmed: VimFSA struct (37-55), VimEvent/VimEventType (522-599), for_dot_repeat (607-661), typed_character (749-772), compute_event_count dot path (845-860), exit_insert_mode (865-892), '.' resolution (1038-1044), create_operation/create_visual_operator (1656-1679), dot_repeat_text_mut + handle_insert_char/backspace/delete (1683-1728), VimModel (1795-1838), blanket VimSubscriber dispatch with Operation (1884-1896) and InsertText (1903-1905), and the FULL VimHandler trait incl. operation signature with replacement_text: &str at line 1987. Verified both app handlers thread replacement_text: code editor vim_handler.rs (fn operation 144, param 150, model.insert at 325-326) and terminal mod.rs (fn operation 2193, param 2199, editor_model.insert at 2325). Repo-wide grep for EditorCommand across crates/ AND app/ returned zero hits — the answer's claim holds and is actually understated (it qualified "graphed crates"; it's absent everywhere). Grep for macro/record/replay/record_macro in crates/vim returned nothing, and the `register` field is a single char (clipboard register), set only on `"` prefix (1133, 1609) — no macro plumbing. I specifically hunted for: (a) line-ref drift — only discrepancy is the spec question's own 56-57 for dot_repeat_event, which the answer already corrected to the accurate 53-54; (b) app-vs-crates scope confusion — none, the answer cleanly separates the code editor handler from the terminal duplicate and cites both; (c) overgeneralization on InsertText "exists specifically for repeat" — verified by enumerating all InsertText constructions (no normal-typing producer); (d) a Change-vs-Delete nuance: for_dot_repeat marks both Change and Delete repeatable but only Change carries replacement_text — the answer's scoping is consistent with the code, not contradicted. No material error found.

### A4-Q10 — CONFIRMED · confidence: high

**Q.** Step D writes a dynamic format!('Modal:{engine}:{submode}') keymap context, but Context.set is HashSet of &'static str (context.rs:5) and every ContextPredicate holds &'static str (context.rs:11-17), with no FromStr/runtime-key path. Do we represent the per-engine submode cross-product as a CLOSED, compile-time-enumerated set of &'static str flags (same mechanism as today's VimNormalMode/VimVisualMode, view.rs:2388-2410), or does Axis 4 hard-require the Axis 2 interned/Cow context-string refactor (R3a) as a prerequisite?

**A.** Closed compile-time static-str flag table; the per-engine submode cross-product should be a compile-time-enumerated set of static-str flags, the same mechanism vim already uses; the Context type forbids non-static keys so the report dynamic-format keymap context is illustrative not insertable; the submode set is finite so a closed static table suffices and Axis 4 does not hard-require the Axis-2 interned or Cow refactor R3a.

Verified all cited lines accurate. Context holds static-str set and map; ContextPredicate variants hold static str; eval does static-str lookups; no FromStr leak or intern path; macros take only literals; mutators static-str-bound; escape-hatch modifier gets a mut Context. Code editor inserts Vim and VimNormalMode and collapses Visual to VimVisualMode; terminal inserts only Vim and VimNormalMode; consumer predicate uses id VimNormalMode. VimMode is a finite enum. All set-insert sites pass literal or const static str. SelectionSet exists. R3a is not a hard prerequisite unless keys come from arbitrary runtime user strings, which the report does not mandate.

_Evidence:_
- Context set and map are static str — `crates/warpui_core/src/keymap/context.rs:4-7` _(both)_
- ContextPredicate variants static str; eval static-str lookups — `crates/warpui_core/src/keymap/context.rs:9-18,101-119` _(code)_
- macros literals only; mutators static-str-bound; no FromStr leak intern — `crates/warpui_core/src/keymap/context.rs:22-51,89-98` _(both)_
- Code editor static flags Vim VimNormalMode; Visual collapsed to VimVisualMode — `app/src/code/editor/view.rs:2388-2410` _(both)_
- Terminal inserts only Vim VimNormalMode — `app/src/editor/view/mod.rs:8676-8700` _(code)_
- Consumer predicate id VimNormalMode — `app/src/editor/view/mod.rs:971` _(code)_
- Escape-hatch modifier gets mut Context — `app/src/editor/view/mod.rs:1382,1463` _(code)_
- VimMode finite enum — `crates/vim/src/vim.rs:14-20` _(both)_
- All set-insert sites literal or const static str — `app/src/workspace/view.rs:21942-22061` _(code)_
- SelectionSet exists — `crates/editor/src/content/selection.rs:73` _(both)_

- _Unblocks:_ Lets the design declare Axis 4 (pluggable modality) shippable standalone without taking the Axis-2 R3a context-string refactor onto its critical path: the Step-D seam should emit a fixed enum of compile-time &'static str submode flags (reusing the vim VimNormalMode mechanism), and the report's format!('Modal:{engine}:{submode}') should be rewritten as illustrative pseudocode mapping to a closed static table rather than a literal runtime insert.
- _Residual / needs decision:_ Design decisions not code facts: closed static-flag table vs adopting R3a interned or Cow strings, both compile; exact Helix and Kakoune submode names not yet in code; whether any future requirement wants user-defined custom modes, which would force R3a, is unverifiable from current code.
- _Verification:_ Checked context.rs line ranges against spec-cited 5 and 11-17, match exactly; read both keymap_context sites, terminal inserts only Vim VimNormalMode and code editor adds VimVisualMode, no overgeneralization; negative grep for FromStr leak intern empty; re-ran the set-insert survey, all static str; verified escape-hatch alias field invocation; confirmed SelectionSet; no shifted lines, no off counts.

### A4-Q11 — CONFIRMED · confidence: high

**Q.** vim_mode: bool, sync_to_cloud: Globally(RespectUserSyncSetting::Yes), toml_path text_editing.vim_mode_enabled, no storage_key (editor.rs:202-210). Migrating to editing_mode/modal_mode: how do mixed-version cloud-synced clients coexist — dual-write legacy bool + new enum, migrate-on-read via vim_mode_enabled() shim (editor.rs:285), and which value wins when an OLD client toggles the bool while a NEW client has set Helix?

**A.** Cloud sync is keyed by storage_key, per-key, last-write-wins, with the value an opaque serialized string and NO field-merge/timestamp at this layer. vim_mode's sync key is "VimModeEnabled" (derived from the setting STRUCT name, not the toml path); its TOML key is "vim_mode_enabled". There is NO dual-write infrastructure anywhere; the only bool->enum precedent (KeepThinkingExpanded -> ThinkingDisplayMode, same Globally sync class) is one-time migrate-on-read-then-DELETE under a NEW distinct storage_key, which does not solve cross-version coexistence. vim_mode_enabled() (editor.rs:285) is a pure READ shim and does nothing for the write/sync path. "Which value wins" is purely last-write-wins per storage_key -- the code encodes no enum-vs-bool precedence -- so an old client toggling the bool can silently override a new client unless the new enum lives under its own key AND the runtime stops deriving mode from the bool. (Independently verified; every cited line reference holds.)

VERDICT: CONFIRMED. I independently opened every cited file plus the un-cited startup-divergence path and the two usage sites; all line references are accurate and say what the answer asserts.

CODE FACTS (re-verified):

1) Sync identity = storage_key; per-key, last-write-wins, opaque string value. The cloud syncer keys every preference by storage_key. On a cloud change it OVERWRITES the local value for that key (maybe_sync_cloud_pref_to_local keys by storage_key at cloud_preferences_syncer.rs:832-835; the actual local overwrite is update_setting_with_storage_key at :926-930 gated on is_changing :910-920; runtime subscription :206-221 applies cloud->local ONLY after has_completed_initial_load :214). On a local change it debounce-pushes to cloud (:222-238). Startup direction is decided by a hash-divergence flag force_cloud_to_match_local / force_local_wins_on_startup (handle_initial_load :533-554), not a per-field merge. So effectively last-writer-wins per storage_key. Two cross-version guards confirmed: (a) a cloud value for a key whose syncing mode resolves to Never is ignored (cloud_syncing_mode_for_storage_key -> SyncToCloud::Never, :850-858); (b) a serialized cloud string that fails to deserialize into the client's expected type is dropped and local is kept (macros.rs:868-879 returns Err "Failed to parse updated value ... Not updating" when both from_file_value and serde fall through).

2) vim_mode's storage_key is "VimModeEnabled" (the SETTING STRUCT name), NOT "vim_mode_enabled". With a toml_path and no explicit storage_key, the macro convenience arm sets storage_key: stringify!($name) where $name is the setting struct (macros.rs:217-218), and the @base arm's storage_key() returns that (macros.rs:278-280). The toml_key()/storage_key trait fallback (lib.rs:254-256; macro override macros.rs:282-288) governs only the TOML file key, which is the last toml_path segment "vim_mode_enabled" under [text_editing]. Confirmed in use: VimModeEnabled::storage_key() at features_page.rs:6133 and the literal string "VimModeEnabled" inserted into a sync-key set at terminal/input.rs:15945. CursorDisplayType (editor.rs:192-201) carries an EXPLICIT storage_key "CursorDisplayType" differing from its toml last-segment "cursor_display_type" -- proving storage_key and toml key are independent and a new enum can/must choose its own unique storage_key. CursorDisplayType was born an enum, NOT a bool->enum migration, so it is not the precedent the question implies.

3) The real bool->enum precedent: KeepThinkingExpanded (bool) -> ThinkingDisplayMode (enum) at initializer.rs:121-171. Migrate-on-read, ONE-TIME, gated on "new key present" (read_value("ThinkingDisplayMode").is_some() -> return at :140-148), reads the old bool directly from prefs (:153-158), maps true->AlwaysShow (:160-164), then DELETES the old key remove_value("KeepThinkingExpanded") (:166-169). NOT dual-write. ThinkingDisplayMode is private:false, SyncToCloud::Globally(RespectUserSyncSetting::Yes) (ai.rs:361-369) -- the SAME sync class as vim_mode -- under a NEW distinct storage_key. Its "Remove ~6 weeks from 3/19/26" TODO (:133) shows it bets old clients churn out; it provides NO cross-version coexistence contract.

4) vim_mode_enabled() (editor.rs:285-287) is `*self.vim_mode.value()` -- a pure in-memory READ shim. It cannot mediate the WRITE/sync path across client versions; the adversarial angle (a read shim does nothing for sync) is correct.

5) No dual-write infrastructure exists in the settings layer; migrations are migrate-once-then-delete. schema_tests.rs enforce non-empty storage_keys (no_empty_storage_keys :28-35) and uniqueness (no_duplicate_storage_keys :62-84), so a new modal_mode enum cannot share "VimModeEnabled" while the old setting exists -- it must take its own key. NUANCE I VERIFIED: the duplicate-key assert is `#[cfg(not(test))]`-gated (:79), i.e. enforced in the real app build but skipped under cfg(test) because test fixtures intentionally overlap keys; this does not weaken the conclusion.

"WHICH VALUE WINS" (old client toggles bool, new client set Helix): the sync layer encodes ONLY last-write-wins per storage_key -- no automatic enum-vs-bool precedence or merge. Two concrete design paths and their code-determined outcomes:
- New enum under a NEW distinct key (e.g. "ModalMode"): the old client never reads/writes "ModalMode", so it cannot directly clobber the enum object. But the old client toggling "VimModeEnabled" pushes a bool to cloud -> the new client receives a CloudPreferencesUpdated for "VimModeEnabled" and overwrites its local bool (:206-221, :832-930). If the new runtime still derives effective mode from VimModeEnabled (as today's vim_mode_enabled() shim does), that stale bool silently flips the new client out of Helix even though "ModalMode" is untouched. Whichever value the runtime READS wins -- and that read precedence is NOT encoded today.
- New enum REUSING "VimModeEnabled" with an enum type: cross-version type collision on one cloud object -- old writes bool, new writes enum, last-write-wins with no merge; each version drops the other's value on deserialize failure (macros.rs:868-879), so the cloud object thrashes bool<->enum on every change. This is exactly the corruption mode the question warns about, and it is additionally illegal per the uniqueness test.

Conclusion on the code-verifiable part: a distinct storage_key for the enum is effectively mandatory (uniqueness + type-safety), no dual-write helper exists to reuse, and the only shipped migration shape is one-time migrate-on-read-then-delete. The "precedence" question (which is authoritative under simultaneous cross-version edits) is a design decision the code does not answer; a read shim does not provide it.

_Evidence:_
- vim_mode is bool, Globally(RespectUserSyncSetting::Yes), toml_path text_editing.vim_mode_enabled, struct name VimModeEnabled, no explicit storage_key field — `app/src/settings/editor.rs:202-210` _(code)_
- vim_mode_enabled() is a pure read shim returning *self.vim_mode.value() — `app/src/settings/editor.rs:285-287` _(code)_
- No-explicit-storage_key+toml_path macro arm sets storage_key: stringify!($name) (=setting struct name, so vim_mode -> VimModeEnabled); @base storage_key() returns it — `crates/settings/src/macros.rs:217-218,278-280` _(code)_
- toml_key() = last segment of toml_path (toml_path_storage_key) for settings with toml_path; trait-level fallback toml_key()=storage_key() only for settings without toml_path — `crates/settings/src/macros.rs:282-288 and crates/settings/src/lib.rs:254-256` _(code)_
- VimModeEnabled::storage_key() used as cloud sync identity in settings UI — `app/src/settings_view/features_page.rs:6133` _(code)_
- Literal string "VimModeEnabled" inserted into a sync-key set, gated on vim_mode_enabled() — `app/src/terminal/input.rs:15945` _(code)_
- Cloud->local sync keys by storage_key; runtime applies cloud update only after has_completed_initial_load, per storage_key — `app/src/settings/cloud_preferences_syncer.rs:206-221` _(code)_
- maybe_sync_cloud_pref_to_local keys by storage_key; Never-mode cloud value ignored (:850-858); local overwritten with cloud value when is_changing (:910-930) = last-write-wins — `app/src/settings/cloud_preferences_syncer.rs:832-930` _(code)_
- Startup conflict direction decided by hash-divergence force_cloud_to_match_local / force_local_wins_on_startup, not field merge — `app/src/settings/cloud_preferences_syncer.rs:533-554` _(code)_
- Cloud value that fails to deserialize into expected type is dropped (Err 'Failed to parse updated value ... Not updating'), keeping local — `crates/settings/src/macros.rs:868-879` _(code)_
- Real bool->enum precedent KeepThinkingExpanded->ThinkingDisplayMode: one-time migrate-on-read, gated on new-key-present return, reads old bool, maps true->AlwaysShow, then deletes old key; NOT dual-write; TODO remove ~6 weeks — `app/src/settings/initializer.rs:121-171` _(code)_
- ThinkingDisplayMode is private:false, Globally(RespectUserSyncSetting::Yes) (same sync class as vim_mode), defined via implement_setting_for_enum with its own distinct storage_key — `app/src/settings/ai.rs:361-369` _(code)_
- CursorDisplayType has explicit storage_key "CursorDisplayType" distinct from toml last segment cursor_display_type; born an enum, not migrated from a bool — `app/src/settings/editor.rs:192-201` _(code)_
- Schema tests enforce non-empty (no_empty_storage_keys) and unique (no_duplicate_storage_keys) storage_keys; uniqueness assert is cfg(not(test))-gated so it fires in real app build — `crates/settings/src/schema_tests.rs:28-35,62-84` _(code)_

- _Unblocks:_ Lets the design pick the migration contract for vim_mode -> modal_mode: (1) give the new enum its own unique storage_key (mandatory per schema uniqueness + type-safety; reusing VimModeEnabled causes bool<->enum thrash with silent drops); (2) reuse the proven one-time migrate-on-read-then-delete shape (KeepThinkingExpanded precedent) rather than inventing dual-write, while accepting it offers no cross-version coexistence; (3) recognize that vim_mode_enabled() as a read shim cannot mediate sync, so to stop an old client's bool toggle from flipping a new client out of Helix the runtime must stop deriving mode from the legacy bool once modal_mode is set (an explicit enum-wins read precedence the code must add).
- _Residual / needs decision:_ The actual coexistence CONTRACT is a design decision the code does not encode: (a) dual-write vs migrate-on-read -- no dual-write helper exists today, only one-time migrate-then-delete; (b) read precedence under simultaneous cross-version edits (enum authoritative vs bool authoritative) is unspecified in code; last-write-wins per storage_key is the only mechanism. The cloud backend's exact write semantics (server-side timestamp/versioning beyond the client's hash-divergence check and SyncQueue) were not traced into the server; client-side behavior is last-write-wins per key. I did not open the three lib.rs:415/580/623 supporting lines individually (substance confirmed via the syncer). Whether the team accepts the precedent's '~6 weeks old-client churn' bet for a public globally-synced vim_mode is a product/rollout choice, not settleable from code.
- _Verification:_ I attacked every load-bearing line reference by opening the file directly. All held: editor.rs:202-210/285-287, macros.rs:217-218/278-288/868-879, lib.rs:254-256, cloud_preferences_syncer.rs:206-221/832-930/533-554, initializer.rs:121-171, ai.rs:361-369, schema_tests.rs:28-84, features_page.rs:6133, terminal/input.rs:15945. I also opened the un-cited startup-divergence path (handle_initial_load :533-554) and the actual local-overwrite call (:926-930), neither of which the answer mis-stated. Nothing was shifted or overgeneralized. The storage_key-vs-toml_key distinction is exactly right (stringify!($name) for the no-explicit-key arm; toml_key = last path segment). The claim that CursorDisplayType is NOT a migration precedent (born an enum) is correct and was honestly flagged by the original answer. The KeepThinkingExpanded precedent is one-time migrate-on-read-then-delete, not dual-write -- confirmed including the new-key-present early return and remove_value. The last-write-wins-per-storage_key conclusion and the two cross-version corruption scenarios are sound and code-grounded. ONE NUANCE the original answer omitted: schema_tests.rs no_duplicate_storage_keys has its assert under #[cfg(not(test))] (line 79) due to intentional test-fixture key overlap, so uniqueness is enforced in the app build but not under cfg(test); this does not change the conclusion that a real new enum must take its own key. I did not separately open lib.rs:415/580/623 (cited as supporting storage_key being the secure-storage/cloud read/write key) -- but the cloud syncer independently confirms storage_key is the cloud identity, so the substance holds. No material error found.

### A4-Q12 — CONFIRMED · confidence: high

**Q.** Forked non-char vim keys (id(Vim)/!id(Vim), actions.rs:50-294) yield distinct named actions (VimEnter vs Enter), described as EDITABLE bindings a user can override/persist by name to keybindings.yaml. When Step D migrates them to id(ModalEditing)+ModalKeystroke (renaming/removing Vim-prefixed action names), does a user's existing persisted custom trigger silently orphan/no-op, or is there a name-migration map?

**A.** Confirmed. The spec question's premise is false and the answer correctly refutes it: the forked non-char Vim keys (VimEnter/VimBackspace/VimDelete/VimTab/VimShiftTab/VimShiftEnter) are FixedBinding (registered via app.register_fixed_bindings at actions.rs:50-287), NOT editable — they have no name, never appear in the settings UI, are never persistable, so renaming those action variants orphans nothing. Persistence is keyed by the EditableBinding NAME string (HashMap<String,PersistedTrigger>), not the Rust action variant. Exactly 3 distinct Vim-NAMED editable/persistable name strings exist: editor_view:vim_scroll_half_page_down, editor_view:vim_scroll_half_page_up, editor_view:vim_exit_insert_mode (the last shared by code + terminal editors). For those, dropping/renaming the name string silently orphans the persisted entry as a no-op (no warning, dead entry left in keybindings.yaml), and there is NO name-migration/alias map anywhere.

I independently opened every cited file plus uncited locations and re-ran the grep; all 12 evidence claims hold, with only trivial line-range generosity (noted below). The answer is CONFIRMED at high confidence.

1) PREMISE CORRECTION VERIFIED. actions.rs:44-287 is one app.register_fixed_bindings([...]) call (open line 50, close line 287). Every Vim fork inside is FixedBinding::new: enter/VimEnter (:56-60), numpadenter/VimEnter (:66-69), shift-enter/VimShiftEnter (:76-79), backspace/VimBackspace (:86-89), shift-backspace/VimBackspace (:96-99), delete/VimDelete (:106-109), tab/VimTab (:162-165), shift-tab/VimShiftTab (:172-175). The spec question text (line 699) itself asserts "These are EDITABLE bindings whose triggers a user can already override and persist" — the code directly contradicts that, exactly as the answer states.

2) FixedBinding is NOT reconfigurable / has NO name. keymap.rs:275 doc comment reads verbatim "This action can't be reconfigured with a custom key binding trigger"; the struct (277-286) has fields trigger/action/command_description/context_predicate/enabled_predicate/group/id — no name field. register_fixed_bindings (388-399) doc reads "These bindings are internal and cannot be changed once they are added". Confirmed.

3) Persistence keyed by EditableBinding NAME string. keyboard.rs:157 + 167-169: CustomKeybindings is HashMap<String, PersistedTrigger> documented "(Editable Binding Name) -> Keybinding". load path (37-56) iterates (name, trigger) and calls app.set_custom_trigger(name, ...) at :44/:47; the ONLY log::warn (49-53) fires for an unparsable trigger string, never for an unknown name. write_custom_keybinding (64-74) only does map.0.insert(name, ...) — nothing prunes orphaned keys. Confirmed.

4) set_custom_trigger -> update_custom_trigger, exact-match by name, no fallback. matcher.rs:137-141 set_custom_trigger calls update_custom_trigger(name, Some(trigger)). keymap.rs update_custom_trigger (422-433) filters BOTH editable_custom_action_bindings (b.name == name, :423-428) AND editable_bindings (b.name == name, :430-431); it never touches fixed_bindings. An unknown/renamed name matches zero bindings -> silent no-op. Confirmed.

5) Settings UI lists only editable bindings. keybindings.rs:754 ctx.editable_bindings().collect(); subsequent dedup sorts by description then name (759-761). Fixed Vim forks (no name) can never appear here. Confirmed.

6) EXACTLY 3 distinct persistable Vim-named editable bindings. My app-wide grep returns only: actions.rs:510 editor_view:vim_scroll_half_page_down (ctrl-d, :515), actions.rs:517 editor_view:vim_scroll_half_page_up (ctrl-u, :522), actions.rs:589 editor_view:vim_exit_insert_mode (ctrl-[, :594), and mod.rs:962 editor_view:vim_exit_insert_mode (ctrl-[, terminal) — the SAME name string, so 3 distinct strings total. (Other grep hits are test fixtures referencing the external editor "vim", unrelated.) Terminal editor also forks VimBackspace as FixedBinding at mod.rs:202-206. Confirmed.

7) NO migration/alias map. grep migrat|alias|legacy|old_name|rename across keyboard.rs + keymap.rs + matcher.rs returns a single hit, keymap.rs:651, a constructor comment in EditableBinding::new: "Explicitly not supporting registering legacy actions, as they will be removed when the conversion to editable bindings is complete" — this is the OPPOSITE of a migration mechanism and reinforces the answer's conclusion. get_binding_by_name (keymap.rs:379-386) is exact-match on editable_bindings_by_name with no alias fallback. Confirmed.

CONCLUSION FOR DESIGN (unchanged): forked non-char modal keys kept as fixed bindings carry zero persistence-orphan risk. The only data-loss surface is the 3 editable Vim-named bindings; if their name strings are dropped/renamed during a ModalEditing migration, existing persisted custom triggers silently die with no warning and leave dead entries in keybindings.yaml. No alias/migration/rename pass exists today.

_Evidence:_
- actions.rs:44-287 is a single register_fixed_bindings call; all Vim forks (VimEnter/VimShiftEnter/VimBackspace/VimDelete/VimTab/VimShiftTab) are FixedBinding::new, not editable — `app/src/code/editor/view/actions.rs:50-287` _(code)_
- The spec question itself (line 699) wrongly asserts these forks are EDITABLE/persistable bindings — directly contradicted by the code — `docs/specs/2026-06-13-improved-keybindings-design-readiness-questions.md:699` _(code)_
- FixedBinding doc says 'This action can't be reconfigured with a custom key binding trigger' and the struct has NO name field; register_fixed_bindings is 'internal and cannot be changed once they are added' — `crates/warpui_core/src/keymap.rs:275-286,388-399` _(both)_
- EditableBinding carries a name; register_editable_bindings doc states the name is used to override triggers via set_custom_trigger — `crates/warpui_core/src/keymap.rs:293-304,401-419` _(code)_
- update_custom_trigger filters editable_custom_action_bindings AND editable_bindings by name==name only; never touches fixed_bindings; unknown name = silent no-op — `crates/warpui_core/src/keymap.rs:422-433` _(code)_
- Custom keybindings file is HashMap<String,PersistedTrigger> documented '(Editable Binding Name) -> Keybinding', keyed on name not on Rust action variant — `app/src/keyboard.rs:155-169` _(code)_
- On load, each persisted (name,trigger) calls app.set_custom_trigger; only an unparsable trigger logs warn (49-53); an unknown name does not warn — `app/src/keyboard.rs:37-56` _(code)_
- write_custom_keybinding only map.0.insert(name,...); nothing prunes orphaned unknown names, so dead entries persist in keybindings.yaml — `app/src/keyboard.rs:64-74` _(code)_
- set_custom_trigger routes name straight to update_custom_trigger with no alias/fallback — `crates/warpui_core/src/keymap/matcher.rs:137-141` _(code)_
- Settings keybindings UI lists only ctx.editable_bindings(), so fixed Vim forks (no name) never appear / can't be customized — `app/src/settings_view/keybindings.rs:754` _(code)_
- Exactly 3 distinct persistable Vim-named editable bindings: vim_scroll_half_page_down (:510), vim_scroll_half_page_up (:517), vim_exit_insert_mode (:589 code editor; :962 terminal, same name string) — `app/src/code/editor/view/actions.rs:510,517,589; app/src/editor/view/mod.rs:962` _(both)_
- Terminal editor also forks VimBackspace as FixedBinding, not editable — `app/src/editor/view/mod.rs:202-206` _(code)_
- No keybinding name migration/alias mechanism exists; grep migrat|alias|legacy|rename across keyboard.rs/keymap.rs/matcher.rs yields only keymap.rs:651, a comment refusing legacy-action support (reinforces no migration) — `crates/warpui_core/src/keymap.rs:651; app/src/keyboard.rs; crates/warpui_core/src/keymap/matcher.rs` _(both)_

- _Unblocks:_ Lets Step D pick the migration seam safely: forked non-char keys can be renamed freely (never persistable), so the only data-loss guard needed is for the 3 editable Vim-named bindings — keep those name strings stable, add a name-alias map, or add a one-time rename pass in load_custom_keybindings, since today an unmatched name silently no-ops with no warning and leaves a dead entry in keybindings.yaml.
- _Residual / needs decision:_ Whether Step D actually renames the editable-binding NAME strings (vim_scroll_half_page_*, vim_exit_insert_mode) vs only the Rust action variants is a future design choice not present in code today; the ModalKeystroke/id(ModalEditing) naming lives only in the spec/report. Which mitigation to adopt (keep names stable, add an alias table in update_custom_trigger/get_binding_by_name, or a one-time keybindings.yaml rename pass in load_custom_keybindings) is unimplemented — code supports none and would silently orphan. Whether new modal non-char keys are forked as fixed (today's pattern, zero orphan risk) or promoted to editable (orphan risk + new override surface) is also an open Step D decision.
- _Verification:_ I opened actions.rs:40-339 (read the full register_fixed_bindings block + start of editable block), keymap.rs:270-447 and 640-664, keyboard.rs:30-169, matcher.rs:125-149, settings_view/keybindings.rs:748-761, and editor/view/mod.rs:198-209 and 955-966. I re-ran the grep for vim-named binding strings app-wide and for migrat|alias|legacy|rename in the three keybinding files. Every one of the answer's 12 evidence claims holds at the cited file:line. Specific adversarial checks: (a) confirmed actions.rs:50-294 is wholly inside register_fixed_bindings (open :50, close :287) and the Vim forks are FixedBinding, not EditableBinding — the spec question's own premise (line 699) is the thing that is false, and the answer is right to flag it; (b) re-counted vim-named editable name strings = exactly 3 distinct (vim_exit_insert_mode is shared by code+terminal editors), matching the answer; (c) verified update_custom_trigger iterates only editable collections and never fixed_bindings, so even a hand-written yaml entry can't reach a fixed binding; (d) the only 'legacy' grep hit (keymap.rs:651) is a constructor comment that refuses legacy-action support — it strengthens, not weakens, the 'no migration map' conclusion. Minor, non-material: the answer's matcher.rs range (134-141) is slightly generous (the fn body is 137-141; 134-136 is its doc comment), and the half_page key-binding lines are 515/522 while the answer cites the name lines 510/517 — both correctly paired with ctrl-d/ctrl-u. No overgeneralization, no app/-vs-crates scope confusion, no off-by count. The answer correctly scopes the orphan risk to editable bindings only and covers both code and terminal editors.

### A4-Q13 — CORRECTED · confidence: high

**Q.** What regression net guards the refactor of VimEventType→EditorCommand, the blanket subscriber (vim.rs:1849), and the two VimHandler impls? Are there existing keystroke-to-VimEvent (FSA-level) and keystroke-to-buffer-mutation (handler-level) golden/unit tests we extend, and will each new engine be validated by shared keystroke-trace fixtures asserting the emitted EditorCommand stream?

**A.** CONFIRMED thesis with two evidence fixes. There are ZERO FSA-level golden tests: crates/vim/src/vim.rs (2064 lines, the FSA) has no test module, and VimEventType is referenced in exactly one file repo-wide (vim.rs itself) while VimEvent appears in no test file — the IR-emission boundary the refactor touches is completely uncharacterized. The actual regression net is exactly 173 handler-level end-to-end keystroke→buffer-mutation tests (49 in app/src/code/editor/view/vim_handler_tests.rs, 124 in app/src/editor/view/vim_handler_tests.rs), but they assert only final buffer text / cursor / mode (buffer_text 84 sites, cursor_position 137, vim_mode 54) and never the emitted VimEvent/EditorCommand stream; the two files are largely duplicated re-implementations (34 identically-named tests, verified via comm -12) with NO shared keystroke-trace fixture format. No snapshot/golden/insta infrastructure exists; building a shared keystrokes→EditorCommand-vec fixture is net-new work. CORRECTIONS: (1) crates/editor/src/selection_tests.rs has 17 #[test], NOT 34 (the answer appears to have re-used the 34 shared-handler-test count); and its four cited add_cursor line refs 584/648/721/830 are all shifted — actual are 548 (test_new_cursor_at), 594 (test_multiselect_vertical_movement_with_goal), 667 (test_multiselect_vertical_extension_with_goal), 776 (test_multiselect_vertical_extension_overlap). (2) The snapshot grep as written returns 15 matches (all `view.snapshot(ctx)` method calls — the unescaped dot in `.snap` matched `.snapshot(`), not NONE; a corrected grep for `insta::`/`assert_snapshot!` returns nothing, so the substantive "no golden/snapshot infra" conclusion holds.

ARCHITECTURE BEING REFACTORED (verified):\n- Private FSA emits the IR: VimFSA::typed_character (crates/vim/src/vim.rs:749) and VimFSA::keypress (vim.rs:775) each return Option<VimEvent>. The IR types are defined at vim.rs:524 (pub struct VimEvent) and vim.rs:540 (pub enum VimEventType).\n- Public boundary VimModel::typed_character (vim.rs:1813) and VimModel::keypress (vim.rs:1819) call ctx.emit(event) — confirmed by reading lines 1800-1834.\n- Blanket lowering `impl<T> VimSubscriber for T where T: VimHandler` at vim.rs:1849 — confirmed verbatim. Two concrete VimHandler impls: `impl VimHandler for CodeEditorView` (app/src/code/editor/view/vim_handler.rs:23) and `impl VimHandler for EditorView` (app/src/editor/view/mod.rs:1959) — both confirmed verbatim.\n\n(1) FSA-LEVEL (keystroke→VimEvent) — NO TESTS. CONFIRMED. `grep -nE '#\\[test\\]|#\\[cfg\\(test\\)\\]|mod tests|fn test_' crates/vim/src/vim.rs` returns NOTHING; wc -l = 2064. `grep -rln 'VimEventType' --include=*.rs .` returns the single file crates/vim/src/vim.rs. `grep -rln 'VimEvent' --include=*test*.rs .` returns nothing (exit 1). So nothing drives typed_character/keypress and asserts the emitted VimEvent/VimEventType. The only vim-crate tests are pure-algorithm unit tests, confirmed present: crates/vim/src/{word_iterator_tests.rs, paragraph_iterator_tests.rs, matching_brackets_tests.rs} and crates/vim/src/text_objects/{word_tests.rs, block_tests.rs, quote_tests.rs, paragraph_tests.rs}.\n\n(2) HANDLER-LEVEL (keystroke→buffer mutation) — EXTENSIVE but BLACK-BOX. CONFIRMED.\n- Code editor: app/src/code/editor/view/vim_handler_tests.rs = 49 #[test], 1734 lines, mounted `#[path = \"vim_handler_tests.rs\"]` at vim_handler.rs:990, all 49 gated by FeatureFlag::VimCodeEditor (grep -c VimCodeEditor = 49). Driven via vim_user_insert (fn at line 103, routing &CodeEditorViewAction::VimUserTyped(UserInput::new(text)) at 106).\n- Terminal editor: app/src/editor/view/vim_handler_tests.rs = 124 #[test], 7833 lines, mounted via #[path] at app/src/editor/view/mod_tests.rs:4524-4525.\n- Total = 173 (49+124). CONFIRMED.\n- Neither handler test file references VimEvent/VimEventType/EditorCommand (grep -lnE over both → exit 1, no files). They assert only end-state: buffer_text (84 sites), cursor_position (137), vim_mode (54) in the code-editor file. So they are end-to-end characterization tests of the whole pipeline, not golden tests of the emitted IR. A VimEventType→EditorCommand rename preserving net buffer behavior passes these unchanged even if intermediate event semantics shift.\n- Parallel duplicates: comm -12 of the two files' `fn test_*` name sets = 34 shared. CONFIRMED. No shared keystrokes→outcome fixture table; each surface has its own harness.\n\n(3) SELECTION-MODEL PRIMITIVES — CORRECTED COUNT/LINES, substance holds. crates/editor/src/selection_tests.rs has 17 #[test] (NOT 34; recounted with all test-attr forms). The multi-cursor tests are real but at different lines than cited: test_new_cursor_at:548, test_multiselect_vertical_movement_with_goal:594, test_multiselect_vertical_extension_with_goal:667, test_multiselect_vertical_extension_overlap:776 (the answer cited 584/648/721/830 and slightly mis-named two as movement_..._extension/overlap). app/src/code/editor/model_tests.rs:436 does call editor.add_cursor_at(14.into(), ctx) — CONFIRMED. So the substantive point (the Helix multi-selection substrate is directly unit-tested independent of the vim FSA) holds.\n\n(4) NO snapshot/golden/insta infra — substantively CONFIRMED, evidence wording corrected. The grep `'insta::|assert_snapshot|golden|.snap'` returns 15 matches, but ALL are `view.snapshot(ctx)` buffer/view method calls (the unescaped `.` in `.snap` matched `.snapshot(`); none are snapshot-testing infrastructure. A corrected grep `'insta::|assert_snapshot!|assert_snapshot('` returns nothing (exit 1). So there is genuinely no insta/golden snapshot test harness.\n\nNET: thesis intact. The refactor is guarded by 173 end-to-end handler tests + selection-model + pure-algorithm unit tests, but the exact layer the question worries about — keystroke→VimEvent IR emission — has no golden test at all, and there is no shared fixture format a new engine (Helix/Kakoune) could drop into. The adversarial premise holds: clean FSA layering exists, but without IR-level golden tests the VimEventType→EditorCommand widening can silently change vim behavior in any way that still nets the same final buffer state and would only be caught incidentally.

_Evidence:_
- vim.rs (the FSA) is 2064 lines and has zero tests — no #[test], #[cfg(test)], or test module — `crates/vim/src/vim.rs (wc -l = 2064; grep -nE '#[test]|#[cfg(test)]|mod tests|fn test_' returns NONE)` _(code)_
- VimEventType is referenced in only one file repo-wide (vim.rs itself); VimEvent appears in no test file — `crates/vim/src/vim.rs (grep -rln 'VimEventType' --include=*.rs . → single file; grep -rln 'VimEvent' --include=*test*.rs . → exit 1, none)` _(code)_
- FSA emits the IR at the public boundary: VimModel::typed_character and VimModel::keypress call ctx.emit(event); the private fsa.typed_character/keypress return Option<VimEvent> — `crates/vim/src/vim.rs:1813-1822 (emit); vim.rs:749 (fn typed_character ->Option<VimEvent>), vim.rs:775 (fn keypress ->Option<VimEvent>)` _(both)_
- IR types defined: pub struct VimEvent at 524, pub enum VimEventType at 540 — `crates/vim/src/vim.rs:524,540` _(code)_
- Blanket impl<T> VimSubscriber for T where T: VimHandler; two concrete impls: CodeEditorView and EditorView — `crates/vim/src/vim.rs:1849; app/src/code/editor/view/vim_handler.rs:23 (impl VimHandler for CodeEditorView); app/src/editor/view/mod.rs:1959 (impl VimHandler for EditorView)` _(both)_
- Code-editor handler tests: 49 #[test], 1734 lines, mounted via #[path] at vim_handler.rs:990, all 49 gated FeatureFlag::VimCodeEditor; driven via vim_user_insert routing VimUserTyped(UserInput::new(text)) — `app/src/code/editor/view/vim_handler_tests.rs (grep -c #[test]=49; grep -c VimCodeEditor=49; fn vim_user_insert:103, VimUserTyped/UserInput::new:106); app/src/code/editor/view/vim_handler.rs:990` _(code)_
- Terminal handler tests: 124 #[test], 7833 lines, mounted via #[path] at mod_tests.rs:4524-4525. Total handler tests = 173 — `app/src/editor/view/vim_handler_tests.rs (grep -c #[test]=124, wc -l=7833); app/src/editor/view/mod_tests.rs:4524-4525` _(code)_
- Neither handler test file references VimEvent/VimEventType/EditorCommand; they assert only final buffer_text (84)/cursor_position (137)/vim_mode (54) — `grep -lnE 'VimEvent|EditorCommand|VimEventType' on both files → none; grep -c on app/src/code/editor/view/vim_handler_tests.rs: buffer_text=84, cursor_position=137, vim_mode=54` _(code)_
- The two handler test files are parallel duplicates: exactly 34 identically-named test fns across both, no shared fixture format — `comm -12 of `fn test_*` names: app/src/code/editor/view/vim_handler_tests.rs vs app/src/editor/view/vim_handler_tests.rs → 34` _(code)_
- CORRECTED: selection_tests.rs has 17 #[test], NOT 34; multi-cursor tests exist but at lines 548/594/667/776 not the cited 584/648/721/830; model_tests.rs:436 calls add_cursor_at — `crates/editor/src/selection_tests.rs (grep -c #[test]=17; test_new_cursor_at:548, test_multiselect_vertical_movement_with_goal:594, test_multiselect_vertical_extension_with_goal:667, test_multiselect_vertical_extension_overlap:776); app/src/code/editor/model_tests.rs:436 (editor.add_cursor_at(14.into(), ctx))` _(code)_
- vim crate's only tests cover pure reusable buffer algorithms, not FSA dispatch — `crates/vim/src/word_iterator_tests.rs; paragraph_iterator_tests.rs; matching_brackets_tests.rs; text_objects/{word_tests,block_tests,quote_tests,paragraph_tests}.rs` _(code)_
- CORRECTED EVIDENCE WORDING, conclusion holds: the broad grep returns 15 matches but all are view.snapshot(ctx) method calls (unescaped dot in '.snap'); a literal grep for insta::/assert_snapshot! returns nothing — no snapshot/golden test infrastructure exists — `grep -rnE 'insta::|assert_snapshot|golden|.snap' crates/vim app/src/code/editor app/src/editor → 15 hits all '.snapshot('; grep -rlnE 'insta::|assert_snapshot!|assert_snapshot(' → exit 1 (none)` _(code)_

- _Unblocks:_ Lets the design doc state precisely what regression scaffolding must be BUILT before the VimEventType→EditorCommand refactor: there is no FSA/IR golden net to "extend" (it does not exist), so Step A/B must add keystroke→emitted-EditorCommand snapshot tests as a prerequisite; the existing ~173 handler tests can serve as behavior-preservation characterization but cannot prove IR equivalence; and the per-engine validation bar must define a NEW shared keystroke-trace fixture format (not reuse one) and likely de-duplicate the two parallel handler harnesses.
- _Residual / needs decision:_ The design-decision half of the question ("will each new engine be validated by shared keystroke-trace fixtures asserting the emitted EditorCommand stream?") is not a current code fact — no such shared fixture format exists, so adopting one is net-new work. Open choices unchanged from original: (a) add FSA-level golden tests snapshotting the VimEvent/EditorCommand vec per keystroke before/while renaming VimEventType→EditorCommand (today the IR boundary has zero direct coverage); (b) unify the two duplicated handler harnesses (34 overlapping tests) into one shared keystrokes→outcome table reusable by vim/helix/kakoune; (c) decide whether end-state buffer assertions suffice or an IR-stream assertion is required to catch behavior-preserving-but-semantics-changing IR edits. I did not exhaustively re-verify the terminal handler file's per-assertion counts (selected_ranges/buffer_text) since the code-editor file was confirmed and the thesis does not hinge on the exact terminal-side breakdown. A4-Q16's Helix per-keystroke multi-selection frame/perf characterization remains a separate, untested question.
- _Verification:_ I independently opened/grepped every cited location plus one uncited (crates/vim/src/vim.rs:1800-1834 to confirm the emit boundary line numbers exactly, and the literal-escaped snapshot grep). CONFIRMED without change: vim.rs 2064 lines + zero tests; VimEventType in only vim.rs; VimEvent in no test file; FSA emit at VimModel::typed_character:1813 / keypress:1819; private fsa fns at 749/775 returning Option<VimEvent>; blanket VimSubscriber at 1849 verbatim; both VimHandler impls (vim_handler.rs:23 CodeEditorView, mod.rs:1959 EditorView) verbatim; 49 code-editor tests (1734 lines, all 49 gated VimCodeEditor, mounted at vim_handler.rs:990); 124 terminal tests (7833 lines, mounted mod_tests.rs:4524); total 173; no VimEvent/EditorCommand in either handler test file; assertion counts 84/137/54; 34 shared test names via comm -12; pure-algo vim tests present; VimEvent:524/VimEventType:540. TWO MATERIAL CORRECTIONS FOUND: (1) selection_tests.rs has 17 #[test], NOT 34 (likely a copy of the 34 shared-handler-test figure), and the four add_cursor line refs 584/648/721/830 are all shifted — actual 548/594/667/776 — with two test names slightly mis-stated (the variants are test_multiselect_vertical_extension_with_goal and test_multiselect_vertical_extension_overlap). The substance — multi-cursor primitives directly unit-tested independent of the FSA, and model_tests.rs:436 add_cursor_at — is correct. (2) The evidence claim that the snapshot/golden grep 'returns NONE' is literally false: it returns 15 matches, all `view.snapshot(ctx)` method calls because the unescaped dot in '.snap' matches '.snapshot('. A corrected grep for insta::/assert_snapshot! returns nothing, so the substantive conclusion (no golden/snapshot infrastructure) is correct. Neither correction touches the core thesis: zero FSA/IR golden tests, 173 end-state handler tests, duplicated harnesses, no shared keystroke-trace fixture, IR boundary uncharacterized.

### A4-Q14 — CONFIRMED · confidence: high

**Q.** Step E says surface editing_mode as a dropdown, but vim_mode today is a toggle (ToggleVimMode -> toggle_and_save_value) with two dependent toggles (vim_unnamed_system_clipboard, vim_status_bar) and vim_status_bar drives a runtime status-bar mode indicator. (a) Where does the dropdown live and what options show when VimCodeEditor-gated; (b) do the vim-only sub-settings become modal_*, stay Vim-gated, or get dropped; (c) what does the runtime status bar SHOW for Helix submodes / selection-count vs the vim mode string?

**A.** A reusable Dropdown<FeaturesPageAction> infra already exists (template: code_editor_line_number_mode_dropdown) and the vim control has TWO UI surfaces that must both be reworked: a command-palette ToggleSettingActionPair (features_page.rs:490-504) and the settings-page VimModeWidget (features_page.rs:6105), which sits in text_editing_widgets right beside the line-number dropdown widget — the natural home. (a) An editing_mode dropdown is mechanically straightforward to host there, but the only engine that exists in code is Vim (VimMode{Normal,Insert,Visual(MotionType),Replace}); there is NO Helix/Kakoune engine anywhere (in fact "kakoune" appears nowhere in the repo, and "helix" only in external-editor CLI parsing), so a VimCodeEditor-gated dropdown can today only offer {off, Vim}. (b) The two sub-settings are currently conditionally visible only when vim_mode_enabled (VimModeWidget:6154) / gated by VIM_MODE_CONTEXT_FLAG in the palette; they are Vim-named settings (storage keys VimUnnamedSystemClipboard/VimStatusBar via storage_key(), toml text_editing.vim_*) and whether they become modal_* vs dropped is a product decision code cannot settle. (c) The runtime indicator (render_vim_status, terminal/input/common.rs:46) renders only vim_state.showcmd + a per-VimMode icon via an exhaustive match (Visual(_) collapses all visual submodes to ONE icon); VimState carries only mode+showcmd so there is NO selection count and NO Helix submode concept, and it lives ONLY in the terminal input editor — the code editor (VimCodeEditor surface) renders no runtime status bar at all.

VERDICT: CONFIRMED. I independently opened every cited file and re-ran the greps. All line references are accurate (a few are 1-3 lines wider/narrower than stated but land on the asserted code). One cosmetic imprecision: the answer says the only "helix"/"kakoune" strings are in external-editor parsing, but "kakoune" appears NOWHERE in the repo (.rs); only "helix" appears (external_editor/mod.rs:268 `"hx" | "helix"`). That strengthens, not weakens, the "no other engine" conclusion.

CODE FACTS (decision is downstream of these):

DROPDOWN INFRA EXISTS (the template to copy). Four pieces, all present for code_editor_line_number_mode and all verified: (1) ViewHandle<Dropdown<FeaturesPageAction>> field (features_page.rs:1385); (2) update_code_editor_line_number_mode_dropdown maps enum variants -> DropdownItem carrying SetCodeEditorLineNumberMode(value) and selects current by index (features_page.rs:3014-3047); (3) SetCodeEditorLineNumberMode(CodeEditorLineNumberMode) action variant (features_page.rs:812) + handler calling .set_value() (features_page.rs:2109-2113); (4) CodeEditorLineNumberModeWidget::render (features_page.rs:5789-5820) renders via render_dropdown_item passing &view.code_editor_line_number_mode_dropdown. Converting vim_mode (bool) to an editing_mode enum dropdown is a well-trodden path.

(a) WHERE THE DROPDOWN LIVES + OPTIONS. Two independent UI surfaces, both gated identically and both needing rework:
  - Command-palette toggle: ToggleSettingActionPair for ToggleVimMode (features_page.rs:490-504), gated by flags::VIM_MODE_CONTEXT_FLAG (= "Vim_Mode_Enabled", settings_view/mod.rs:455; inserted into the active context at workspace/view.rs:22108 — verified exact line).
  - Settings-page widget: VimModeWidget (features_page.rs:6105-6213), registered into text_editing_widgets (features_page.rs:2833) right after CodeEditorLineNumberModeWidget (features_page.rs:2826) — the dropdown's natural home.
  The editing engine RUNS only when FeatureFlag::VimCodeEditor.is_enabled() && AppEditorSettings::vim_mode_enabled() (code editor: view.rs:357,363,1953; terminal editor: editor/view/mod.rs:4920-4921). The ONLY engine in code is Vim: closed enum VimMode { Normal, Insert, Visual(MotionType), Replace } (crates/vim/src/vim.rs:14-20) with MotionType { Charwise, Linewise } (vim.rs:325-327). A repo-wide grep finds NO Helix/Kakoune engine/FSA/handler; "kakoune" is absent entirely, "helix" appears only in external_editor file:line:column parsing (util/file/external_editor/mod.rs:267-268). So a VimCodeEditor-gated dropdown can TODAY only offer {Off/None, Vim}.

(b) FATE OF THE TWO SUB-SETTINGS (code constraints only; final choice is product). vim_unnamed_system_clipboard and vim_status_bar are CONDITIONALLY VISIBLE under vim: in VimModeWidget they render only inside `if vim_mode_enabled { ... }` as a subgroup (features_page.rs:6154-6209); in the palette they are gated `context & id!(flags::VIM_MODE_CONTEXT_FLAG)` (features_page.rs:512,528). They are Vim-named at persistence: type/storage-key identifiers VimUnnamedSystemClipboard / VimStatusBar (used via ::storage_key() at 6169/6193), toml paths text_editing.vim_unnamed_system_clipboard / text_editing.vim_status_bar (settings/editor.rs:211-228), each an independent bool (vim_status_bar default true) with its own toggle_and_save_value handler (features_page.rs:1903-1920). Constraints: vim_unnamed_system_clipboard is a vim-register concept (no register subsystem for any other engine), and vim_status_bar is consumed only by the terminal vim status indicator. Whether these become modal_*, stay Vim-gated, or get dropped is NOT code-decidable.

(c) RUNTIME STATUS BAR CONTENT. Indicator is render_vim_status (terminal/input/common.rs:46-87). It renders exactly two things: vim_state.showcmd and a single icon chosen by an EXHAUSTIVE match over the closed VimMode enum — Normal -> green, Insert -> blended, Visual(_) -> ONE blue icon for all visual submodes, Replace -> red (common.rs:49-61). VimState (crates/vim/src/vim.rs:1791-1794) has ONLY `mode` and `showcmd` — NO selection-count field. Gated `vim_state.is_some() && *vim_status_bar.value()` and wired ONLY into terminal input surfaces (classic.rs:96,191; universal.rs:48,105), with the agent surface explicitly passing show_vim_status=false (agent.rs:148,622). The CODE editor (the very surface the dropdown gates) renders NO runtime status bar: it reads vim_state()/vim_mode() (code/editor/view.rs:1942-1949) but a tree-wide grep confirms render_vim_status/add_vim_status_to_stack are called ONLY in terminal/input. Adding Helix submodes or a selection count would require widening the VimMode enum AND the exhaustive icon match (won't compile otherwise) — the status bar is structurally vim-shaped, not engine-agnostic.

_Evidence:_
- vim_mode is a bool toggle whose handler calls toggle_and_save_value; satellites vim_unnamed_system_clipboard and vim_status_bar have identical handlers — `app/src/settings_view/features_page.rs:1896-1920` _(code)_
- vim_mode/vim_unnamed_system_clipboard/vim_status_bar are bool settings; vim_status_bar default true, others false; toml keys text_editing.vim_* — `app/src/settings/editor.rs:202-228` _(code)_
- Command-palette ToggleVimMode is a ToggleSettingActionPair gated by VIM_MODE_CONTEXT_FLAG; the two satellites are gated context & id!(VIM_MODE_CONTEXT_FLAG) — `app/src/settings_view/features_page.rs:490-536` _(code)_
- VIM_MODE_CONTEXT_FLAG = 'Vim_Mode_Enabled' — `app/src/settings_view/mod.rs:455` _(code)_
- The flag is inserted into the active context at runtime (verified exact line) — `app/src/workspace/view.rs:22108` _(code)_
- Settings-page VimModeWidget renders the two satellites only inside `if vim_mode_enabled`, as a subgroup; satellites accessed via VimUnnamedSystemClipboard::storage_key()/VimStatusBar::storage_key() — `app/src/settings_view/features_page.rs:6105-6213` _(code)_
- VimModeWidget is registered into text_editing_widgets right after CodeEditorLineNumberModeWidget — `app/src/settings_view/features_page.rs:2820-2834` _(code)_
- Dropdown infra template fully present: field (1385), update fn mapping enum->DropdownItem with Set* selecting current by index (3014-3047), Set* action variant (812) + handler calling set_value (2109-2113), widget render via render_dropdown_item passing the dropdown handle (5789-5820) — `app/src/settings_view/features_page.rs:1385,812,2109-2113,3014-3047,5789-5820` _(code)_
- Only engine in code is Vim: VimMode{Normal,Insert,Visual(MotionType),Replace}; MotionType only Charwise/Linewise — `crates/vim/src/vim.rs:14-20,325-327` _(both)_
- No Helix/Kakoune engine exists; 'kakoune' absent repo-wide, 'helix' only in external-editor CLI parsing — `app/src/util/file/external_editor/mod.rs:267-268` _(both)_
- Editing engine runs only when FeatureFlag::VimCodeEditor.is_enabled() && vim_mode_enabled() (code editor; terminal editor 4920-4921) — `app/src/code/editor/view.rs:357,363,1953` _(code)_
- render_vim_status renders only showcmd + a per-VimMode icon via exhaustive match; Visual(_) collapses to one icon; VimState has only mode+showcmd (no selection count) — `app/src/terminal/input/common.rs:46-87 + crates/vim/src/vim.rs:1791-1794` _(both)_
- Status bar gated vim_state.is_some() && vim_status_bar.value(); wired only into terminal input (classic/universal); agent passes show_vim_status=false; not called anywhere in code editor — `app/src/terminal/input/classic.rs:96,191;app/src/terminal/input/universal.rs:48,105;app/src/terminal/input/agent.rs:148,622` _(both)_
- Code editor (VimCodeEditor) exposes vim_state()/vim_mode() but never calls render_vim_status (tree-wide grep) — no runtime status bar on that surface — `app/src/code/editor/view.rs:1942-1949` _(both)_

- _Unblocks:_ Lets Step E scope the settings-UI rework precisely: (1) host an editing_mode dropdown in text_editing_widgets modeled on code_editor_line_number_mode_dropdown, replacing BOTH the VimModeWidget switch AND the command-palette ToggleSettingActionPair; (2) decide conditional-visibility of vim sub-settings per selected engine (today gated on a bool, easily generalized to 'engine == Vim'); (3) recognize the runtime indicator is a separate, terminal-only, vim-shaped surface (render_vim_status with an exhaustive VimMode match and no selection count) that must be widened/relocated — not a free side effect of the dropdown swap.
- _Residual / needs decision:_ Each sub-question smuggles a product/architecture decision code cannot settle: (a) WHICH options editing_mode exposes — only Vim exists, so anything beyond {off, Vim} requires building new engines first; (b) whether vim_unnamed_system_clipboard/vim_status_bar should be renamed modal_*, kept Vim-gated, or dropped — code only shows they are Vim-named, independently toggleable bools shown only when vim is active; no other engine exists to share the register/status concept; (c) what a Helix submode / selection-count indicator should display — there is no Helix submode enum, no selection-count field on VimState, and the icon match is exhaustive over the closed VimMode enum, so the design must define new submode states AND decide whether the indicator also appears on the code-editor surface (which has no runtime status bar today) or stays terminal-only. I did not exhaustively trace the legacy/agent input render paths beyond the cited gates, but the call-site grep confirms no code-editor status bar.
- _Verification:_ I opened all cited files and re-ran the greps independently. Verified: (1) VimMode enum at vim.rs:14-20 (Normal,Insert,Visual(MotionType),Replace) and MotionType:325-327 (Charwise,Linewise) — exhaustive match in common.rs:49-61 with Visual(_) -> single icon. (2) VimState struct vim.rs:1791-1794 has ONLY `mode` and `showcmd` — confirmed no selection-count field. (3) settings/editor.rs:202-228 vim_mode default false, vim_unnamed_system_clipboard default false, vim_status_bar default true, all bool, toml text_editing.vim_*. (4) features_page.rs:490-504 ToggleVimMode ToggleSettingActionPair gated VIM_MODE_CONTEXT_FLAG; 506-536 satellites gated `context & id!(VIM_MODE_CONTEXT_FLAG)`; 1896-1920 all three handlers call toggle_and_save_value; 2826/2833 widget registration order; 6105-6213 VimModeWidget with satellites only inside `if vim_mode_enabled` (6154-6209) accessed via ::storage_key(). (5) Dropdown infra: field 1385, action variant 812, handler 2109-2113 (set_value), update fn 3014-3047, and the widget render fn is at 5789-5820 (struct at 5780) calling render_dropdown_item at 5800-5816 with &view.code_editor_line_number_mode_dropdown — the answer's '5790-5820' lands exactly on this render fn (slightly tighter than literal but correct). (6) mod.rs:455 flag string; workspace/view.rs:22108 inserts it (verified exact, not shifted). (7) render_vim_status/add_vim_status_to_stack callers found ONLY in terminal/input/classic.rs and universal.rs; gating `vim_state.is_some() && *vim_status_bar.value()` at classic.rs:96 and universal.rs:48; agent.rs:148,622 pass show_vim_status=false; NO call in code/editor/view.rs. (8) Code editor gating view.rs:357 (FeatureFlag::VimCodeEditor.is_enabled()), 363, 1953; reads vim_mode()/vim_state() at 1942-1949. ONE imprecision: the answer implies 'kakoune' strings exist in external_editor, but a repo-wide grep finds 'kakoune' nowhere and 'helix' only at external_editor/mod.rs:268 — this strengthens the no-other-engine claim. Also the code-editor read range is more precisely 1942-1949 than the cited 1943-1947, and terminal gating 4920-4921 vs cited 4921 — both immaterial. No material error found; no overgeneralization, no app-vs-crates confusion, no shifted refs of consequence.

### A4-Q15 — CONFIRMED · confidence: high

**Q.** Cursor shape is derived from vim_mode: Option<VimMode> on the render element (element.rs:203) while users also have an independent CursorDisplayType setting (element.rs:51, editor.rs:192). When a modal mode is active, does the mode's per-submode cursor (block in normal/visual, underline in replace, bar in insert) OVERRIDE the user CursorDisplayType, and does that ownership move into the Step D proposed ModalCursorState?

**A.** Yes — whenever a vim submode is active the per-submode cursor UNCONDITIONALLY overrides the user CursorDisplayType in both editors; the user setting is consulted only in the vim-OFF fallback. The two editors are asymmetric: the terminal EditorElement falls back to the user setting (gated by allow_user_cursor_preference, today set true only at app/src/terminal/input.rs:2840), while the code editor's RichTextElement hardcodes Bar and never threads the user setting in at all. Step D's ModalCursorState (active flag + CursorDisplayType) captures only the active-mode shape; it does NOT encode the precedence-vs-user-setting fallback, which today lives in the element's unwrap_or(...) arm. All independently verified.

CONFIRMED after opening every cited file:line plus uncited corroborating locations.

Two parallel cursor-derivation functions, structurally identical in the mode-active arm but DIVERGENT in the fallback arm — verified these are the ONLY two (grep for VimMode->CursorDisplayType mapping returns exactly element.rs:1537 and mod.rs:966):

CODE EDITOR — crates/editor/src/render/element/mod.rs:963-971 cursor_display_type(): self.vim_mode.map(|m| Normal|Visual(_)=>Block, Replace=>Underline, Insert=>Bar).unwrap_or(CursorDisplayType::Bar). VERIFIED verbatim. The RichTextElement struct (mod.rs:127) carries ONLY vim_mode: Option<VimMode>; its constructor (mod.rs:416-441) takes no cursor-preference param (verified param list: model, parent_view, display_options, display_state, vim_mode, vim_visual_tails). The code editor view passes only self.vim_mode(app) into RichTextElement::new (code/editor/view.rs:2191-2198, verified). So vim-on => mode mapping; vim-off => hardcoded Bar, user setting never reachable.

TERMINAL EDITOR — app/src/editor/view/element.rs:1534-1542 get_cursor_type(): same mode arms, .unwrap_or(self.preferred_cursor_type). VERIFIED. preferred_cursor_type field at element.rs:191; constructor param cursor_display_type at element.rs:229; assignment preferred_cursor_type: cursor_display_type.unwrap_or_default() at element.rs:257. All VERIFIED. The Option comes from self.cursor_display_override at view/mod.rs:8645, which is Some(*editor_settings.cursor_display_type) only when options.allow_user_cursor_preference else None (view/mod.rs:3028-3032). VERIFIED. allow_user_cursor_preference defaults false (view/mod.rs:1489 and 1583, two default builders); repo grep over app/src shows the ONLY true is app/src/terminal/input.rs:2840 (terminal command input). VERIFIED. Additionally (uncited) view/mod.rs:3389-3392 live-updates cursor_display_override from the user setting only when is_some() — corroborates, does not contradict.

CursorDisplayType is an independent user setting (app/src/settings/editor.rs:192-201) whose enum default is Bar (editor.rs:51-56, #[default] Bar). VERIFIED. So Insert==Bar coincidentally equals the default, masking the conflict for default users but silently overriding users who picked Block/Underline.

VimMode is a closed 4-variant enum (crates/vim/src/vim.rs:14-20: Normal, Insert [#[default]], Visual(MotionType), Replace) — NO Select submode. VERIFIED. The doc question's "block in normal/select" example is therefore aspirational; a Helix/Kakoune select row does not exist in code. vim_mode() returns None when vim disabled (view/mod.rs:7642-7650, vim_state gated on vim_mode_enabled), which makes unwrap_or the off-state branch. VERIFIED.

STEP D / ModalCursorState — report .understand-anything/keybindings-architecture-report.md:214 proposes changing element field vim_mode: Option<VimMode> to modal: Option<ModalCursorState> (active flag + CursorDisplayType), keeping the fork as if self.modal.is_some(); report:202 has state() return "active flag + submode + desired cursor shape". VERIFIED. This formalizes the is_some() (active) arm only; it does NOT, as written, own the unwrap_or(...) fallback nor declare a per-(engine,submode) precedence vs the user CursorDisplayType. The answer's three consequences (code-editor would change behavior if it began deferring to the user setting; terminal opt-in fallback must be preserved or it regresses; Insert==Bar masks the conflict) all follow correctly from the verified code.

The adversarial framing is correct: today the mode wins almost everywhere (active vim submode overrides user CursorDisplayType in both surfaces; with mode inactive only the terminal command input honors the setting; the code editor never does), and the precedence table is undecided in code.

Minor note on the QUESTION (not the answer): the doc question cites "element.rs:51" for CursorDisplayType, but the enum actually lives at settings/editor.rs:51-56 — the answer correctly resolved this to settings/editor.rs.

_Evidence:_
- Code editor derives cursor from vim_mode and hardcodes Bar when vim is off, never consulting user CursorDisplayType — `crates/editor/src/render/element/mod.rs:963-971` _(code)_
- Code editor RichTextElement struct carries only vim_mode: Option<VimMode> (mod.rs:127); constructor (mod.rs:416-441) takes no cursor-preference param — `crates/editor/src/render/element/mod.rs:127,416-441` _(both)_
- Terminal get_cursor_type maps vim submodes to cursor and falls back to preferred_cursor_type when vim is off — `app/src/editor/view/element.rs:1534-1542` _(code)_
- preferred_cursor_type field (191) is populated from cursor_display_type.unwrap_or_default() (257); constructor param at 229 — `app/src/editor/view/element.rs:191,229,257` _(code)_
- cursor_display_override is Some(user CursorDisplayType) only when allow_user_cursor_preference true, else None; passed into element at 8645 — `app/src/editor/view/mod.rs:3028-3032,8645` _(code)_
- allow_user_cursor_preference defaults false (1489,1583); repo grep over app/src shows the ONLY true is the terminal command input — `app/src/editor/view/mod.rs:1489,1583; app/src/terminal/input.rs:2840` _(both)_
- CursorDisplayType is an independent user setting with enum default Bar — `app/src/settings/editor.rs:51-56,192-201` _(code)_
- VimMode is a closed 4-variant enum (Normal, Insert[default], Visual(MotionType), Replace) with no Select submode — `crates/vim/src/vim.rs:13-20` _(both)_
- vim_mode() returns None when vim disabled (vim_state gated on vim_mode_enabled), making unwrap_or the off branch — `app/src/editor/view/mod.rs:7642-7650` _(code)_
- Code editor view passes only self.vim_mode(app) into RichTextElement::new (no cursor pref) — `app/src/code/editor/view.rs:2191-2198` _(code)_
- Step D proposes element field modal: Option<ModalCursorState> = active flag + CursorDisplayType, keeping is_some fork; state() returns active flag + submode + desired cursor shape — `.understand-anything/keybindings-architecture-report.md:202,214` _(graph)_
- Uncited corroboration: cursor_display_override is live-updated from the user setting only when is_some(), confirming only opted-in editors track CursorDisplayType — `app/src/editor/view/mod.rs:3389-3392` _(code)_

- _Unblocks:_ Lets the design declare an explicit per-(engine, submode) cursor precedence table and decide whether Step D's ModalCursorState owns only the active-mode shape or also the user-setting fallback — preventing a silent regression in the terminal command input (which today honors CursorDisplayType when modal is inactive) and a silent behavior change in the code editor (which today never honors it).
- _Residual / needs decision:_ Non-code design decisions, unresolvable from current code: (1) the precedence table per (engine, submode) vs user CursorDisplayType — should any submode (e.g. helix/vim insert) DEFER to the user setting instead of forcing Bar? (2) Should ModalCursorState own only the active-mode shape (status quo) or also subsume the inactive fallback so behavior is unified — unifying would CHANGE the code editor (today never honors the user pref) and must preserve the terminal opt-in (allow_user_cursor_preference) or regress it. (3) Whether a Helix/Kakoune Select submode gets its own cursor row (none exists today). I did not exhaustively confirm that input.rs:2840 is literally the primary command-input editor's options block (file path and uniqueness of the true value make it overwhelmingly likely), and I did not grep crates/ for the field since the EditorViewOptions struct is app-defined.
- _Verification:_ Opened all 11 cited locations directly and confirmed each says exactly what the answer asserts: mod.rs:963-971 (verbatim unwrap_or(Bar)); mod.rs:127 + 416-441 (struct + 6-param constructor, no cursor pref); element.rs:1534-1542 (verbatim unwrap_or(preferred_cursor_type)); element.rs:191/229/257 (field/param/unwrap_or_default assignment); view/mod.rs:3028-3032 (override gated on allow_user_cursor_preference) + 8645 (passed into element); settings/editor.rs:51-56 (#[default] Bar) + 192-201 (setting); vim.rs:13-20 (4 variants, default Insert, no Select); view/mod.rs:7642-7650 (None when off); code/editor/view.rs:2191-2198 (only vim_mode threaded); report:202,214 (ModalCursorState). Re-ran the allow_user_cursor_preference grep over app/src myself: only one literal `true` (input.rs:2840); two default builders set false (1489,1583); 1524 passes options through. Independently checked COMPLETENESS via grep for VimMode->CursorDisplayType mapping and fn defs — exactly two derivation functions exist (element.rs:1534, mod.rs:963); no third path was missed. Inspected one location the answer did NOT cite (view/mod.rs:3389-3392) — it reinforces the model (only opted-in editors track the user setting). No shifted line refs, no app-vs-crates confusion, no overgeneralization, no off-by count. The only imprecision is in the QUESTION doc itself ("element.rs:51" for CursorDisplayType, actually settings/editor.rs:51), which the answer already resolved correctly. Nothing to correct.

---

