[← Back to index](../README.md)

## Axis 3 — Layering, precedence & unbinding

### A3-Q1 — CONFIRMED · confidence: high

**Q.** Of the 397 FixedBinding::new sites (all keystroke triggers), how many sit in a user-reachable view context a user would plausibly rebind (code-editor up/down/left/right/enter/tab/backspace/delete) versus genuinely internal/transient (modal-escape, onboarding callouts, lightbox nav), and which subset is in-scope for 'remappable' in this axis?

**A.** Confirmed. Of 397 FixedBinding::new sites (384 ::new + 13 ::new_per_platform, exact), 168 (~42%) sit under persistent, user-reachable editor contexts (CodeEditorView, EditorView, RichTextEditorView, Terminal, Input) and register the core nav/edit/selection keys (up/down/left/right/enter/tab/backspace/delete/home/end/word-move/select-*, with Vim forks). This empirically refutes the report's L9 (report.md:134) and line 234 claim that FixedBinding is "reserved for internal/transient." Genuinely internal/transient sites total ~92 (MODAL_TRANSIENT 68 + TEST_EXAMPLE 24), with ~48 semi-transient (BLOCKLIST_INLINE 44 + AUTH 4) and an 89-site app-chrome (OTHER_VIEW) gray zone whose remappable status is a product call. The full bucketing reproduces exactly (sum 397). Two minor refinements to supporting evidence, neither overturning a conclusion: (1) the EditableBinding-absence proof should grep .with_key_binding(...) not EditableBinding::new(...) (that call carries name+description, not keystrokes) — the proper check still shows no bare nav key is editable in the four editor files, though terminal/input.rs:1944 binds bare `tab` editable and actions.rs gives Delete an editable ctrl-d alias; (2) FixedBinding has no `name` but does carry a runtime `id: BindingId` (atomic counter), so "no stable handle" means no stable string name, not no id.

INDEPENDENTLY VERIFIED FACTS\n\n1) COUNT (exact). `grep -rhoE FixedBinding::new app/ crates/ --include=*.rs | wc -l` = 397; split = 384 plain `FixedBinding::new(` + 13 `FixedBinding::new_per_platform(`. Verified.\n\n2) TRIGGER KIND (verified). keymap.rs:511-533 `new()` builds `trigger: Trigger::Keystrokes(keys)` (line 525); keymap.rs:497-508 `new_per_platform` ends in `Self::new(...)` (line 507). So all 397 are keystroke triggers — the trigger-kind axis is moot, exactly as the question states.\n\n3) KILLER CLAIM (TRUE, directly read). app/src/code/editor/view/actions.rs:47-49: `text_entry = id!("CodeEditorView") & !id!("IMEOpen")`, `editable_state = text_entry & !id!(NON_EDITABLE_KEYMAP_CONTEXT)`. Under these (lines 50-229): enter/numpadenter/shift-enter (Vim forks), backspace/shift-backspace/delete (Vim forks), shift-up/down/left/right, up/down/left/right (Move*), home/end, tab/shift-tab (Vim forks), meta-left/right + shift word-movement (incl. new_per_platform mac vs linux/windows), shift-home/end, cmdorctrl-a SelectAll — all FixedBinding. Mirror contexts confirmed: terminal text editor `id!("EditorView") & !id!("IMEOpen")` (mod.rs:195+), terminal `id!("Terminal")` (init.rs:91, e.g. `FixedBinding::new("up", TerminalAction::Up, id!("Terminal") & !id!("IMEOpen"))`), notebook `id!("RichTextEditorView") & !id!("IMEOpen")` (view.rs:97; e.g. view.rs:132 `FixedBinding::new("delete", EditorViewAction::Delete, text_entry)`). None are modal/transient.\n\n4) PER-FILE EDITOR_CORE COUNTS (exact): editor/view/mod.rs 49, terminal/view/init.rs 38, code/editor/view/actions.rs 38, notebooks/editor/view.rs 35, terminal/input.rs 5, code/local_code_editor.rs 1, notebooks/notebook.rs 1, ai/ai_document_view.rs 1 = 168.\n\n5) BUCKETING (reproduced EXACTLY): EDITOR_CORE 168, OTHER_VIEW 89, MODAL_TRANSIENT 68, BLOCKLIST_INLINE 44, AUTH 4, TEST_EXAMPLE 24; sum = 397.\n\n6) REPORT CLAIMS REFUTED (verified verbatim). keybindings-architecture-report.md:134 (L9): "FixedBinding appears reserved for internal/transient bindings; user-facing shortcuts are EditableBinding." Line 234: "the only FixedBinding sites observed are internal/transient (modal escape, lightbox nav, onboarding callouts)." Both are empirically false given fact 3-5. (Note: the answer labels this "L9/§6"; the report has no literal §6 heading — the relevant text is the L9 table row at line 134 and the Honest-unknowns bullet at line 234. The cited line numbers are correct.)\n\n7) GRAY-ZONE spot-check confirmed: drive/index.rs has 9 FixedBinding and uses `id!("DriveIndex") & !id!("DisableDriveIndexVimKeybindings")` (lines 578, 583; toggle inserted at 5077) — vim-style nav, persistent app chrome.\n\nREFINEMENTS (supporting evidence, no conclusion changed)\n\nA) EditableBinding-absence method. `EditableBinding::new(name, description, action)` (keymap.rs:646) carries NO keystroke; the trigger attaches via `.with_key_binding("...")`. So grepping `EditableBinding::new` for key strings tests the wrong field (it matched the description "Delete" case-insensitively). The CORRECT check — grep `.with_key_binding("up|down|left|right|enter|numpadenter|backspace|delete|tab|home|end")` across the four editor files — returns NONE, so the conclusion (bare core nav keys are exclusively Fixed in those four files) HOLDS. Caveats the original framing missed: terminal/input.rs:1944 binds bare `tab` as an EditableBinding (Open completions) — input.rs is in EDITOR_CORE but not among the "four files"; and actions.rs:504-508 gives the Delete ACTION an editable `ctrl-d` alias while the bare `delete` key stays Fixed. So users can already rebind some of these actions to a different key, just not the bare nav key.\n\nB) "No stable handle." FixedBinding (keymap.rs:277-286) has no `name` field (true; EditableBinding does, line 294), but it DOES have `id: BindingId` (line 285). BindingId is a runtime atomic counter (`NEXT_BINDING_ID.fetch_add`, keymap.rs:268-271), registration-order-dependent — not a stable, config-usable string handle. So "nameless, no stable id" is defensible as "no stable string name," not "no identifier at all."\n\nBOTTOM LINE. Every numeric claim and the central thesis reproduce exactly. Axis 3's "fixed bindings become remappable" leaf is a 160-257-site fixed→editable identity-and-migration project (minimal = ~168 editor-core; maximal = ~257 incl. app-chrome views), NOT a contained Unbound+precedence refactor — confirming the question's thesis and refuting report L9/line 234.

_Evidence:_
- FixedBinding::new sites total exactly 397 (384 plain + 13 new_per_platform); reproduced — `grep -rhoE FixedBinding::new app/ crates/ --include=*.rs | wc -l = 397; split 384 + 13` _(code)_
- new() builds Trigger::Keystrokes (line 525); new_per_platform routes through Self::new (line 507) — all 397 are keystroke triggers — `crates/warpui_core/src/keymap.rs:497-508,511-533` _(code)_
- Code editor registers full core nav/edit/selection set (enter/numpadenter/shift-enter/backspace/shift-backspace/delete/up/down/left/right/home/end/tab/shift-tab/word-move/shift-home/end/cmdorctrl-a, with Vim forks) as FixedBinding under id!("CodeEditorView") & !id!("IMEOpen") — `app/src/code/editor/view/actions.rs:47-229` _(code)_
- Mirror contexts are persistent, not modal: EditorView (mod.rs:195), Terminal (init.rs:91), RichTextEditorView (notebooks/editor/view.rs:97,132) — `app/src/editor/view/mod.rs:195; app/src/terminal/view/init.rs:91; app/src/notebooks/editor/view.rs:97,132` _(code)_
- Per-file EDITOR_CORE counts exact: 49/38/38/35/5/1/1/1 = 168 — `grep -c FixedBinding::new over the 8 files` _(code)_
- Bucketing of all 397 reproduced exactly: EDITOR_CORE 168, OTHER_VIEW 89, MODAL_TRANSIENT 68, BLOCKLIST_INLINE 44, AUTH 4, TEST_EXAMPLE 24 (sum 397) — `bash bucketing loop over grep -rln FixedBinding::new` _(code)_
- Report L9 (line 134) and line 234 assert Fixed is reserved for internal/transient and user-facing shortcuts are EditableBinding — verbatim present and empirically false — `/home/mhb/warp/.understand-anything/keybindings-architecture-report.md:134,234` _(code)_
- REFINEMENT: EditableBinding::new takes (name, description, action), not a keystroke; trigger attaches via .with_key_binding. Proper grep of .with_key_binding for bare nav keys in the four editor files = none, so conclusion holds; but input.rs:1944 binds bare tab editable and actions.rs:504-508 gives Delete an editable ctrl-d alias — `crates/warpui_core/src/keymap.rs:646; app/src/code/editor/view/actions.rs:504-508; app/src/terminal/input.rs:1938-1944` _(code)_
- REFINEMENT: FixedBinding has no name field but does carry id: BindingId, a runtime atomic-counter id (not a stable string handle); EditableBinding has both name and id — `crates/warpui_core/src/keymap.rs:277-286,293-304,268-271` _(code)_

- _Unblocks:_ Sizing the Axis 3 "fixed bindings become auditable/remappable" leaf: the data shows it is NOT a contained Unbound+precedence refactor but a 160-257-site fixed->editable identity-and-migration project, because the core navigation/editing keys users most want to rebind are Fixed (nameless, no stable id) under persistent editor contexts. This gates whether 'remappable' can be promised in Phase 2 and feeds Q3 (stable identity for nameless fixed bindings) and Q13 (context-scoped override as prerequisite, given the enter->Enter / enter->VimEnter opposing-predicate forks).
- _Residual / needs decision:_ The cut between "plausibly rebindable" and "internal/transient" for the 89-site OTHER_VIEW gray zone and 44-site BLOCKLIST_INLINE set is a product decision, not a code fact — code only gives the distribution (minimal scope ~168 editor-core; maximal ~257 incl. app chrome). EDITOR_CORE 168 counts every FixedBinding in the 8 files, so as "strictly core nav/edit" it slightly overcounts (e.g., terminal copy/fork-behind-flags, and input.rs includes a tab that is actually editable); the floor is the explicitly enumerated code-editor set at actions.rs:50-229. Not measured here: per-key platform forks (13 new_per_platform sites) make any key-scoped disable platform-specific (relevant to Q15). Whether the runtime BindingId could serve as an override handle (vs. needing a new stable name) feeds Q3.
- _Verification:_ I attempted to refute and could not break the substance. Re-ran the count (397 = 384 + 13, exact) and the full bucketing loop (EDITOR_CORE 168, OTHER_VIEW 89, MODAL_TRANSIENT 68, BLOCKLIST_INLINE 44, AUTH 4, TEST_EXAMPLE 24, sum 397) — reproduced identically. Opened keymap.rs:497-533 (trigger kind), :277-304 (structs), :268-271 (BindingId), actions.rs:44-233 (every cited line is correct, predicates verbatim), and confirmed EditorView/Terminal/RichTextEditorView predicates by direct read. Verified report.md:134 and :234 quote-for-quote. Checked an uncited location (drive/index.rs) to test the gray-zone: confirmed 9 Fixed + DisableDriveIndexVimKeybindings at 578/583/5077. Two genuine refinements that do NOT overturn any conclusion: (1) Evidence #6's stated method (grep EditableBinding::new for key strings) is imprecise because that call carries name+description, not keystrokes — my own grep got a false-positive on the description "Delete"; the methodologically correct check (grep .with_key_binding for bare nav keys in the four files) returns none, so the conclusion stands, but I found bare `tab` IS editable in terminal/input.rs:1944 and the Delete action has an editable ctrl-d alias in actions.rs, so "exclusively Fixed" is scoped to the bare nav keys in the four files, not global. (2) FixedBinding does have a runtime id: BindingId (atomic counter), so "no stable handle" should read "no stable string name." Neither affects the numbers or the central refutation of L9/line 234. The "§6" label in the original has no matching report heading, but the cited line numbers 134/234 are correct.

### A3-Q3 — CONFIRMED · confidence: high

**Q.** How do fixed bindings acquire a stable, portable identity for audit/override given FixedBinding has no name field (keymap.rs:277-286), as_lens hardcodes name=Default (keymap.rs:633), and BindingId is an unstable per-run atomic counter (keymap.rs:264-272)?

**A.** Confirmed. Fixed bindings have NO stable, portable identity today: FixedBinding carries only an unstable per-run BindingId (no name; keymap.rs:277-286), as_lens surfaces name="" to every consumer (keymap.rs:633 + lens.name is &'a str at :243), and the entire override/audit/persistence path keys on the &'static str name that fixed bindings lack (update_custom_trigger only touches editable bindings, keymap.rs:422-433; UI lookup rejects empty names, keybindings.rs:939). The proposed "derive id from action TypeId + context predicate" is non-unique — at least 3 same-action+same-context FixedBinding pairs differing only by trigger collide in app/src/code/editor/view/actions.rs, and the problem is in fact broader than 3 (see below) because CodeEditorViewAction is a single enum, so all its variants share one TypeId. All 15 evidence items verify exactly.

VERDICT: CONFIRMED. Every cited file:line and every count was independently opened and re-run; all 15 evidence items hold exactly. One precision nuance (below) STRENGTHENS the answer rather than weakening it.

CODE FACTS (all re-verified):
1. FixedBinding has no name. Struct (keymap.rs:277-286) = trigger, action, command_description, context_predicate, enabled_predicate, group: Option<&'static str>, id: BindingId. The only string-ish field is the optional group, and grep shows 0 with_group calls across all 46 FixedBinding registrations in app/src/code/editor/view/actions.rs, so group is None and is not an identity. EditableBinding (keymap.rs:293-304) carries name: &'static str — THE stable key.
2. as_lens erases identity to "". FixedBinding::as_lens (keymap.rs:631-642) sets name: Default::default(); BindingLens.name is &'a str (keymap.rs:243), so Default = "".
3. BindingId is a per-run, non-portable counter. One global NEXT_BINDING_ID: AtomicUsize (keymap.rs:264-272) fetch_add'd by every constructor (FixedBinding::new :531, empty :548, standard :568, custom :591, EditableBinding::new :662). grep BindingId + serial/persist/json across app/ and crates/ = 0 hits — never serialized.
4. The override/audit/persistence machinery keys exclusively on name, which fixed bindings lack. update_custom_trigger(name) filters editable_custom_action_bindings and editable_bindings by b.name == name and NEVER touches fixed_bindings (keymap.rs:422-433). Settings UI keys on row.binding.name (keybindings.rs:592 set_custom_trigger; :674 set_custom_keybinding; persists/telemeters action: name at :606/:631/:683). The list lookup at keybindings.rs:939 is .find(|b| !name.is_empty() && b.name == name) — it explicitly rejects empty names, so empty-named fixed bindings are unaddressable. util/bindings.rs:491 set_custom_keybinding(binding_name: &str). Net: no handle exists to key an override/audit onto a fixed binding.
5. The proposed derived id (hash of action TypeId + serialized context predicate) is REFUTED:
 (a) Non-unique. Confirmed same-action + same-context FixedBinding pairs differing ONLY by trigger:
   - VimBackspace under text_entry & id!("Vim"): "backspace" (86-89) AND "shift-backspace" (96-99).
   - SelectBackwardsByWord under text_entry: "shift-alt-left"/"shift-ctrl-left" (189-196) AND "shift-meta-left" (197-201).
   - SelectForwardsByWord under text_entry: "shift-alt-right"/"shift-ctrl-right" (202-209) AND "shift-meta-right" (210-214).
   PRECISION NUANCE (strengthens the refutation): CodeEditorViewAction is a SINGLE enum (actions.rs:619), so every variant shares ONE TypeId via action.type_id() (ActionType wraps it, action.rs:43). Under a literal "action TypeId + context" id, the collision set is therefore FAR larger than 3 pairs — e.g. MoveUp/MoveDown/MoveLeft/MoveRight all sit under editable_state with the same TypeId and collapse to one id, and SelectUp/Down/Left/Right all sit under text_entry likewise. The enter fork (actions.rs:51-60) — Enter under editable_state & !id!("Vim") vs VimEnter under text_entry & id!("Vim") — remains DISTINGUISHABLE, but strictly by CONTEXT, NOT by action TypeId (Enter and VimEnter are variants of the same enum, hence identical TypeId and identical type_name()). The answer's phrasing "distinct action AND distinct context" is loose on the "distinct action" half; the operative conclusion (the fork is distinguishable; the three cited pairs are NOT) still holds and the three cited pairs are robust collisions under ANY id scheme (same variant, same context, only trigger differs).
 (b) Action identity isn't release-stable. The Action trait (action.rs:10-19) exposes only type_name() (= std::any::type_name, std-documented as not stable across compiler versions) and TypeId via Any (ActionType, action.rs:32-45) — both runtime-only.
 (c) ContextPredicate isn't serializable/hashable. The enum (context.rs:9-18) derives Debug/Clone/Eq/PartialEq but NOT Serialize/Deserialize and NOT Hash, so a "serialized context predicate" requires adding impls before the derived id is even expressible.

Editor scope note (unchanged): the audited collisions are in the CODE editor (app/src/code/editor/view/actions.rs). The terminal editor (app/src/editor/view/) registers an analogous Fixed set and would need the same audit; the three concrete collisions cited are from the code editor only — the answer states this correctly and does not overgeneralize.

_Evidence:_
- FixedBinding has no name field; only group: Option<&'static str> and id: BindingId — `crates/warpui_core/src/keymap.rs:277-286` _(code)_
- EditableBinding carries the stable name: &'static str key that overrides use — `crates/warpui_core/src/keymap.rs:293-304` _(code)_
- as_lens sets name=Default::default(); BindingLens.name is &'a str (keymap.rs:243) so every fixed binding surfaces name="" — `crates/warpui_core/src/keymap.rs:631-642 and :243` _(code)_
- BindingId is one global AtomicUsize counter bumped per binding by all constructors (531,548,568,591,662); grep BindingId+serial/persist/json = 0 hits — `crates/warpui_core/src/keymap.rs:264-272` _(both)_
- update_custom_trigger filters editable + editable_custom_action bindings by name and never touches fixed_bindings — `crates/warpui_core/src/keymap.rs:422-433` _(code)_
- Settings UI keys overrides/persistence/telemetry on binding.name (set_custom_trigger :592, set_custom_keybinding :674, action: name :606/631/683) — `app/src/settings_view/keybindings.rs:592,606,631,683` _(code)_
- UI binding-list lookup explicitly rejects empty names, so empty-named fixed bindings are unaddressable — `app/src/settings_view/keybindings.rs:939` _(code)_
- Override write path is name-keyed (binding_name: &str) — `app/src/util/bindings.rs:491` _(code)_
- Synthetic (action+context) id collides: VimBackspace same context (text_entry & Vim), triggers backspace vs shift-backspace — `app/src/code/editor/view/actions.rs:86-89 and 96-99` _(code)_
- Collides: SelectBackwardsByWord same text_entry context, shift-alt/ctrl-left vs shift-meta-left — `app/src/code/editor/view/actions.rs:189-201` _(code)_
- Collides: SelectForwardsByWord same text_entry context, shift-alt/ctrl-right vs shift-meta-right — `app/src/code/editor/view/actions.rs:202-214` _(code)_
- enter fork is distinguishable BY CONTEXT (Enter under editable_state&!Vim vs VimEnter under text_entry&Vim); note both are same TypeId so action does NOT distinguish them — `app/src/code/editor/view/actions.rs:51-60` _(code)_
- CodeEditorViewAction is a single enum, so all variants share one TypeId — making a TypeId-based id collide far more broadly than the 3 cited pairs — `app/src/code/editor/view/actions.rs:619` _(code)_
- Action trait exposes only type_name() (std::any::type_name) and TypeId via Any (ActionType wraps action.type_id()); neither guaranteed stable across compiler versions — `crates/warpui_core/src/core/action.rs:10-19,32-45` _(code)_
- ContextPredicate derives Debug/Clone/Eq/PartialEq but NOT Serialize/Deserialize/Hash, so it can't be serialized/hashed for a derived id today — `crates/warpui_core/src/keymap/context.rs:9-18` _(code)_
- 397 FixedBinding::new sites repo-wide; 38 ::new in the code-editor actions file (46 total FixedBinding registrations there, 0 with_group) — `grep -rn FixedBinding::new app/ crates/ = 397; grep -c FixedBinding::new actions.rs = 38; grep -c FixedBinding:: = 46; grep -c with_group = 0` _(code)_

- _Unblocks:_ Lets the design doc rule out "just expose fixed bindings in the UI / derive an id from action+context" as a free option, and forces an explicit identity decision: every remappable fixed binding needs an authored stable string key (add name or promote to EditableBinding), because no existing field (name is absent, lens name is \"\", BindingId is per-run and unpersisted, group is None) provides a portable handle, and a content-hash id collides on real multi-trigger-same-behavior bindings.
- _Residual / needs decision:_ The CHOICE among {add a &'static str name at every Fixed site, promote Fixed to EditableBinding, derive a synthetic id} is a design decision, not a code fact; code only constrains it (the derive-an-id route is non-viable as-is: non-unique, TypeId/type_name not release-stable, ContextPredicate not Serialize/Hash). I did not enumerate how many of the 397 FixedBinding::new sites sit under truly user-facing contexts (A3-Q1/A3-Q6's job), nor did I audit the terminal editor (app/src/editor/view/) for its own same-action+same-context collisions — its Fixed set is structurally analogous but I did not enumerate concrete collisions there. The exact size of the TypeId-collapsed collision set in actions.rs was reasoned (MoveUp/Down/Left/Right; SelectUp/Down/Left/Right) but not exhaustively enumerated.
- _Verification:_ I opened every cited file and re-ran every grep rather than trusting the answer. Struct definitions (keymap.rs:277-286, 293-304, 242-254), as_lens (631-642), BindingId (264-272) and all five constructors (531/548/568/591/662) match verbatim. update_custom_trigger (422-433) confirmed to filter only editable collections. Settings UI lines 592/606/631/674/683 and the empty-name-rejecting lookup at 939 all verified; util/bindings.rs:491 verified. All three collision pairs verified character-for-character (86-89/96-99, 189-201, 202-214), including that the contexts are structurally identical (both text_entry.clone() & id!("Vim") for the backspace pair; both text_entry.clone() for the word-select pairs). ContextPredicate derive line (context.rs:9) confirmed: Debug/Clone/Eq/PartialEq only — no Serialize/Deserialize/Hash. Action trait (action.rs:10-19,32-45) confirmed: type_name + TypeId only. Counts re-run: 397 / 38 / 46 / 0 with_group / 0 BindingId-serialize. I also checked a location the answer did NOT cite: the CodeEditorViewAction enum definition (actions.rs:619). This surfaced the one imprecision in the answer: because CodeEditorViewAction is a single enum, all its variants share ONE TypeId, so the proposal's "action TypeId" does NOT distinguish enum variants. Consequently (i) the answer's claim that the enter fork has "distinct action" is loose — the fork is distinguishable only by CONTEXT; and (ii) the collision set under a pure-TypeId id is much larger than the 3 cited pairs. Both points STRENGTHEN, not weaken, the answer's thesis, and the three cited pairs remain genuine collisions under any scheme. No evidence item was wrong; no shifted line refs; no app/-vs-crates scope confusion; no overgeneralization (the editor-scope caveat is correctly stated). Verdict therefore confirmed.

### A3-Q6 — CONFIRMED · confidence: high

**Q.** Is binding registration order deterministic and stable across releases/platforms, given precedence falls back to 'recency' = registration order produced by ~265 register_* calls fired during view init across 109 files, and given editable_bindings_by_name resolves ties by first-enabled (keymap.rs:379-385)?

**A.** Registration order is fully deterministic for a fixed build+feature-flag+platform config: it is a single synchronous, source-ordered init sequence (app/src/lib.rs:1648-1693 inside the once-called, non-async initialize_app, lib.rs:1086, sole caller lib.rs:1065), NOT a function of lazy view creation or responder-chain timing. But it is NOT invariant across releases (source reordering), platforms (cfg gates, lib.rs:1652), or runtime feature-flag states (conditional init blocks, lib.rs:1691) — any of these silently shifts which binding wins by 'recency'. There is a real, code-confirmed asymmetry: keystroke/standard/custom matching walks bindings() in reverse (LIFO/last-registered wins, keymap.rs:441-447,454-464) while get_binding_by_name (the settings-UI path) uses forward order returning the earliest-registered enabled binding (keymap.rs:378-385, doc comment is explicit). Every load-bearing claim verified against code; only the approximate call-site COUNTS differ from my reproduction (262/106 non-test, 387 FixedBinding::new vs the answer's 248/102/373), a method-dependent gap the answer itself flagged as non-substantive.

VERDICT: CONFIRMED. I independently opened every cited file plus uncited locations (notebooks/editor/view.rs fn layout, the editor_view:delete trigger bodies) and re-ran the grep/classification. Every substantive claim holds; the only deltas are non-load-bearing count figures the answer already hedged.

CODE FACTS RE-VERIFIED:

1. Single synchronous source-ordered init chain. app/src/lib.rs:1648 is the comment "Register initial keybindings prior to creating menus", followed by a flat list of `module::init(ctx)` calls through :1693. This block is inside `pub(crate) fn initialize_app` (lib.rs:1086, non-async), whose ONLY caller is lib.rs:1065. Confirmed verbatim. Order = literal source order of init calls, then register_* order within each init, then array-literal order. Not tied to window/view creation.

2. No lazy per-view registration. I re-ran the enclosing-fn classification over all non-test register_fixed_bindings/register_editable_bindings call sites: 212 attributed to `fn init` (matches the answer exactly), and the only 5 attributed to `fn new` are ALL in crates/warpui_core/src/core/mod_tests.rs (test code), so production code has ZERO register_* in a constructor/render. NOTE: the 212 figure is itself a naive-classifier UNDERCOUNT — nested helper fns defined inside init (e.g. `fn debug_notebooks_enabled() -> bool` at notebooks/editor/view.rs:100) cause the heuristic to mis-bucket ~12 calls that actually live in `pub fn init` (notebooks/editor/view.rs:90). The real init-resident count is higher, which STRENGTHENS the "all registration funnels through init" thesis. The named helpers (init_actions_from_parent_view, register_conversation_list_view_bindings, init_overlapping_keybindings, add_*_as_editable_binding, register_input_mode_bindings, add_toggle_setting_action_pairs_as_bindings) are init-time helpers invoked transitively from the lib.rs chain (verified: workspace/mod.rs:101 register_conversation_list_view_bindings and :103 init_actions_from_parent_view sit inside fn init at workspace/mod.rs:78, called from lib.rs:1654).

3. Consumption order / precedence. keymap.rs:454-464 bindings() yields editable_bindings() first then fixed (.rev()). editable_bindings() (keymap.rs:441-447) iterates `.iter().rev()`, and the doc comment at :439-440 states "the most recently registered editable binding will have the highest precedence" — LIFO/recency confirmed. All three match paths walk the same iterator: push_keystroke (matcher.rs:324, early-return :329-331), match_standard (matcher.rs:351), match_custom (matcher.rs:363). editable_bindings_by_name is a HashMap (keymap.rs:30) storing Vec<usize> "in the order they were registered" (doc :28-30), pushed at keymap.rs:417 — used only for name lookup, not matching, so no HashMap-iteration nondeterminism enters tiebreaking.

4. Forward/reverse asymmetry (the sharp finding) — CONFIRMED. get_binding_by_name (keymap.rs:379-385) does `indices.iter().find_map(... is_enabled)` in FORWARD registration order; the doc comment at :378 literally reads "Returns the earliest-registered currently-enabled binding with the given name." This is the OPPOSITE of the matcher's reverse/recency order. The settings UI / palette read through this path: app/src/util/bindings.rs:471-473 keybinding_name_to_keystroke -> ctx.get_binding_by_name; app/src/search/command_palette/view.rs:267 uses ctx.get_binding_by_name. So displayed/edited precedence (first-registered) can diverge from dispatched precedence (last-registered) for editable same-name bindings with DIFFERING default triggers. A user override updates ALL same-name bindings (update_custom_trigger, keymap.rs:422-433 iterates both editable_custom_action_bindings and editable_bindings filtering by name), so the asymmetry only bites for differing DEFAULT triggers.

5. Duplicate names — CONFIRMED at exact lines. editor_view:delete is registered by the code editor (app/src/code/editor/view/actions.rs:504, CodeEditorViewAction::Delete), terminal editor (app/src/editor/view/mod.rs:769, EditorAction::Delete), and notebooks (app/src/notebooks/editor/view.rs:589, EditorViewAction::Delete); editor_view:up at actions.rs:329 / notebooks:427 / mod.rs:618; editor_view:end at actions.rs:380 / notebooks:476 / mod.rs:659. Different actions and context predicates. NUANCE I checked that the answer did not: all three editor_view:delete sites use the SAME default key "ctrl-d", so this specific triplet would NOT produce a divergent UI-vs-dispatch DISPLAY (the answer correctly scoped this example to "real duplicate-name case" and flagged divergence as a residual unknown — no overclaim).

DETERMINISM CAVEATS — CONFIRMED:
- Across releases: any reorder/insert of an init line or a binding shifts Vec indices and thus reverse-order precedence. Nothing pins absolute order.
- Across platforms: init chain is cfg-gated, e.g. lib.rs:1652 `#[cfg(not(target_family = "wasm"))] code::editor::find::view::init`. Per-binding variants e.g. root_view.rs:457 `with_linux_or_windows_key_binding("f11")` and :467 `with_enabled(|| ...)` change which binding is enabled at match time.
- Across feature-flag states (same binary): lib.rs:1691 `if FeatureFlag::CodeReviewSaveChanges.is_enabled() { code_review::init(ctx); }` conditionally registers a whole block, shifting later indices.

CONCLUSION (unchanged): 'recency' is deterministic/reproducible for a given (build, platform, flag-state) tuple, but is an emergent artifact of source ordering + cfg/flag gating, so it silently changes across releases/platforms/flag flips, and is compounded by the forward(UI)/reverse(matcher) asymmetry. The design should replace implicit registration-order recency with an explicit stored ordering key and unify get_binding_by_name with the matcher's resolution order.

_Evidence:_
- All keybinding registration runs in one synchronous source-ordered init chain at startup; comment 'Register initial keybindings prior to creating menus' at the top of a flat module::init(ctx) list — `app/src/lib.rs:1648-1693` _(code)_
- The init chain is inside non-async pub(crate) fn initialize_app, which has a single call site (runs once, not per-window) — `app/src/lib.rs:1086 (def), app/src/lib.rs:1065 (only caller)` _(code)_
- 212 register_* call sites attributed to `fn init` and ZERO in production fn new/render — reproduced exactly; the 5 'new'-attributed sites are all in mod_tests.rs (test). The 212 is a naive-classifier undercount because helper fns nested inside init (e.g. fn debug_notebooks_enabled at notebooks/editor/view.rs:100) mis-bucket ~12 init-resident calls; true init count is higher, strengthening the thesis — `app/ + crates/ register_* enclosing-fn tally; crates/warpui_core/src/core/mod_tests.rs (5 'new'); app/src/notebooks/editor/view.rs:90 fn init vs :100 nested helper` _(code)_
- Helper register fns invoked transitively from the top-level init chain (register_conversation_list_view_bindings at :101 + settings_view::init_actions_from_parent_view at :103 sit inside fn init at workspace/mod.rs:78, called from lib.rs:1654) — `app/src/workspace/mod.rs:78,101,103; app/src/lib.rs:1654` _(code)_
- COUNT (re-run, non-test, app/+crates/): 262 register_* call sites across 106 files; 387 FixedBinding::new; 451 EditableBinding::new. Differs from the answer's reported 248/102/373 but consistent in order of magnitude and explained by mod_tests.rs/test-file filtering method; same order of magnitude as question's ~265/109 — `grep -rn over app/ crates/ excluding /tests/,/examples/,_test.rs` _(code)_
- bindings() yields editable (reverse/LIFO = recency, doc 'most recently registered ... highest precedence') then fixed (reverse); all three match paths walk it — `crates/warpui_core/src/keymap.rs:441-447 (.rev()), :454-464; matcher.rs:324 (early-return :329-331), :351, :363` _(both)_
- get_binding_by_name returns the EARLIEST-registered enabled binding (forward find_map) — doc comment literally 'Returns the earliest-registered currently-enabled binding' — opposite tie order from the matcher — `crates/warpui_core/src/keymap.rs:378-385` _(code)_
- editable_bindings_by_name stores Vec<usize> in registration order (push at :417; doc 'stored in the order they were registered'), so name lookup is deterministic, not HashMap-iteration-order dependent — `crates/warpui_core/src/keymap.rs:28-30, :405-419 (push :417)` _(code)_
- Settings UI / palette read the displayed/edited trigger through get_binding_by_name — `app/src/util/bindings.rs:471-473; app/src/search/command_palette/view.rs:267` _(code)_
- A user override updates ALL bindings sharing a name (override applies uniformly; default-trigger asymmetry only): iterates editable_custom_action_bindings and editable_bindings filtering by name — `crates/warpui_core/src/keymap.rs:422-433` _(code)_
- Same editable name editor_view:delete registered by 3 distinct views with different actions+contexts (real duplicate-name case). NOTE: all 3 share default trigger 'ctrl-d', so this specific triplet would not by itself diverge in display — answer scoped it correctly and flagged divergence as residual — `app/src/code/editor/view/actions.rs:504; app/src/editor/view/mod.rs:769; app/src/notebooks/editor/view.rs:589` _(code)_
- Platform cfg gate changes which inits run, altering registration order across platforms — `app/src/lib.rs:1652 (#[cfg(not(target_family="wasm"))] code::editor::find::view::init)` _(code)_
- Runtime feature flag conditionally registers a whole binding block, shifting indices within the same binary — `app/src/lib.rs:1691 (if FeatureFlag::CodeReviewSaveChanges.is_enabled() { code_review::init(ctx); })` _(code)_
- Per-binding platform/enabled variants further affect which binding is enabled at match time — `app/src/root_view.rs:457 (with_linux_or_windows_key_binding('f11')), :467 (with_enabled)` _(code)_

- _Unblocks:_ Enables deciding whether the new layer model can keep 'recency' (= registration index) as a precedence tiebreaker. Decision input: recency is reproducible per build but unstable across releases/platforms/flags and asymmetric between the settings UI (first-registered) and the matcher (last-registered). To make "the UI shows the truth", the design should replace implicit registration-order recency with an explicit, stored ordering key per binding/layer and unify the UI lookup (get_binding_by_name) with the matcher's resolution order so displayed precedence equals dispatched precedence.
- _Residual / needs decision:_ Whether 'recency' SHOULD be a user-visible precedence axis is a product decision, not a code fact. Not exhaustively verified: whether any same-name editable duplicate pair has mutually-satisfiable context predicates AND differing default triggers such that the forward(UI)-vs-reverse(matcher) asymmetry actually produces a real divergent display-vs-dispatch — I confirmed the editor_view:delete triplet shares 'ctrl-d' so it does NOT diverge, but did not scan all ~450 EditableBinding names for a pair that both differs in default trigger and has overlapping contexts. The exact call-site counts remain method-dependent (262/106/387 in my non-test reproduction vs the answer's 248/102/373 vs the question's ~265/109); none of these is load-bearing for the determinism/asymmetry conclusion.
- _Verification:_ I opened every cited file plus uncited spots and tried to break the answer. Results: (1) All lib.rs anchors are verbatim correct — comment at :1648, cfg gate at :1652, feature flag at :1691, initialize_app def at :1086 with sole caller at :1065. (2) keymap.rs anchors are correct and even understated: the doc comments themselves spell out the asymmetry — :378 'earliest-registered currently-enabled binding' for get_binding_by_name vs :439-440 'most recently registered ... highest precedence' for editable_bindings(). So the forward(UI)/reverse(matcher) asymmetry is not an inference, it is documented in source. (3) matcher.rs:324/351/363 all walk keymap.bindings(); confirmed. (4) update_custom_trigger (:422-433) iterates both editable_custom_action_bindings AND editable_bindings by name — confirmed all-same-name. (5) Duplicate-name triplets exist at the EXACT cited lines. (6) workspace/mod.rs:101-103 transitive init confirmed. (7) root_view.rs:457/467 variants confirmed.

Things I corrected / flagged (all non-material): (a) The reported call-site COUNTS do not exactly reproduce — my non-test grep gives 262 register_* sites / 106 files and 387 FixedBinding::new, vs the answer's 248/102/373. The gap is test-file filtering (e.g. mod_tests.rs) and counting method; the answer's residual_unknowns already disclosed this. My numbers are actually closer to the question's ~265/109. (b) The answer's '212 in fn init' is itself a naive-classifier artifact: nested helper fns inside init (the smoking gun being `fn debug_notebooks_enabled() -> bool` at notebooks/editor/view.rs:100, which my first pass wrongly credited with 12 register calls that actually belong to `pub fn init` at line 90). The true init-resident count is higher — this STRENGTHENS the 'all registration funnels through init' conclusion rather than weakening it; the 5 'new'-attributed sites are all in mod_tests.rs test code, so production has zero constructor/render registration. (c) editor_view:delete uses the same default key 'ctrl-d' at all three sites, so that specific example would not produce a divergent display; but the answer used it only as a 'real duplicate-name case' and explicitly flagged real divergence as a residual unknown — no overgeneralization. No app/-vs-crates scope confusion, no shifted line refs, no graph-vs-code contradiction found. The conclusion is sound and, on the structural point, more strongly supported than stated.

### A3-Q7 — CONFIRMED · confidence: high

**Q.** If user overrides move from in-place custom_trigger mutation (keymap.rs:300,422-433) to a separate layer, what drives the live re-render that Tracked<EditableBinding> provides today — keep per-binding Tracked, or move to a layer-level revision counter?

**A.** Today live re-render is driven by per-binding autotracking: each EditableBinding is a Tracked with its own TrackedId; render-time reads go Deref→track_read (registering the view as a dependent, mod.rs:272-287) and update_custom_trigger mutates custom_trigger via iter_mut → DerefMut→track_update, which pushes every dependent view into invalidations (≈ ctx.notify, mod.rs:290-296). A layer refactor preserves live-apply ONLY if the new override store is itself Tracked (or carries a Tracked revision counter) and render reads/writes flow through Deref/DerefMut; storing overrides as plain side data silently breaks the distributed keystroke-hint surfaces. Both granularities are mechanically supported (mod.rs:41-46): per-binding gives fine-grained single-hint invalidation; a layer-level counter is correct but over-invalidates. That choice is a design decision, not a code fact. Independently verified: all 10 evidence claims hold at the cited lines.

VERDICT: Confirmed. I opened every cited file (plus uncited locations: app.rs:1704 chain and tip_view.rs:550-570) and each evidence claim says what the answer asserts.

MECHANISM (verified):
- Tracked<T> assigns a unique TrackedId at construction via TrackedId::next() (atomic, tracked.rs:74-81). editable_bindings and editable_custom_action_bindings are both Vec<Tracked<EditableBinding>> (keymap.rs:27,37).
- Reads: Deref::deref calls track_read(self.id) (tracked.rs:41-48). track_read, only while cache.rendering_view is set, records the view as a dependent of that TrackedId (mod.rs:272-287). Doc confirms reads-during-render are cached as the view's deps (mod.rs:50-63).
- Writes: DerefMut::deref_mut calls track_update(self.id) (tracked.rs:50-55). track_update extends cache.invalidations with every view in value_dependencies[id] (mod.rs:290-296) — effectively ctx.notify per dependent view.
- Trigger of the write: Keymap::update_custom_trigger (keymap.rs:422-433) iterates editable_custom_action_bindings.iter_mut().filter(...) then editable_bindings.iter_mut().filter(...) and assigns binding.custom_trigger = trigger.clone(); each field assignment on &mut Tracked goes through DerefMut→track_update for that binding's TrackedId. Reached from Matcher::set_custom_trigger/remove_custom_trigger (matcher.rs:137-152) ← app set_custom_keybinding/reset (util/bindings.rs:491+).

GRANULARITY = PER-BINDING (verified): one Tracked<EditableBinding> == one TrackedId. Single-binding render path get_binding_by_name (keymap.rs:379-385) → matcher.rs:274-275 → AppContext::get_binding_by_name (crates/warpui_core/src/core/app.rs:1704) → keybinding_name_to_keystroke/_display_string (util/bindings.rs:471-487) derefs only the name-matching binding(s). Iterator readers editable_bindings()/bindings() (keymap.rs:441-464) deref EVERY binding via as_lens (741-759), so walkers depend on all. The keymap keeps TWO Tracked copies per custom-action binding with DIFFERENT ids (primary in editable_bindings + a separate Tracked::new((*binding).clone()) at keymap.rs:412); update_custom_trigger feeds both in lockstep — a layer model must keep both fed (A3-Q8 overlap).

KEY NUANCE (verified): the settings keybindings list does NOT rely on Tracked. on_page_selected (keybindings.rs:748, snapshot at 754-792) caches self.bindings/self.rows, and every edit handler (remove_keystroke 590-615, reset 617-641, cancel 643-667, confirm 669-699) refreshes via explicit update_binding_list + ctx.notify and mutates row.binding.trigger. render (keybindings.rs:734,1098) draws cached rows. So Tracked autotracking drives the DISTRIBUTED keystroke-hint surfaces (get_binding_by_name path), NOT the settings list, which survives via explicit notify.

DISPATCH independent of autotracking (verified): push_keystroke (matcher.rs:307-346, reads keymap.bindings() at :324, early-return :330-331), match_standard (:351), match_custom (:364) read at dispatch time, not under render tracking — matching always sees the fresh trigger; only on-screen display needs Tracked.

DESIGN-DECISION (code-verifiable constraints): mod.rs:41-46 explicitly supports both per-value (granular) and whole-model (coarse) tracking, so either "keep per-binding Tracked" or "layer-level Tracked revision counter" is viable. Invariant that MUST hold for the layer refactor: (1) the override/layer store is a Tracked field (or holds a Tracked revision counter), and (2) render-time hint readers read through Deref (track_read) while writes go through DerefMut (track_update). If overrides move to a plain non-Tracked side store (or are mutated without DerefMut), track_update never fires and distributed hints go stale — and because the settings list refreshes via explicit ctx.notify, that regression would be INVISIBLE in the settings page during testing. Trade-off: a layer-level counter is simplest but over-invalidates (any override bumps it, invalidating all single-hint views); per-binding keeps fine-grained single-hint invalidation.

_Evidence:_
- Deref::deref calls track_read; DerefMut::deref_mut calls track_update; each Tracked gets a unique TrackedId via TrackedId::next() (atomic counter) — `crates/warpui_core/src/core/autotracking/tracked.rs:41-55,74-81` _(code)_
- track_read records the rendering view as a dependent of the TrackedId (only while cache.rendering_view is set); track_update extends cache.invalidations with every view in value_dependencies[id] (≈ ctx.notify) — `crates/warpui_core/src/core/autotracking/mod.rs:272-296` _(code)_
- Autotracking doc explicitly supports per-value (granular) vs whole-model (coarse) tracking — directly informs per-binding vs layer-counter choice — `crates/warpui_core/src/core/autotracking/mod.rs:41-46` _(both)_
- editable_bindings and editable_custom_action_bindings are both Vec<Tracked<EditableBinding>>; register keeps a second Tracked::new((*binding).clone()) with a different TrackedId for custom-action bindings — `crates/warpui_core/src/keymap.rs:27,37,405-418` _(both)_
- update_custom_trigger iterates both lists via iter_mut().filter(name) and assigns binding.custom_trigger; field assignment on &mut Tracked routes through DerefMut→track_update — `crates/warpui_core/src/keymap.rs:422-433` _(code)_
- Matcher::set_custom_trigger and remove_custom_trigger both call keymap.update_custom_trigger (the notify path on override) — `crates/warpui_core/src/keymap/matcher.rs:137-152` _(code)_
- as_lens reads custom_trigger/trigger (741-759); iterator readers editable_bindings()/bindings() deref every Tracked binding via as_lens (441-464); get_binding_by_name derefs only the name-matching binding(s) until first enabled (379-385) — `crates/warpui_core/src/keymap.rs:379-385,441-464,741-759` _(code)_
- Keystroke-hint surfaces read a single binding live at render via get_binding_by_name → keybinding_name_to_keystroke/_display_string; set_custom_keybinding (491) writes via ctx.set_custom_trigger — `app/src/util/bindings.rs:471-487,491` _(code)_
- Settings keybindings list does NOT depend on Tracked: on_page_selected (748) caches self.bindings/self.rows (754-792); each edit handler (remove 590-615, reset 617-641, cancel 643-667, confirm 669-699) refreshes via explicit update_binding_list + ctx.notify; render at 734/1098 draws cached rows — `app/src/settings_view/keybindings.rs:748-792,590-699,734,1098` _(code)_
- Dispatch matching reads keymap.bindings() at dispatch time (not render-tracked): push_keystroke :324 (early-return :330-331), match_standard :351, match_custom :364 — `crates/warpui_core/src/keymap/matcher.rs:307-346,351,364` _(code)_
- AppContext::get_binding_by_name (cited as app.rs:1704) resolves to crates/warpui_core/src/core/app.rs:1704 and delegates matcher→keymap.get_binding_by_name; the bindings.rs hint path reads through it — `crates/warpui_core/src/core/app.rs:1704-1706` _(code)_

- _Unblocks:_ Lets the design doc state a concrete invariant for the layering refactor: overrides may move to a separate layer, but that layer must live under autotracking — store it as a Tracked field (per-binding Tracked to retain fine-grained single-hint invalidation, or a layer-level Tracked revision counter for simpler coarse invalidation) and ensure render-time hint readers (get_binding_by_name path) and the override write path keep flowing through Deref/DerefMut. It also flags the test-masking trap: the settings list refreshes via explicit ctx.notify, so a broken Tracked path would still look fine in the settings page while distributed keystroke hints silently go stale.
- _Residual / needs decision:_ The keep-per-binding-Tracked vs layer-level-revision-counter choice is a design decision, not a code fact; code only constrains it (either is correct if the layer store is Tracked and reads/writes flow through Deref/DerefMut; a layer counter over-invalidates while per-binding stays fine-grained). Open residue: (a) acceptable invalidation granularity vs simplicity (over-invalidating all hint views per override is likely fine perf-wise but unmeasured); (b) whether the new layer model collapses the two parallel Tracked copies (editable_bindings + editable_custom_action_bindings, keymap.rs:412) into one store or keeps per-layer shadows — overlaps A3-Q8; (c) whether the settings list should migrate onto autotracking (currently explicit ctx.notify), which changes what a Tracked regression makes visible. I did not exhaustively audit every app/ render path that draws a shortcut, but the get_binding_by_name/editable_bindings/keybinding_name_to_keystroke reader set is representative; I confirmed one distributed reader chain end-to-end (bindings.rs → core/app.rs:1704 → matcher → keymap:379).
- _Verification:_ I independently opened tracked.rs (41-55,74-81), mod.rs (41-63,259-296), keymap.rs (20-49,277-485,735-760), matcher.rs (125-152,260-286,305-374), app/util/bindings.rs (469-513), app/settings_view/keybindings.rs (585-799), and uncited locations crates/warpui_core/src/core/app.rs:1704-1706 and app/tips/tip_view.rs:550-570. Every one of the 10 evidence claims says exactly what the answer asserts at the cited lines — no shifted line refs, no app/-vs-crates scope confusion in the evidence array. I re-ran the get_binding_by_name grep (3 defs: keymap.rs:379, core/app.rs:1704, matcher.rs:274) and confirmed the delegation chain. Three micro-notes, none material: (1) The path shorthand 'app.rs:1704' actually lives at crates/warpui_core/src/core/app.rs (there are 8+ app.rs files); the LINE is correct. I added the full path. (2) get_binding_by_name 'deref only the matched one' is approximate: it derefs each binding registered under that name until the first ENABLED one (find_map over editable_bindings_by_name indices) — for the typical single-name case this is the matched binding, so the granularity claim holds. (3) The detailed_answer's parenthetical example tips/tip_view.rs:557-566 is actually a TipsAction::Click/KeyDown EVENT handler reading ctx.editable_bindings(), not a render-time keystroke-hint surface; I dropped that as a hint-surface citation but the genuine render-time hint path (get_binding_by_name → keybinding_name_to_keystroke, util/bindings.rs:471-487) is verified, so the thesis is unaffected. The core mechanism (per-binding Tracked drives distributed hints; settings list survives via explicit ctx.notify; dispatch independent of autotracking) and the design-decision framing (both granularities viable per mod.rs:41-46, invariant = override store must be Tracked with reads/writes through Deref/DerefMut) are fully supported.

### A3-Q8 — CONFIRMED · confidence: high

**Q.** Does the layer refactor unify or shadow the parallel custom-action collections fixed_custom_action_bindings and editable_custom_action_bindings (keymap.rs:396,411), maintained alongside the main vecs and updated in lockstep by update_custom_trigger (keymap.rs:422-433) and walked separately via custom_action_bindings() (keymap.rs:474)?

**A.** Confirmed. The shadow vecs (fixed_custom_action_bindings keymap.rs:36, editable_custom_action_bindings :37) are independent filtered clones of Custom-trigger bindings, kept only as the macOS menuNeedsUpdate optimization (documented :32-35). They are kept coherent by exactly three lockstep write sites (register_fixed_bindings :396, register_editable_bindings :411-412, update_custom_trigger :424) and read by exactly two consumers — neither on a keystroke hot path. keymap.rs:428/:431 are the only two custom_trigger writers in the codebase. A layer model can unify them into a single layered store plus a derived Custom index (mirroring editable_bindings_by_name :30), collapsing update_custom_trigger's dual write to one mutation; unify-vs-shadow is a design decision, not a code fact.

Every load-bearing claim was independently opened and verified; no material error found. The answer is precise on line refs and scope.

COLLECTIONS (keymap.rs:25-38): primary fixed_bindings (:26) and editable_bindings (:27); name index editable_bindings_by_name (:30); shadows fixed_custom_action_bindings (:36) and editable_custom_action_bindings (:37). The comment at :32-35 states the shadows exist to optimize Custom-action lookups for the macOS [WarpDelegate menuNeedsUpdate] selector. VERIFIED verbatim.

THREE LOCKSTEP WRITE SITES (verified, and grep confirms these are the ONLY writers of either shadow vec):
1. register_fixed_bindings (:391-399): extends fixed_bindings then pushes Custom-trigger entries' clone into fixed_custom_action_bindings (:396).
2. register_editable_bindings (:405-419): extends editable_bindings with Tracked wrappers, pushes an INDEPENDENT Tracked clone Tracked::new((*binding).clone()) of each Custom-trigger entry into editable_custom_action_bindings (:411-412), and updates editable_bindings_by_name (:414-417).
3. update_custom_trigger (:422-433): dual write — mutates custom_trigger on editable_custom_action_bindings filtered by name (:424-428) AND on editable_bindings filtered by name (:430-432).
The grep `\.custom_trigger\s*=` returns ONLY keymap.rs:428 and :431 across crates/ and app/ — confirming "the only two custom_trigger writers."

WHY LOCKSTEP IS REQUIRED: EditableBinding has #[derive(Clone)] at keymap.rs:292 (struct at :293), and the shadow holds a fresh Tracked over a cloned EditableBinding (:411-412), so custom_trigger is independently owned per copy. Primary and shadow drift unless update_custom_trigger writes both. VERIFIED.

READS — two distinct paths:
- Primary bindings() (:454-464): editable_bindings().rev/filter chained with fixed_bindings.iter().rev().filter(); does NOT touch shadows. Consumed by push_keystroke (matcher.rs:324), match_standard (:351), match_custom (:364), default_binding_for_custom_action (:233-234), bindings_for_context (:284), and the pass-through get_bindings (:304). All VERIFIED at the exact cited lines.
- Shadow custom_action_bindings() (:474-484): editable_custom_action_bindings().rev/filter chained with fixed_custom_action_bindings.iter().rev().filter().

SHADOW CONSUMERS — exactly two (verified by grep of custom_action_bindings call sites): keymap.custom_action_bindings() (:474) is called only from matcher.rs:224 (Matcher::custom_action_bindings wrapper, :223-225, reached only via app.rs:1911-1912 AppContext::custom_action_bindings → mac/app.rs:386 warp_app_has_custom_action_for_keystroke) and matcher.rs:252 (binding_for_custom_action_in_context, :246-263). app/ has ZERO references to custom_action_bindings — confirming app/ never touches the shadows directly; the macOS consumer lives in crates/warpui (read directly, outside graph scope).

SPEC-DOC INACCURACY CORRECTION — VERIFIED CORRECT: the spec's A3-Q8 "Why it matters" (line 493) claims the shadow is "used by default_binding_for_custom_action, matcher.rs:229-243." That is wrong: default_binding_for_custom_action walks self.keymap.bindings() at matcher.rs:233-234 (the PRIMARY path). The shadow's real consumers are binding_for_custom_action_in_context (:252) and the macOS menu path (:386). Consequence (correct): the shadow sits off every keystroke/standard/custom dispatch hot path, so unifying it has no matcher-correctness blast radius beyond the macOS menu lookup and binding_for_custom_action_in_context.

ENTRY POINTS: set_custom_trigger (matcher.rs:137-141 → update_custom_trigger Some), remove_custom_trigger (matcher.rs:146-152 → update_custom_trigger None), AppContext::set_custom_trigger exposure at app.rs:1637. VERIFIED — every external mutation routes through the lockstep.

FEASIBILITY OF COLLAPSE: editable_bindings_by_name (:30) is an existing derived Vec<usize> index carrying no duplicated binding state — a valid precedent for replacing the two shadow clone-vecs with a derived Custom index, which would make custom_trigger single-source-of-truth and reduce update_custom_trigger to one mutation. This is a sound design inference; the answer correctly caveats unify-vs-shadow as a design decision in residual_unknowns, not a code fact.

NITPICK (immaterial): the answer says EditableBinding "derives Clone (keymap.rs:292)" — the #[derive(Clone)] attribute is at :292 exactly and the struct keyword at :293, so the cite is correct, not off by one.

_Evidence:_
- Four collections: primary fixed_bindings (:26) + editable_bindings (:27), name index editable_bindings_by_name (:30), and two shadow vecs (:36,:37) whose stated purpose (:32-35) is the macOS menuNeedsUpdate optimization — `crates/warpui_core/src/keymap.rs:25-38` _(code)_
- register_fixed_bindings clones Custom-trigger entries into fixed_custom_action_bindings at :396 — `crates/warpui_core/src/keymap.rs:391-399` _(code)_
- register_editable_bindings pushes an INDEPENDENT Tracked clone Tracked::new((*binding).clone()) into editable_custom_action_bindings (:411-412) and updates editable_bindings_by_name (:414-417) — `crates/warpui_core/src/keymap.rs:405-419` _(code)_
- update_custom_trigger dual-writes custom_trigger to editable_custom_action_bindings (:428) and editable_bindings (:431); grep confirms these are the ONLY two custom_trigger writers in crates/ and app/ — `crates/warpui_core/src/keymap.rs:422-433` _(both)_
- Primary bindings() walks editable then fixed primary vecs in rev order and never touches the shadows — `crates/warpui_core/src/keymap.rs:454-464` _(code)_
- Shadow custom_action_bindings() walks the two shadow vecs in rev order; grep shows it is the only API exposing the shadows — `crates/warpui_core/src/keymap.rs:474-484` _(both)_
- push_keystroke (:324), match_standard (:351), match_custom (:364) all consume keymap.bindings() (primary), not the shadow — `crates/warpui_core/src/keymap/matcher.rs:324,351,364` _(code)_
- default_binding_for_custom_action walks bindings() (primary) at :233-234, contradicting the spec doc line 493 claim that the shadow feeds it — `crates/warpui_core/src/keymap/matcher.rs:229-243` _(both)_
- Shadow consumer #1: binding_for_custom_action_in_context uses keymap.custom_action_bindings() at :252 — `crates/warpui_core/src/keymap/matcher.rs:246-263` _(code)_
- Shadow consumer #2: macOS warp_app_has_custom_action_for_keystroke calls ctx.custom_action_bindings() at :386 (via app.rs:1911-1912 → matcher.rs:223-225); app/ has zero shadow references — `crates/warpui/src/platform/mac/app.rs:375-396` _(both)_
- set_custom_trigger (:137-141) and remove_custom_trigger (:146-152) both route through update_custom_trigger, so lockstep covers every external mutation — `crates/warpui_core/src/keymap/matcher.rs:137-152` _(code)_
- Precedent for a derived index with no duplicated state already exists: editable_bindings_by_name HashMap of Vec<usize> — `crates/warpui_core/src/keymap.rs:28-30` _(code)_
- EditableBinding has #[derive(Clone)] at :292 (struct at :293), so each shadow copy owns an independent custom_trigger Option<Trigger> — `crates/warpui_core/src/keymap.rs:292-304` _(code)_

- _Unblocks:_ Lets the design doc decide the keymap data structure for layering: replace the two cloned shadow vecs with a single layered store + a derived Custom index (mirroring editable_bindings_by_name), collapsing update_custom_trigger's dual write to one mutation and removing the primary/shadow desync class — while knowing the shadow only feeds binding_for_custom_action_in_context and the macOS menu path, not the keystroke/standard/custom match hot paths.
- _Residual / needs decision:_ The unify-vs-shadow choice is a design decision, not a code fact: code shows it is feasible and low-risk to unify (shadow is off all dispatch hot paths; the editable_bindings_by_name derived-index precedent exists), but whether each layer/mode-pack carries its own Custom shadow or the keymap derives a single global Custom index is for the design doc. Also undetermined in code: whether a per-layer model retains the macOS menuNeedsUpdate perf optimization (the shadow's only motivation per keymap.rs:32-35) as a derived/cached index or accepts a full bindings() scan. Scope note: the knowledge graph covers warpui_core/editor/vim only; the macOS consumer in crates/warpui and AppContext wrappers in core/app.rs were read directly and confirmed.
- _Verification:_ I independently opened keymap.rs (lines 1-100, 277-305, 385-485), matcher.rs (130-160, 220-370), mac/app.rs (375-399), core/app.rs (1630-1645, 1908-1914), and the spec block (lines 491-496). I re-ran the grep for custom_trigger assignment writers (only :428 and :431) and for every custom_action_bindings call site (mac/app.rs:386, app.rs:1911-1912, matcher.rs:224/252, keymap.rs:474). I checked for additional shadow-vec writers (none beyond :396, :411-412, :424) and confirmed app/ has no references to the shadows. I verified the answer's most aggressive claim — its correction of the spec doc — by reading default_binding_for_custom_action directly: it walks self.keymap.bindings() (matcher.rs:233-234, primary), so the spec's line-493 assertion that the shadow feeds default_binding_for_custom_action is indeed wrong, and the answer's correction is right. I looked for shifted line refs, hot-path overgeneralization, app/-vs-crates scope confusion, and miscounts; found none material. The only nit (EditableBinding derive Clone) is actually correct — the #[derive(Clone)] is at :292 exactly. The feasibility/unify portion is a design inference and is properly flagged as such in residual_unknowns.

### A3-Q9 — CONFIRMED · confidence: high

**Q.** Must a real Unbound state suppress the original_trigger fallback in match_custom (verified at matcher.rs:370-373), and does doing so break the intended 'rebind a menu action's keystroke but keep the menu item firing' behavior that the fallback was built for?

**A.** Yes — a real Unbound MUST suppress the original_trigger fallback, because today Trigger::Empty does NOT unbind a Custom-triggered binding (match_custom still matches via original_trigger at matcher.rs:370-373). And no, suppressing it need not break the legitimate rebind case: Rebound (trigger=Keystrokes) and Unbound (trigger=Empty) are already discriminable via Trigger::is_empty (keymap.rs:52-54), so the fallback can be gated on !trigger.is_empty() to suppress it only for genuine unbinds while preserving rebinds. The same fallback exists in two sibling resolvers (matcher.rs:240, :258), so the guard must be applied at all three Custom resolvers, not just match_custom.

Independently verified against the code; the colleague's answer is accurate on every material point.\n\n1) match_custom (matcher.rs:363-377) matches a Custom action two ways: (a) current trigger `if let Trigger::Custom(tag) = binding.trigger` (365-368), and (b) UNCONDITIONALLY the recorded `if let Some(Trigger::Custom(tag)) = binding.original_trigger` (370-373). There is no Empty/non-Empty guard before consulting original_trigger. Confirmed verbatim.\n\n2) 'none' flow: app/src/keyboard.rs:15 defines REMOVED_KEYBINDING_SERIALIZATION="none"; TryFrom at keyboard.rs:192-193 decodes the literal "none" into UserDefinedKeybinding::Removed; the Removed arm at keyboard.rs:43-44 calls app.set_custom_trigger(name, Trigger::Empty). set_custom_trigger (matcher.rs:137-141) → keymap.update_custom_trigger(name, Some(Empty)) (keymap.rs:422-433), setting custom_trigger=Some(Empty). (Minor note: the answer compressed the two-step "none"→Removed→Empty into "line 44 maps 'none'"; line 44 actually maps Removed→Empty, with the literal-string decode at 192-193 — substantively correct.)\n\n3) Lens result: EditableBinding::as_lens (keymap.rs:741-747) — when custom_trigger is Some it returns (trigger=custom_trigger, original_trigger=Some(&self.trigger)); else (&self.trigger, None). So an 'unbound' menu/Custom editable binding yields trigger=Empty, original_trigger=Some(Custom(tag)). push_keystroke (matcher.rs:324-325) only matches Trigger::Keystrokes, so Empty correctly drops the KEYBOARD match; BUT match_custom (370-373) still matches original_trigger=Custom(tag) and returns the action. Net: menu/Custom dispatch is NOT unbound — the leak the report's L4 (report:129) describes ("cannot truly unbind a default except by overloading Trigger::Empty"). Confirmed.\n\n4) Rebound case: custom_trigger=Keystrokes([..]) (keyboard.rs:47) → trigger=Keystrokes (keyboard fires via push_keystroke 325) AND original_trigger=Custom(tag) (menu still fires via match_custom 370-373). This is the intended 'rebind a menu action's keystroke but keep the menu item firing' behavior. Confirmed.\n\n5) Disentangling Unbound vs Rebound needs NO new field: Rebound has trigger=Keystrokes (non-empty); Unbound has trigger=Empty (Trigger::is_empty, keymap.rs:52-54). Guarding the fallback with `!binding.trigger.is_empty()` suppresses original_trigger only when the current trigger is itself inert (Empty), making 'none' a true unbind for Custom triggers while leaving rebinds untouched. Confirmed sound.\n\n6) Scope: the identical `(Trigger::Custom(tag), _) | (_, Some(Trigger::Custom(tag)))` fallback appears in default_binding_for_custom_action (matcher.rs:237-242, :240) and binding_for_custom_action_in_context (matcher.rs:255-262, :258). Confirmed — a true Unbound state / is_empty guard must be applied at all three Custom resolvers (cross-refs A3-Q11).\n\n7) Fixed bindings are NOT affected: FixedBinding::as_lens hardcodes original_trigger:None (keymap.rs:638) and there is no custom_trigger override path for fixed bindings (register_fixed_bindings, keymap.rs:391-399). So the unbind leak is specific to editable Custom-triggered bindings — exactly the class menu items use. Confirmed.\n\nTEST GAP confirmed: keymap_tests.rs:319 (`update_custom_trigger("first", None)`) only exercises the remove-customization revert path with Keystrokes-default actions (assertions at 327-339), not the Empty/unbind path. A grep over matcher_tests.rs returned ZERO references to match_custom / Trigger::Empty / original_trigger, corroborating that no test asserts the current 'none on a Custom-triggered editable binding still fires via match_custom' behavior. A failing test should be added on a Custom-trigger editable binding with custom_trigger=Empty asserting match_custom currently returns Action (documents the leak) and the desired post-fix None.

_Evidence:_
- match_custom consults original_trigger unconditionally (no Empty guard), so an 'unbound' Custom binding still dispatches via the second branch — `crates/warpui_core/src/keymap/matcher.rs:363-377 (esp. 370-373)` _(code)_
- 'none' constant decodes to UserDefinedKeybinding::Removed (TryFrom) which maps to Trigger::Empty via set_custom_trigger — `app/src/keyboard.rs:15,192-193,43-44` _(code)_
- set_custom_trigger -> update_custom_trigger sets custom_trigger=Some(Empty) — `crates/warpui_core/src/keymap/matcher.rs:137-141; crates/warpui_core/src/keymap.rs:422-433` _(both)_
- as_lens: when custom_trigger=Some, trigger=custom_trigger and original_trigger=Some(default trigger); thus Empty override leaves original_trigger=Custom(tag) — `crates/warpui_core/src/keymap.rs:741-747` _(code)_
- Rebound case: custom_trigger=Keystrokes makes keyboard fire (push_keystroke only matches Keystrokes) while menu still fires via original_trigger — `crates/warpui_core/src/keymap/matcher.rs:324-325,370-373; app/src/keyboard.rs:47` _(code)_
- Trigger::Empty exists and Trigger::is_empty discriminates Unbound(Empty) from Rebound(Keystrokes), enabling a !trigger.is_empty() fallback gate — `crates/warpui_core/src/keymap.rs:43-49,52-54` _(code)_
- Same Custom-fallback leak exists in two sibling resolvers, so suppression must apply to all three resolvers — `crates/warpui_core/src/keymap/matcher.rs:237-242(:240),255-262(:258)` _(code)_
- Fixed bindings have original_trigger:None and no custom_trigger override path, so the unbind leak is specific to editable Custom-triggered bindings — `crates/warpui_core/src/keymap.rs:638,391-399` _(both)_
- Existing test only covers custom_trigger=None revert with Keystrokes defaults; grep of matcher_tests.rs finds no match_custom/Trigger::Empty/original_trigger test asserting the leak — `crates/warpui_core/src/keymap_tests.rs:319-339; grep matcher_tests.rs (0 hits)` _(both)_
- Report L4/R2 corroborate: cannot truly unbind except by overloading Trigger::Empty; R2 calls for an explicit Unbound state — `.understand-anything/keybindings-architecture-report.md:129,140` _(code)_

- _Unblocks:_ Lets the design define two distinct states — Rebound (keep original_trigger fallback) vs Unbound (suppress it) — and specify that Unbound suppression is implemented either by gating the original_trigger fallback on !trigger.is_empty() or by a dedicated Unbound/tombstone variant, applied uniformly across all three Custom-action resolvers, so 'none' actually unbinds menu/Custom dispatch without breaking legitimate keystroke rebinds of menu actions.
- _Residual / needs decision:_ Product decision (not code-settleable): whether 'unbind keystroke' should also disable menu/Custom dispatch, or whether some actions must remain reachable from the menu even when keyboard-unbound — i.e., whether Unbound means 'no keystroke' or 'fully inert'. Design decision (cross-refs A3-Q10/Q12): whether to model Unbound as a distinct enum/layer tombstone vs. reuse Trigger::Empty + an is_empty() fallback guard; both are viable in code but interact with the conflict map and any settings 'state' badge. Either way the fix must be applied at all three Custom resolvers (match_custom, default_binding_for_custom_action, binding_for_custom_action_in_context). One small unverified runtime detail: I confirmed the data-flow statically but did not execute a test proving match_custom returns Action for a live Empty-override Custom binding — the recommended failing test would close that.
- _Verification:_ I opened every cited file and confirmed each line reference. matcher.rs:363-377 match_custom has the unconditional original_trigger second branch (370-373) with no is_empty guard — verified verbatim. matcher.rs:137-141, keymap.rs:422-433, keymap.rs:741-747, keymap.rs:43-55, keymap.rs:638, keymap.rs:391-399, matcher.rs:324-346, and the two sibling resolvers at matcher.rs:237-243/246-263 (fallback at :240 and :258) all say exactly what the answer asserts. I checked a location the answer under-cited: the actual 'none'->Removed string decode is in TryFrom at keyboard.rs:192-193, not literally at line 44 (line 44 is Removed->Trigger::Empty) — a harmless compression in the answer's prose; the substance and the constant at line 15 are correct, so I added 192-193 to the evidence. I independently grepped matcher_tests.rs (where match_custom lives, being a Matcher method) and found ZERO references to match_custom/Trigger::Empty/original_trigger, which strengthens (rather than weakens) the TEST GAP claim. Report lines 129 (L4) and 140 (R2) match the quoted text exactly. No overgeneralization found: the answer correctly scopes the leak to editable Custom-triggered bindings and excludes fixed bindings, and correctly notes app/ vs crates/ boundaries (keyboard.rs in app/, matcher/keymap in crates/warpui_core). No shifted line refs, no off counts, no graph-vs-code contradiction. The proposed !trigger.is_empty() gate is logically sound against the verified as_lens semantics.

### A3-Q10 — CONFIRMED · confidence: high

**Q.** In the new model, are Unbound bindings excluded from the precedence walk / catalog / conflict detection, or retained as tombstones — given EditableBinding::new defaults trigger=Empty (keymap.rs:660) and removal sets custom_trigger=Empty (keybindings.rs:592), both still yielded by bindings()?

**A.** Today there is no first-class Unbound/tombstone type. An "unbound" binding is just Trigger::Empty, which is STILL yielded by bindings() (the only filter on either editable or fixed iterators is is_enabled()/enabled_predicate — never trigger emptiness). It is RETAINED in the precedence walk (engine bindings()) and the UI catalog (rendered as trigger=None and kept as a resettable row), EXCLUDED from conflict detection (Empty→None is flattened out and has_conflict(None)=false), and acts as a de-facto tombstone for its OWN default in keystroke matching (push_keystroke/match_standard never read original_trigger). But it LEAKS for Custom/menu triggers because match_custom, default_binding_for_custom_action, and binding_for_custom_action_in_context all fall back to original_trigger==Custom. So Trigger::Empty is a within-the-same-binding override-nullification, not a cross-layer key-level block: in a multi-layer model it cannot suppress a SEPARATE lower-layer binding on the same key — the linear scan simply skips the Empty entry and continues. The new resolver needs an explicit Unbound entry recognized by all three match paths (and the Custom original_trigger fallback suppressed for genuine unbinds), while staying invisible to ConflictMap (already true).

VERDICT: confirmed. I independently opened all cited files and every evidence claim's file:line says what the answer asserts. Line numbers are exact (not shifted), the editable-vs-fixed and keystroke-vs-custom distinctions are correctly drawn (no overgeneralization), and the app/-vs-crates scope is correct.

CODE FACTS RE-VERIFIED:

1) Premise correct — Empty/Unbound survives bindings(). Keymap::bindings() (keymap.rs:454-464) chains editable_bindings() (mapped to as_binding) then reversed fixed_bindings; the ONLY filter on editable is is_enabled() (keymap.rs:446) and on fixed is is_enabled() (keymap.rs:461). is_enabled() = enabled_predicate.is_none_or(|p| p()) for FixedBinding (keymap.rs:626-628) and for EditableBindingLens (keymap.rs:782-784); neither consults the trigger. So a Trigger::Empty binding is iterated by every consumer. EditableBinding::new defaults trigger=Trigger::Empty (keymap.rs:660). as_lens: when custom_trigger is Some, lens.trigger=custom_trigger and original_trigger=Some(&self.trigger) (keymap.rs:742-744) — so a removal that sets custom_trigger=Empty yields effective Empty with the original preserved.

2) Trigger enum (keymap.rs:43-49) has exactly Keystrokes/Standard/Custom/Empty — NO explicit Unbound variant. Empty is documented "cannot actually be matched" (keymap.rs:48): inert, not a designed block.

3) Three match paths all walk the SAME bindings() iterator:
   - push_keystroke (matcher.rs:324-338): only `if let Trigger::Keystrokes` matches; original_trigger is NEVER read here, so an unbound editable Keystrokes binding's default does not resurface (tombstone for its own default; early return at 329-331).
   - match_standard (matcher.rs:351-358): only Trigger::Standard; Empty falls through.
   - match_custom (matcher.rs:363-377): matches Trigger::Custom (365-368) OR original_trigger==Custom (370-373). So a "removed" (custom_trigger=Empty) Custom/menu binding still fires — the leak. Same original_trigger fallback in default_binding_for_custom_action (matcher.rs:237-242) and binding_for_custom_action_in_context (matcher.rs:255-262).

4) Catalog & conflict detection (app/, read directly):
   - trigger_to_keystroke maps Trigger::Empty => None (app/src/util/bindings.rs:256; whole fn 234-258). CommandBinding.trigger is Option<Keystroke> (bindings.rs:534), built via trigger_to_keystroke in from_lens (bindings.rs:752) and from_editable_lens (bindings.rs:765). So an unbound binding renders as no-shortcut.
   - remove_keystroke (app/src/settings_view/keybindings.rs:590-602): set_custom_trigger(name, Trigger::Empty) at 592, conflict_map.update(&row.binding.trigger, None) at 596, persists UserDefinedKeybinding::Removed at 600, sets row.binding.trigger=None at 602 but KEEPS the row (resettable via reset_to_default_keystroke at 617-627).
   - ConflictMap is HashMap<Keystroke,usize> (keybindings.rs:105-107); update() only counts Some (110-121); has_conflict(None)=false (line 130); FromIterator flattens out None (line 139); startup populates from binding.trigger via flatten (794-800). So None/unbound entries are already excluded from conflict detection.

ANSWER (excluded vs tombstone): Neither cleanly — a hybrid with no first-class Unbound type. Unbound = Trigger::Empty: RETAINED in the precedence walk and the UI catalog (None row), EXCLUDED from conflict detection, tombstone for its OWN default in keystroke matching, but LEAKY for Custom/menu via original_trigger.

DESIGN IMPLICATION (correctly labeled as a decision, not a code fact): Trigger::Empty is a within-the-same-binding override-nullification, not a cross-layer key-level block. In the single-layer model that suffices (default and override are one object; custom_trigger overrides trigger in place). In a multi-layer model, a user-layer Unbound that must suppress a SEPARATE lower-layer (default/mode-pack) binding on the same keystroke is NOT achievable by Empty alone — push_keystroke's linear scan (matcher.rs:324-338) just skips the Empty entry and matches the lower-layer binding. So a real tombstone needs an explicit "block this key at this layer" entry recognized by all three paths, with the Custom original_trigger fallback suppressed for genuine unbinds (Q9/Q11), while remaining invisible to ConflictMap (already true).

_Evidence:_
- bindings() yields editable (as_binding) then reversed fixed, each filtered ONLY by is_enabled(), never by trigger emptiness — Empty/Unbound survives the walk — `crates/warpui_core/src/keymap.rs:454-464` _(code)_
- editable_bindings() iterator filters only on is_enabled() at :446, not on trigger — `crates/warpui_core/src/keymap.rs:441-447` _(code)_
- FixedBinding::is_enabled() = enabled_predicate.is_none_or(|p| p()); trigger never consulted (this is_enabled returns a BindingLens with original_trigger:None at :638, confirming it is FixedBinding's) — `crates/warpui_core/src/keymap.rs:626-628` _(code)_
- EditableBindingLens::is_enabled() = enabled.is_none_or(|p| p()); trigger never consulted — `crates/warpui_core/src/keymap.rs:782-784` _(code)_
- Trigger enum has exactly Keystrokes/Standard/Custom/Empty — no first-class Unbound; Empty documented 'cannot actually be matched' — `crates/warpui_core/src/keymap.rs:43-49` _(code)_
- EditableBinding::new defaults trigger=Trigger::Empty — `crates/warpui_core/src/keymap.rs:660` _(code)_
- as_lens: when custom_trigger is Some, lens.trigger=custom_trigger and original_trigger=Some(&self.trigger); custom_trigger=Empty yields effective Empty with original preserved — `crates/warpui_core/src/keymap.rs:742-744` _(code)_
- push_keystroke only matches Trigger::Keystrokes and never reads original_trigger (early return :329-331), so an unbound Keystrokes binding's default does not resurface — `crates/warpui_core/src/keymap/matcher.rs:324-338` _(code)_
- match_standard only matches Trigger::Standard; Empty falls through; original_trigger not read — `crates/warpui_core/src/keymap/matcher.rs:351-358` _(code)_
- match_custom matches Trigger::Custom (:365-368) OR original_trigger==Custom (:370-373), so a 'removed' (custom_trigger=Empty) Custom/menu binding still fires — the leak — `crates/warpui_core/src/keymap/matcher.rs:363-377` _(code)_
- default_binding_for_custom_action also falls back to original_trigger==Custom, same leak path — `crates/warpui_core/src/keymap/matcher.rs:237-242` _(code)_
- binding_for_custom_action_in_context also falls back to original_trigger==Custom — `crates/warpui_core/src/keymap/matcher.rs:255-262` _(code)_
- Settings remove_keystroke sets custom_trigger=Empty (:592), conflict_map.update(old, None) (:596), persists Removed (:600), sets row trigger=None (:602) but keeps the row (resettable at :617-627) — `app/src/settings_view/keybindings.rs:590-602` _(code)_
- ConflictMap is HashMap<Keystroke,usize> (:105-107); update() counts only Some; has_conflict(None) returns false (:130) — `app/src/settings_view/keybindings.rs:104-132` _(code)_
- ConflictMap::from_iter flattens out None (:139), so unbound (None) entries are never counted as conflicts — `app/src/settings_view/keybindings.rs:135-145` _(code)_
- Conflict map populated at startup from binding.trigger via flatten, dropping None — `app/src/settings_view/keybindings.rs:794-800` _(code)_
- trigger_to_keystroke maps Trigger::Empty => None, so the UI catalog renders an unbound binding as no-shortcut — `app/src/util/bindings.rs:234-258` _(code)_
- CommandBinding.trigger is Option<Keystroke> (:534); built from lens via trigger_to_keystroke in from_lens (:752) and from_editable_lens (:765) — `app/src/util/bindings.rs:530-771` _(code)_

- _Unblocks:_ Lets the design doc specify Unbound as an explicit tombstone in the layer model rather than reusing Trigger::Empty: an Unbound layer entry that (1) is recognized by the precedence walk as a key-level 'block' that stops fall-through to lower-precedence/default layers (which Trigger::Empty does NOT do today — the scan just skips it), (2) is applied once at the bindings()/resolver level so all three match paths (push_keystroke, match_standard, match_custom) inherit it and the Custom original_trigger leak is closed, and (3) stays invisible to ConflictMap (already true via None handling), avoiding phantom conflicts in Axis 5's conflict map.
- _Residual / needs decision:_ Tombstone-vs-delete is a design decision, not a code fact. Code establishes only: (a) no first-class Unbound type exists — Trigger::Empty is reused, and Empty entries are retained in iteration/catalog, excluded from conflict detection, inert in keystroke matching but leaky for Custom/menu via original_trigger; (b) Trigger::Empty cannot block a SEPARATE lower-layer binding on the same key — it only nullifies its own binding's trigger. Open and not settleable from current code: (1) whether to suppress the original_trigger Custom fallback for a genuine unbind (interacts with Q9's legitimate-rebind use case); (2) how Unbound composes with the other 'off' switches — context_predicate=Just(false), enabled_predicate, Trigger::Empty (Q12) — so 'is this key active?' has one answer; (3) whether the settings catalog ever filters out None-trigger rows from display in any view other than remove_keystroke's keep-the-row path (I confirmed remove_keystroke keeps the row, but did not exhaustively audit every catalog render filter).
- _Verification:_ I opened every cited file/range and confirmed all line refs are exact, not shifted. Specifically verified: (1) bindings() filters only on is_enabled() for BOTH editable (:446) and fixed (:461), and neither is_enabled (FixedBinding :626-628; EditableBindingLens :782-784) consults the trigger; I disambiguated the two is_enabled definitions by their surrounding as_lens (the :626 one builds a BindingLens with original_trigger:None at :638 = FixedBinding). (2) Trigger enum (:43-49) really has no Unbound variant — confirms the 'no first-class type' claim. (3) push_keystroke (:324-338) and match_standard (:351-358) never reference original_trigger, while match_custom (:370-373), default_binding_for_custom_action (:237-242) and binding_for_custom_action_in_context (:255-262) all do — confirming the asymmetric Custom 'leak'. (4) app/ side: trigger_to_keystroke Empty=>None at exactly :256; CommandBinding.trigger Option<Keystroke> at :534; from_lens :752 / from_editable_lens :765; remove_keystroke block :590-602 with the exact statements (set_custom_trigger Empty :592, update(...,None) :596, persist Removed :600, row.trigger=None :602, row kept); ConflictMap has_conflict(None)=false :130 and from_iter flatten :139; startup flatten :794-800. I checked a location the answer did NOT explicitly call out — the Trigger enum definition and the struct boundary distinguishing the two is_enable implementations — and both support, not contradict, the answer. No overgeneralization found: the answer correctly limits the leak to Custom/menu and the 'own-default tombstone' to keystroke matching. No app/-vs-crates confusion. Counts: not applicable (no numeric count claim was made beyond 'four' implicit Trigger variants, which is correct). The doc comment at keymap.rs:765 ('Will return None if there is no trigger') is slightly misleading since as_binding never returns Option, but the answer does not rely on it, so this is immaterial.

### A3-Q11 — CONFIRMED · confidence: high

**Q.** Does the (layer, recency) precedence and the new Unbound state apply uniformly across ALL THREE match paths that independently walk bindings() — push_keystroke for Keystrokes (matcher.rs:324), match_standard for Standard (matcher.rs:351), match_custom for Custom (matcher.rs:363) — or only to keystroke matching?

**A.** Confirmed. Precedence (layer + recency) IS uniform across all three match paths because all three walk the same Keymap::bindings() iterator and take the first match — ordering is centralized in bindings(), not in any per-path code. But the existing Unbound state (Trigger::Empty) is NOT uniform: it suppresses dispatch in push_keystroke and match_standard, yet match_custom's original_trigger fallback (matcher.rs:370-374) still matches an unbound Custom/menu binding, so unbinding a Custom action silently no-ops. The crux — is_enabled() (keymap.rs:782-784) filters only on enabled_predicate, never on the trigger, so an Empty-trigger binding stays in the iterator with original_trigger=Some(Custom(tag)) and re-matches. Verified independently.

PRECEDENCE — UNIFORM (verified). All three loops iterate the identical iterator self.keymap.bindings(): push_keystroke at matcher.rs:324 (filters Trigger::Keystrokes at :325, early-returns full match at :329-331), match_standard at matcher.rs:351 (filters Trigger::Standard at :352, returns first match at :354), match_custom at matcher.rs:364 (filters Trigger::Custom at :365, returns at :367). The (layer, recency) order lives entirely inside Keymap::bindings() (keymap.rs:454-464): editable_bindings() first (keymap.rs:441-447 — .rev() = reverse-registration/LIFO recency, filtered by is_enabled()), chained with fixed_bindings.iter().rev(). So layer = editable-before-fixed and recency = LIFO-within-group are both centralized in one place; any new (layer, recency) sort installed at bindings() is inherited by all three paths. The three dispatchers also share an identical structure — the answer's cited app.rs lines 1873/1988/2014 are EXACTLY the `for (i, ctx) in context_chain.iter_mut().enumerate().rev()` reverse responder/context-chain loops in dispatch_standard_action (fn at app.rs:1866), dispatch_custom_action_internal (fn at app.rs:1981) and dispatch_keystroke (fn at app.rs:2005), each immediately calling match_standard/match_custom/push_keystroke — so context-layering is uniform too. (Note the file is crates/warpui_core/src/core/app.rs, which the evidence path states correctly; the prose 'app.rs' is shorthand, and app/src/core/app.rs does not exist.)

UNBOUND STATE — NOT UNIFORM (the correctness hole, verified). Unbinding maps to custom_trigger = Some(Trigger::Empty): on-disk 'none'/UserDefinedKeybinding::Removed -> app.set_custom_trigger(name, Trigger::Empty) (keyboard.rs:15 REMOVED_KEYBINDING_SERIALIZATION='none', :44), and the settings UI remove_keystroke does the same (settings_view/keybindings.rs:592); both route through Keymap::update_custom_trigger (keymap.rs:422). EditableBinding::as_lens (keymap.rs:742-746) then yields trigger=&Empty AND original_trigger=Some(&self.trigger) (the default, which for a Custom action is Trigger::Custom(tag)). Crucially, is_enabled() (keymap.rs:782-784) tests only self.enabled (the enabled_predicate), NOT the trigger, so an Empty-trigger binding is NOT filtered out of editable_bindings()/bindings(); it still appears in the iterator. Per path:
- push_keystroke (matcher.rs:325): only matches Trigger::Keystrokes on binding.trigger; Empty skipped -> unbind effective.
- match_standard (matcher.rs:352): only matches Trigger::Standard on binding.trigger; never consults original_trigger; Empty skipped -> unbind effective.
- match_custom (matcher.rs:364-374): checks Trigger::Custom on binding.trigger (Empty skipped) AND ALSO on binding.original_trigger (matcher.rs:370-374). For an unbound Custom binding original_trigger=Some(Trigger::Custom(tag)), so it STILL MATCHES and dispatches. Unbinding a Custom/menu trigger therefore silently no-ops. The Trigger::Empty enum comment 'empty trigger (cannot actually be matched)' (keymap.rs:48) reflects the author's assumption that Empty can never match — an assumption the original_trigger fallback silently violates for Custom triggers. FixedBinding (keymap.rs:277-286) has no name field and no custom_trigger, so this only affects editable Custom bindings (fixed/menu bindings cannot be unbound at all).

ASYMMETRY: match_standard ignores original_trigger entirely while match_custom consults it. The fallback is presumably intentional for REMAPPING (custom_trigger=Keystrokes keeps the menu's Custom dispatch alive), but it leaks for UNBINDING (custom_trigger=Empty). The two Trigger families have divergent original_trigger semantics today.

TEST COVERAGE: matcher_tests.rs has 0 references to match_standard/match_custom/Standard/Custom (verified by grep -c = 0); only push_keystroke is exercised, via the test_keystroke helper at matcher_tests.rs:229-236. The Standard/Custom paths and the unbind-of-Custom hole are untested in the matcher.

DESIGN IMPLICATION: To make precedence + Unbound apply uniformly, resolve layer order and Unbound/tombstone suppression once at the bindings()/resolver level (all three inherit it), AND make match_custom's original_trigger fallback tombstone-aware (skip when the current trigger is an Unbound tombstone). Otherwise re-layering/unbinding leaks dispatch for menu/Custom actions. Add Standard and Custom unbind/precedence fixtures (Q12). 103 non-test with_custom_action/with_standard_action registrations size the blast radius (reproduced exactly).

_Evidence:_
- All three match paths iterate the same self.keymap.bindings() iterator and take the first match: push_keystroke loop at :324 filters Keystrokes (early-return :329-331); match_standard loop at :351 filters Standard (returns :354); match_custom loop at :364 filters Custom (returns :367). — `crates/warpui_core/src/keymap/matcher.rs:324-377` _(code)_
- match_custom UNIQUELY also matches on binding.original_trigger, so an unbound Custom binding (original_trigger=Some(Custom(tag))) still dispatches — unbind no-ops for Custom. match_standard/push_keystroke only inspect binding.trigger. — `crates/warpui_core/src/keymap/matcher.rs:365-374` _(code)_
- Precedence ordering (layer=editable-before-fixed; recency=reverse-registration/LIFO) is centralized in Keymap::bindings(): editable_bindings() (.rev(), is_enabled-filtered) chained with fixed_bindings.iter().rev(). — `crates/warpui_core/src/keymap.rs:441-464` _(both)_
- as_lens yields trigger=Empty and original_trigger=Some(&self.trigger) when custom_trigger is set; default trigger of a Custom action is Trigger::Custom(tag) (with_custom_action :737). — `crates/warpui_core/src/keymap.rs:733-759` _(code)_
- CRITICAL: is_enabled() tests only the enabled_predicate, NOT the trigger, so an Empty-trigger binding is NOT filtered out of the iterator — enabling the original_trigger re-match. — `crates/warpui_core/src/keymap.rs:782-784` _(code)_
- Trigger enum has Empty variant commented 'empty trigger (cannot actually be matched)' — author assumption the original_trigger fallback silently violates for Custom. — `crates/warpui_core/src/keymap.rs:43-49` _(code)_
- All three dispatchers wrap the matcher call in an identical reverse context-chain loop `for (i,ctx) in context_chain.iter_mut().enumerate().rev()` (cited lines are the loop lines, not fn sigs): dispatch_standard_action fn:1866 loop:1873; dispatch_custom_action_internal fn:1981 loop:1988; dispatch_keystroke fn:2005 loop:2014. File is crates/warpui_core/src/core/app.rs (not app/src). — `crates/warpui_core/src/core/app.rs:1873,1988,2014` _(code)_
- Unbind = custom_trigger Some(Trigger::Empty): on-disk 'none'/Removed -> set_custom_trigger(name, Trigger::Empty); routes through Keymap::update_custom_trigger which only touches editable bindings. — `app/src/keyboard.rs:15,44; crates/warpui_core/src/keymap.rs:422-433` _(code)_
- Settings UI remove_keystroke also sets Trigger::Empty as the custom trigger. — `app/src/settings_view/keybindings.rs:592` _(code)_
- FixedBinding has no name field and no custom_trigger, so unbind-by-name only affects editable Custom bindings; fixed/menu bindings cannot be unbound. — `crates/warpui_core/src/keymap.rs:277-304` _(code)_
- matcher_tests.rs has 0 occurrences of match_standard/match_custom/Standard/Custom (grep -c=0); only push_keystroke is tested via test_keystroke helper. — `crates/warpui_core/src/keymap/matcher_tests.rs:229-236` _(code)_
- grep -rn 'with_custom_action|with_standard_action' app/src crates --include=*.rs | grep -v test | wc -l = 103 registrations (reproduced exactly). — `app/src + crates (grep)` _(code)_

- _Unblocks:_ Confirms the resolver must apply layer-precedence AND Unbound/tombstone suppression once at the bindings()/shared-helper level so all three match paths inherit them, and that match_custom's original_trigger fallback must be made tombstone-aware — otherwise unbinding or re-layering menu/Standard actions silently fails. Justifies adding Standard and Custom unbind/precedence test fixtures (Q12).
- _Residual / needs decision:_ Whether the new Unbound is modeled as a distinct tombstone layer entry or reuses Trigger::Empty is a design choice, not a code fact. The fix for match_custom's original_trigger fallback (tombstone-aware skip vs. remove the fallback vs. gate on layer state) is a design decision. The 103 figure is total non-test with_custom_action/with_standard_action registrations and was not partitioned by user-facing context predicate (e.g. CodeEditorView) vs internal/menu plumbing. I did not exhaustively confirm that every Custom registration's default self.trigger is Trigger::Custom (verified the with_custom_action setter does so), nor enumerate cases where custom_trigger could be set on a binding whose default trigger is non-Custom.
- _Verification:_ I independently opened every cited file. matcher.rs:324-377 confirms all three loops iterate self.keymap.bindings(); push_keystroke early-returns at 329-331, match_standard returns at 354, match_custom returns at 367 AND has the extra original_trigger check at 370-374 — exactly as claimed. keymap.rs:454-464 confirms bindings() = editable_bindings().rev() chained with fixed_bindings.iter().rev() (centralized layer+recency). The load-bearing claim hinged on whether an Empty-trigger binding is filtered out before match_custom sees it; I opened is_enabled() (keymap.rs:782-784, NOT cited at that exact line by the answer's lens evidence but I checked it as my independent location) and confirmed it tests only enabled_predicate, never the trigger — so the Empty binding stays in the iterator and the original_trigger fallback re-matches. The unbind no-op for Custom is real. I traced the full chain: update_custom_trigger -> custom_trigger=Some(Empty) -> as_lens (trigger=Empty, original_trigger=Some(Custom(tag))) -> match_custom line 370 re-match. keyboard.rs:15/44, settings keybindings.rs:592, Trigger::Empty (keymap.rs:48), and the 103-count grep all reproduced exactly; matcher_tests grep returned 0 for Standard/Custom. The one discrepancy I hunted: the answer's prose writes bare 'app.rs' and the path app/src/core/app.rs does not exist — but the EVIDENCE source field correctly gives crates/warpui_core/src/core/app.rs, and lines 1873/1988/2014 are precisely the `.rev()` context-chain loop lines (fn signatures are at 1866/1981/2005), so the line refs are accurate to the loops, not shifted. Minor: test_keystroke helper is at 229-236 (answer said 230-236) and match_custom loop is line 364 (question header said 363, = fn sig) — immaterial. Extra check the answer omitted: FixedBinding (277-286) has no name field, confirming only editable Custom bindings are affected. Nothing materially wrong; the answer is accurate and well-cited.

### A3-Q12 — CORRECTED · confidence: high

**Q.** How does a user-layer Unbound relate to the THREE existing suppression mechanisms — context_predicate=Just(false), the zero-arg enabled_predicate closure (keymap.rs:626-627), and Trigger::Empty (keymap.rs:660) — so that 'is this key active in this context?' has a single, well-defined answer instead of four overlapping ones?

**A.** Architecture thesis is correct and well-anchored: the three suppression mechanisms live at three different layers and are never unified by any single is_active()/classify_state() (grep confirms none for bindings). enabled_predicate is a context-free, fn()->bool whole-binding filter applied at the bindings() iterator source (keymap.rs:446/461/471/481); context_predicate (incl. Just(false)) is evaluated per-call at each of the four consumers (matcher.rs:285,327,353,366/371), leaving the binding inside bindings(); Trigger::Empty is the implicit no-match fall-through and IS the existing unbind path (set_custom_trigger(name, Trigger::Empty), app/src/keyboard.rs:44 and settings_view/keybindings.rs:592). A user-layer Unbound is only a genuine fourth switch if it is a distinct tombstone rather than reusing Trigger::Empty. ONE correction: the answer says enabled_predicate feature-flag gating is "~30 app/ sites" — the actual count is 93 with_enabled call sites (83 closure-form + ~10 fn-pointer-form), ~3x higher.

VERDICT: corrected. Every structural/architectural claim was independently re-opened and holds at (or within one line of) the cited location. The only material defect is an undercounted statistic.

1) enabled_predicate — GLOBAL, context-free, whole-binding exclusion at the SOURCE. Type `EnabledPredicate = fn() -> bool` (keymap.rs:237). Read by is_enabled = `self.enabled_predicate.is_none_or(|predicate| predicate())` on both FixedBinding (keymap.rs:626-627) and EditableBindingLens (keymap.rs:782-783). APPLIED as `.filter(|b| b.is_enabled())` at the bindings() iterator: editable_bindings() keymap.rs:446, fixed branch :461, custom-action editable :471, custom-action fixed :481. A disabled binding is EXCLUDED from bindings() entirely — never reaches any matcher loop, never appears in bindings_for_context, and never reaches the settings list (which iterates ctx.editable_bindings() → keymap.editable_bindings(), already is_enabled-filtered, keybindings.rs:754). Doc-comment at keymap.rs:231-236 confirms the intent: feature-flag gating that re-evaluates at runtime. Semantics = UNAVAILABLE (binding does not exist in this build/runtime). CORRECTION: usage is broader than the answer claims — not "~30 app/ sites" but 93 `.with_enabled(` calls in app/src/ (83 closure-form like `.with_enabled(|| FeatureFlag::CodeFindReplace.is_enabled())`, plus ~10 fn-pointer-form like `.with_enabled(debug_notebooks_enabled)` and `.with_enabled(enabled_predicate)`); both code (app/src/code/editor/view/actions.rs:605) and terminal editor (app/src/editor/view/mod.rs:916 AgentMode, :923 !AgentMode) use it — so the same-mechanism, no-divergence claim still holds.

2) context_predicate — PER-CONTEXT, evaluated at each consumer, not at the source. ContextPredicate enum context.rs:10-18, variant Just(bool) at :17; default for editable bindings is Just(true) (keymap.rs:657). eval at context.rs:101-119; `Self::Just(val) => *val` at :117, so Just(false) returns false unconditionally. APPLIED per match-site, NOT at bindings(): bindings_for_context UI listing `.filter(move |binding| binding.context_predicate.eval(&context))` (matcher.rs:285), push_keystroke `binding.context_predicate.eval(ctx)` (matcher.rs:327, inside the Trigger::Keystrokes arm at :325 with early-return :329-331), match_standard (matcher.rs:353), match_custom (matcher.rs:366 and original_trigger path :371). The binding stays inside bindings(); it is rejected separately at each call. Just(false) = never matches in any context. Semantics = UNAVAILABLE in this context.

3) Trigger::Empty — IMPLICIT no-match fall-through and the EXISTING unbind path. Trigger::Empty keymap.rs:48 "empty trigger (cannot actually be matched)"; is_empty :52-53; default trigger of every freshly registered EditableBinding (keymap.rs:660). No consumer has a match arm for it: push_keystroke matches only Trigger::Keystrokes (matcher.rs:325), match_standard only Trigger::Standard (:352), match_custom only Trigger::Custom (:365/:370). An Empty-trigger binding falls through all three loops — present in bindings(), never matched. Unbinding TODAY = set_custom_trigger(name, Trigger::Empty) (matcher.rs:137 → keymap.rs:422 update_custom_trigger, which iterates both editable_custom_action_bindings :430-ish and editable_bindings :430). Two callers: (a) app/src/keyboard.rs:44 — on-disk Removed sentinel → UserDefinedKeybinding::Removed → set_custom_trigger(name, Trigger::Empty); (b) app/src/settings_view/keybindings.rs:592 — UI remove_keystroke → set_custom_trigger(..., Trigger::Empty) plus write_custom_keybinding(..., Removed) at :600. The override resolves in as_lens (keymap.rs:742-747): custom_trigger=Some(Empty) makes the resolved trigger Empty while original_trigger holds the default. Semantics = USER-UNBOUND.

UNIFICATION STATUS (the crux): No single is_active(binding, ctx) and no classify_state() for bindings exists. Grep for is_active/classify/is_unbound across crates/warpui_core/src returns only windowing/fullscreen hits (is_active_window_fullscreen, app_is_active) — nothing about binding suppression; the settings UI files have zero hits. The composite "is this key active here?" is implicitly is_enabled() (at bindings()) AND context_predicate.eval(ctx) (at each loop) AND trigger is a matchable non-Empty variant — computed at three different places, never named or centralized. A user-layer Unbound is NOT automatically a fourth mechanism: if it reuses Trigger::Empty it collapses into mechanism 3; if it is a distinct layer tombstone it becomes a genuine fourth needing explicit ordering.

MISREPORT RISK (grounded in code): the settings UI models a row as a single Option<Keystroke> (trigger_to_keystroke maps Trigger::Empty → None at app/src/util/bindings.rs:256) and decides affordances purely from `if self.binding.trigger.is_some()` (keybindings.rs:397), consulting neither enabled_predicate nor context_predicate. Because the UI list (keybindings.rs:754 ctx.editable_bindings()) applies is_enabled but NO context filter, an enabled_predicate=false binding is silently absent while a context_predicate=false binding still appears identical to an active one. So the UI today cannot distinguish "unavailable (feature/context)" from "user-unbound (Empty)" from "default-no-key (Empty)" — exactly the four-overlapping-states ambiguity the question warns about.

_Evidence:_
- enabled_predicate is a zero-arg context-free closure (fn()->bool), read by is_enabled on FixedBinding — `crates/warpui_core/src/keymap.rs:237,626-627` _(code)_
- is_enabled also exists on EditableBindingLens with identical is_none_or(|p| p()) body — `crates/warpui_core/src/keymap.rs:782-783` _(code)_
- enabled_predicate applied as whole-binding filter at the bindings() iterator source (editable, fixed, and both custom-action branches), excluding disabled bindings before any matcher loop — `crates/warpui_core/src/keymap.rs:446,461,471,481` _(code)_
- context_predicate (incl. Just(false)) is NOT filtered at bindings(); evaluated per-call. push_keystroke eval is at line 327 (Trigger::Keystrokes arm at 325), match_standard 353, match_custom 366 and original_trigger 371 — `crates/warpui_core/src/keymap/matcher.rs:325,327,353,366,371` _(both)_
- bindings_for_context (UI listing) applies the context filter separately — `crates/warpui_core/src/keymap/matcher.rs:285` _(code)_
- ContextPredicate::Just(bool) variant; Just(val)=>*val evaluates to false unconditionally for Just(false); editable default is Just(true) — `crates/warpui_core/src/keymap/context.rs:17,117; crates/warpui_core/src/keymap.rs:657` _(code)_
- Trigger::Empty is default editable trigger; cannot be matched; no match arm in any dispatch loop (each matches only its own variant) — `crates/warpui_core/src/keymap.rs:48,660; crates/warpui_core/src/keymap/matcher.rs:325,352,365` _(code)_
- Unbinding implemented as set_custom_trigger(name, Trigger::Empty) on the on-disk Removed load path — `app/src/keyboard.rs:44` _(code)_
- set_custom_trigger -> update_custom_trigger updates custom_trigger across BOTH editable_custom_action_bindings and editable_bindings; as_lens resolves custom_trigger=Some(Empty) to a resolved Empty trigger with original_trigger holding the default — `crates/warpui_core/src/keymap/matcher.rs:137; crates/warpui_core/src/keymap.rs:422,430,742-747` _(code)_
- UI remove_keystroke sets Trigger::Empty and persists UserDefinedKeybinding::Removed — `app/src/settings_view/keybindings.rs:592,600` _(code)_
- No unified binding-state is_active()/classify_state()/is_unbound(); only unrelated windowing is_active_* hits exist — `grep is_active|classify|is_unbound over crates/warpui_core/src (only windowing/state.rs, platform hits) and app/src/settings_view/keybindings.rs, app/src/util/bindings.rs (no hits)` _(code)_
- Settings UI collapses state to single Option<Keystroke> (Trigger::Empty -> None) and affordances key off trigger.is_some(), ignoring enabled/context predicates; UI list (editable_bindings) applies no context filter — `app/src/util/bindings.rs:256; app/src/settings_view/keybindings.rs:397,754` _(code)_
- CORRECTION: enabled_predicate feature-flag gating is ~93 with_enabled call sites in app/src/ (83 closure-form + ~10 fn-pointer-form), not ~30; used in both code and terminal editors — `grep -rn '\.with_enabled(' app/src (93 total; 83 match '\.with_enabled(||'); app/src/code/editor/view/actions.rs:605; app/src/editor/view/mod.rs:916,923` _(code)_

- _Unblocks:_ Lets the design doc specify a single is_active(binding, ctx, layers) predicate plus a classify_state() that orders the (now four) off-switches, deciding explicitly that enabled_predicate=false and context_predicate=false render as 'unavailable' while Trigger::Empty/user-layer Unbound render as 'user-unbound', so the matcher and the settings badge cannot disagree.
- _Residual / needs decision:_ Pure design decisions (not code-derivable): (1) whether the new user-layer Unbound should be a distinct tombstone/layer entry or reuse Trigger::Empty — reuse keeps it mechanism 3, a tombstone makes it a genuine fourth needing explicit ordering; (2) canonical precedence among the four when more than one fires (e.g. enabled_predicate=false vs user-Unbound on the same binding); (3) how the settings state badge should render 'unavailable' (enabled/context predicate false) distinctly from 'user-unbound' vs 'default-no-key', which today all collapse to Option<Keystroke>::None or absence. Also not exhaustively classified: of the 93 with_enabled sites, the exact split between FeatureFlag-gated vs ContextFlag-gated vs debug-gated vs settings-view enable/disable pairs (sampled, not fully enumerated) — but all are binding-level enabled_predicate setters, which is the load-bearing point.
- _Verification:_ I independently opened every cited file. Verified verbatim: Trigger enum/Empty (keymap.rs:43-55), EnabledPredicate type + doc (231-237), FixedBinding is_enabled (626-628) and EditableBindingLens is_enabled (782-784), bindings()/editable_bindings() filters (441-484), EditableBinding::new defaults context Just(true)/enabled None/trigger Empty (653-664), with_enabled at 605 and 674, as_lens custom_trigger resolution (741-759), update_custom_trigger (422-430), ContextPredicate enum+eval (context.rs:9-120), matcher.rs push_keystroke/match_standard/match_custom/bindings_for_context (280-374), app/src/keyboard.rs:44, settings_view/keybindings.rs:397/592/600/754, util/bindings.rs:256. Hunted for the documented failure modes: (a) line drift — the only imprecision is the answer attributing push_keystroke's context_predicate.eval to lines 325-326 when the eval is actually at 327 (the Trigger::Keystrokes arm is 325); immaterial. bindings_for_context filter is at 285 not 284 (the method begins 282; closure 284-285). (b) overgeneralization — checked: the answer correctly scopes the enabled-filter to all four bindings() branches and confirms BOTH code and terminal editors use with_enabled (no code-vs-terminal divergence); update_custom_trigger genuinely covers editable_bindings too, so the unbind path is real. (c) app-vs-crates scope — claims correctly separated. (d) COUNTS — re-ran the grep: the answer's '~30 app/ sites' for enabled_predicate is materially wrong; actual is 93 with_enabled sites (83 closure-form). This is the one substantive correction; it is a supporting statistic, not part of the core thesis, so verdict is 'corrected' not 'refuted'. (e) graph-vs-code — graph covers only warpui_core/editor/vim; all app/ claims I verified directly against source, no contradiction. The 'no unified is_active' claim survives scrutiny: the is_active grep hits are exclusively windowing/fullscreen, not binding classification.

### A3-Q15 — CONFIRMED · confidence: high

**Q.** When a first-class Unbound replaces the overloaded Trigger::Empty, does the on-disk 'none' sentinel (keyboard.rs:15,44 to UserDefinedKeybinding::Removed) keep meaning 'unbind' and migrate silently vs via a schema bump — and since fixed bindings have no name, how does the file express disabling a nameless default key, especially given platform is baked at parse (keymap.rs:914-919) so a key-scoped disable is platform-specific?

**A.** The on-disk 'none' sentinel is a raw string keyed by editable-binding NAME, so it CAN keep meaning 'unbind' for free — only the in-memory mapping at keyboard.rs:44 would flip from Trigger::Empty to a hypothetical Unbound. But there is NO schema/version field in the file, so any reinterpretation is necessarily silent. Because the file is name-keyed and FixedBinding has no name field, the format physically cannot express disabling a nameless fixed default; a key-scoped disable would be a NEW concept that, given platform is baked at parse and normalized() emits resolved cmd-/ctrl-, would store a non-portable per-OS keystroke. Independently re-verified line-by-line — confirmed.

VERDICT: CONFIRMED. I opened every cited file (keyboard.rs, keymap.rs, matcher.rs, settings_view/keybindings.rs, editor/view/mod.rs, code/editor/view/actions.rs) plus locations the answer did not cite (Trigger enum keymap.rs:43-49, BindingId keymap.rs:264-272, grep for version/schema and BindingId in keyboard.rs) and re-ran all greps. Every evidence claim holds.\n\n(A) 'none' is name-scoped and token-stable. CustomKeybindings is HashMap<String, PersistedTrigger> (keyboard.rs:167-169), keyed by editable-binding name (doc :155-166). REMOVED_KEYBINDING_SERIALIZATION = \"none\" (keyboard.rs:15). From writes \"none\" for Removed (:182-184); TryFrom returns Removed iff string == \"none\" (:192-193). Load maps Removed to set_custom_trigger(name, Trigger::Empty) (:43-44). The Trigger enum (keymap.rs:43-49) currently has only Keystrokes/Standard/Custom/Empty — no Unbound — so the answer correctly frames Unbound as HYPOTHETICAL: replacing Empty with Unbound touches only the RHS at keyboard.rs:44, not the file token. So 'none' keeps meaning unbind with zero file changes. CONFIRMED.\n\n(B) Migration is silent by construction. grep for version/schema in keyboard.rs returns only two doc-comment hits about the 'normalized version' of keystrokes — no schema/version field exists. No versioning machinery gates a migration. CONFIRMED.\n\n(C) Fixed bindings have no name, so the name-keyed file cannot disable them. FixedBinding (keymap.rs:277-286) has fields trigger/action/command_description/context_predicate/enabled_predicate/group/id — NO name; only EditableBinding has name: &'static str (:294). Fixed bindings are identified by process-local BindingId (AtomicUsize, :264-272), never persisted (grep confirms no BindingId reference in keyboard.rs). FixedBinding::new's first arg is a KEYSTROKE str parsed by Keystroke::parse (:511-533, parse at :522). Settings UI lists editable bindings only — ctx.editable_bindings().collect() (keybindings.rs:754) — and unbinds by name: set_custom_trigger(row.binding.name, Trigger::Empty) (:592) plus write_custom_keybinding(name, Removed) (:600). Fixed defaults are neither shown nor disableable today. CONFIRMED.\n\n(D) Platform baked at parse → key-scoped disable is platform-specific. Keystroke::parse resolves cmdorctrl to cmd on Mac / ctrl elsewhere (keymap.rs:914-919); this runs from FixedBinding::new (:522) and the file loader (keyboard.rs:199). Persisted form is normalized() (keyboard.rs:180), which emits ctrl-/alt-/shift-/cmd-/meta- and NEVER re-emits cmdorctrl (keymap.rs:970-993). new_per_platform bakes platform at registration (:502-506; editor/view/mod.rs:212 is a concrete new_per_platform with mac:'cmd-enter'/linux_and_windows:'ctrl-shift-enter'). So a key-scoped disable storing a resolved keystroke is non-portable. CONFIRMED.\n\n(E) Reload distinguishes explicit unbind from untouched default — for NAMED editable bindings. load_custom_keybindings calls set_custom_trigger only for names present in the file (keyboard.rs:38-48); absent names keep their default. The matcher scans only Trigger::Keystrokes (matcher.rs:325, early-return :329-331), so an Empty/Unbound editable binding never matches. The answer correctly scopes this to named bindings. CONFIRMED.\n\nCOUNTS independently re-run: FixedBinding::new = 397; register_fixed_bindings = 150; register_editable_bindings = 123 — exact match. Per-file concentration: editor/view/mod.rs=49, terminal/view/init.rs=38, code/editor/view/actions.rs=38, notebooks/editor/view.rs=35 — exact match. editor/view/mod.rs:281 is verbatim FixedBinding::new(\"up\", EditorAction::Up, id!(\"EditorView\") & !id!(\"IMEOpen\")); code/editor/view/actions.rs:47 is id!(\"CodeEditorView\") & !id!(\"IMEOpen\"). All four heaviest files are user-facing editors. CONFIRMED.\n\nOnly cosmetic nit: the prose renders contexts as id!(EditorView)/id!(CodeEditorView) without the string-literal quotes the code uses (id!(\"EditorView\")); immaterial to every conclusion.

_Evidence:_
- On-disk unbind sentinel is the literal 'none', keyed by editable-binding name; CustomKeybindings is a bare HashMap with no version field — `app/src/keyboard.rs:15,167-169` _(code)_
- 'none' round-trips to UserDefinedKeybinding::Removed via raw string compare (From writes 'none', TryFrom returns Removed iff =='none'); token can stay unchanged across a hypothetical Unbound migration — `app/src/keyboard.rs:182-184,192-193` _(code)_
- Load maps Removed to set_custom_trigger(name, Trigger::Empty); replacing Empty with Unbound changes only this in-memory RHS, not the file token. Trigger enum currently has no Unbound (Keystrokes/Standard/Custom/Empty) — `app/src/keyboard.rs:43-44; crates/warpui_core/src/keymap.rs:43-49` _(code)_
- No version/schema field in keyboard.rs (only doc-comment 'normalized version' hits); no BindingId reference, confirming BindingId is never persisted — `app/src/keyboard.rs (grep version|schema|BindingId)` _(code)_
- FixedBinding struct has no name field (fields: trigger/action/command_description/context_predicate/enabled_predicate/group/id BindingId); EditableBinding has name: &'static str — `crates/warpui_core/src/keymap.rs:277-286,293-304` _(both)_
- BindingId is a process-local auto-increment AtomicUsize, never written to disk — `crates/warpui_core/src/keymap.rs:264-272` _(code)_
- FixedBinding::new first arg is a keystroke string parsed via Keystroke::parse, not a name — `crates/warpui_core/src/keymap.rs:511-533 (parse at :522)` _(code)_
- Platform baked at parse: cmdorctrl resolves to cmd on Mac / ctrl elsewhere at Keystroke::parse — `crates/warpui_core/src/keymap.rs:914-919` _(code)_
- Persisted form is normalized(), which emits resolved ctrl-/alt-/shift-/cmd-/meta- and never re-emits cmdorctrl; loader also parses via Keystroke::parse — `crates/warpui_core/src/keymap.rs:970-993; app/src/keyboard.rs:180,199` _(code)_
- new_per_platform bakes platform at registration (mac vs linux_and_windows); concrete site is a new_per_platform mac:'cmd-enter'/linux_and_windows:'ctrl-shift-enter' — `crates/warpui_core/src/keymap.rs:502-506; app/src/editor/view/mod.rs:212-215` _(code)_
- Settings UI lists editable bindings only (editable_bindings().collect()) and unbinds by name (set_custom_trigger + write_custom_keybinding Removed); fixed defaults are neither shown nor disableable — `app/src/settings_view/keybindings.rs:592,600,754` _(code)_
- Reload distinguishes explicit unbind (present 'none') from untouched default (absent name): set_custom_trigger only called for names in file — `app/src/keyboard.rs:38-48` _(code)_
- Matcher scans only Trigger::Keystrokes (early-return at :329-331), so an Empty/Unbound editable binding never matches — `crates/warpui_core/src/keymap/matcher.rs:324-331` _(code)_
- 397 FixedBinding::new sites; 150 register_fixed_bindings; 123 register_editable_bindings. Heaviest under user-facing editors: editor/view/mod.rs=49, terminal/view/init.rs=38, code/editor/view/actions.rs=38 (under id!("CodeEditorView") at :47), notebooks=35 — `app/src/editor/view/mod.rs:281; app/src/code/editor/view/actions.rs:47` _(code)_

- _Unblocks:_ Lets the design fix the back-compat/migration story for unbind: keep 'none' as a name-scoped unbind that maps to a first-class Unbound with no file change and no schema bump (silent, safe for existing files), and treat disabling nameless fixed defaults as a separate NEW schema feature — explicitly acknowledging it needs either a non-portable per-OS keystroke key or a platform-neutral default identity (R5), and that today's settings UI/file cannot even address fixed bindings.
- _Residual / needs decision:_ Pure design decisions not settleable from code: (1) keep 'none' as name-scoped unbind for back-compat vs reinterpret it (code shows the token can be preserved for free, but with no versioning any change is silent); (2) whether a key-scoped disable for nameless fixed defaults stores a resolved per-OS keystroke (non-portable, per D) or a platform-neutral default identity (which needs a platform-in-data-model change, R5); (3) whether to add a schema-version field at all (none today). Also, an exact classification of how many of the 397 FixedBinding sites sit under user-facing contexts would require evaluating every ContextPredicate; I confirmed the top ~160 (the four editor files) are user-facing keystroke defaults but did not exhaustively classify all 397 — note actions.rs uses both text_entry (CodeEditorView) and a narrower editable_state predicate, so not all 38 are under the identical context, though all are user-facing editor keys.
- _Verification:_ I attacked every line ref and count rather than trusting them. (1) Re-ran all three greps myself: 397/150/123 — exact match to the answer. (2) Re-ran per-file concentration: 49/38/38/35 for the four editor files — exact match. (3) Opened keyboard.rs fully: :15, :167-169, :180-184, :192-193, :43-48, :199 all say exactly what is claimed. (4) Opened keymap.rs: FixedBinding (:277-286) genuinely has no name field; EditableBinding (:294) has name; new (:511-533) takes a keystroke str; cmdorctrl resolution (:914-919) and normalized() (:970-993) are verbatim as described; new_per_platform (:502-506) confirmed. (5) Verified the Trigger enum (:43-49) has NO Unbound today — the answer correctly frames Unbound as a hypothetical replacement, so it is not a false 'this exists' claim. (6) Verified matcher.rs:325 scans only Trigger::Keystrokes with early-return at :329-331. (7) settings keybindings.rs:592/600/754 confirmed (editable-only, name-scoped unbind). (8) Checked the two locations the answer did NOT explicitly cite for these sub-claims: grep for version/schema in keyboard.rs (none, only doc comments) and grep for BindingId in keyboard.rs (none) — both support claims (B) and the 'BindingId never persisted' point. (9) Confirmed app/-vs-crates scope is correct: keyboard.rs and settings_view in app/, keymap.rs/matcher.rs in crates/warpui_core. (10) Checked for overgeneralization: the answer is careful to scope reload-distinguishability and unbind to NAMED editable bindings and explicitly states fixed defaults cannot be disabled — no 'all bindings' overreach. Only defect found is cosmetic: prose drops the string quotes in id!(\"EditorView\")/id!(\"CodeEditorView\"); the actual code uses quoted string literals. Immaterial.

### A3-Q16 — CONFIRMED · confidence: high

**Q.** What is the golden-test harness for the new resolver, given matcher_tests.rs (251 lines) has exactly three tests (test_matcher, test_editable_binding_matching, test_bindings_for_context) and ZERO coverage of shadowing, chord-prefix, precedence ordering, or unbind?

**A.** There is no golden/table-driven/snapshot harness today. matcher_tests.rs is exactly 251 lines with exactly 3 imperative tests built on a test_keystroke(&str,view,&ctx)->Option<Action> helper, and a keyword scan confirms zero coverage of shadowing, chord-prefix conflict, editable-vs-fixed precedence, or unbind. There isn't even an Unbound outcome to assert against: MatchResult has only None|Pending|Action. The harness must be built from scratch; the two verified bugs are currently unpinned.

CODE FACT (decisive, independently verified): No resolver golden-test harness exists. crates/warpui_core/src/keymap/matcher_tests.rs is exactly 251 lines (wc -l) and contains exactly three #[test] fns — test_matcher (:5), test_editable_binding_matching (:89), test_bindings_for_context (:179) — plus one non-test helper fn test_keystroke (impl Matcher, :230-241, no #[test] attr) that returns Option<Arc<dyn Action>> and collapses both MatchResult::None and MatchResult::Pending to None (the `_ => None` arm at :238). A grep of that file for shadow|precedence|unbind|unbound|disable|reset_to_default|custom_trigger returns NOTHING (exit 1).

WHY THE CONFLICT CASES ARE UNCOVERED:
1) short-shadows-chord (matcher.rs:329-331 early return): In test_matcher and test_editable_binding_matching the chord "a b" is only resolved in ctx_b, where the short "a" binding's context predicate id!("a") is FALSE (ctx_b holds only "b"), so the short binding is never an eligible competitor at the instant the chord fires — the chord's own predicate id!("a")|id!("b") is what evaluates true. The same-context case where a complete short binding shadows a prefix-sharing chord is never asserted.
2) editable-always-beats-fixed (keymap.rs:454-464): bindings() yields editable bindings first (LIFO via editable_bindings() at :441-447), then fixed in reverse, and push_keystroke (matcher.rs:324-338) linear-scans and returns the FIRST exact match (:329-331) — so an editable beats a same-key fixed. But test_matcher is fixed-only (FixedBinding::new, :13-17) and test_editable_binding_matching is editable-only (register_editable_bindings, :99-109); neither ever puts an editable and a fixed binding on the same trigger+context, so this precedence is never exercised through the resolver.
3) Unbind/Unbound: the concept does not exist in code. MatchResult (matcher.rs:49-53) is exactly {None, Pending, Action}; grep unbound|unbind across the entire keymap module is empty (exit 1). A broad warpui_core scan only surfaces unrelated UI DropShadow / layout-"unbounded" tokens. The "new Unbound outcome" the question proposes is genuinely new — there is no fixture, no variant, and no resolver path for it.

THE THREE RESOLVER PATHS to be covered (matcher.rs:324-346): exact-match -> Action (:329-331); longer-candidate present -> retain_pending -> Pending (:341); no candidate -> None (:344). The 3 existing tests are imperative keystroke sequences (basic match, multi-keystroke, failed-match isolation, context-change clearing, per-view pending) — they touch these paths only in non-conflicting contexts.

PARTIAL COVERAGE EXISTS, but only at the LIST level in a different file (keymap_tests.rs), never through the resolver: test_keymap_bindings_list (:123-238) pins bindings() iteration order (editable LIFO then fixed reverse: Idle,Close,Open,Paste,Copy) but with NO shared triggers (cmd-o/cmd-w/cmd-c/cmd-v/Empty, all distinct) and via map.bindings() not push_keystroke; test_disabled_bindings (:344-403) tests enabled-filter via with_enabled()/map.bindings() (enable/disable, NOT user-unbind); test_custom_triggers (:253) tests editable trigger override via update_custom_trigger. NONE of the keymap_tests.rs tests call push_keystroke. So even the broader suite has no shadow / chord-prefix / same-key precedence / Unbound coverage.

PRIMITIVES AVAILABLE TO BUILD THE TABLE-DRIVEN HARNESS: Matcher::test_keystroke (matcher_tests.rs:230-241) gives string-keystroke -> Option<Action>; the id!()/context macros (keymap::macros) build active contexts; Keymap::new(vec![FixedBinding...]) and register_editable_bindings seed mixed layers; get_bindings() (matcher.rs:303) and bindings_for_context (used in test_bindings_for_context, :200-226) enumerate winners for the A3-Q17 startup snapshot; the #[cfg(debug_assertions)] validate_bindings scaffolding (matcher.rs:181-203) is also present. A new suite would extend test_keystroke to also surface Pending/None (and a future Unbound) so (layers, context, keystroke-seq) -> expected MatchResult can be table-asserted, seeded with the two verified-bug fixtures.

SCOPE NOTE: The "what should the harness be" is a design choice (table schema, fixture set, snapshot vs unit) — see residual_unknowns. The code-verifiable answer is that no such harness exists yet and exactly which gaps/primitives are present.

_Evidence:_
- matcher_tests.rs is exactly 251 lines with exactly 3 #[test] fns (test_matcher, test_editable_binding_matching, test_bindings_for_context) plus a non-test test_keystroke helper that collapses Pending+None to None — `crates/warpui_core/src/keymap/matcher_tests.rs:1-251 (tests at :5,:89,:179; helper at :230-241, `_ => None` at :238)` _(code)_
- Keyword scan of matcher_tests.rs for shadow|precedence|unbind|unbound|disable|reset_to_default|custom_trigger returns NONE (grep exit 1) — `crates/warpui_core/src/keymap/matcher_tests.rs (grep -niE)` _(code)_
- No Unbound outcome exists: MatchResult has exactly None|Pending|Action, and grep unbound|unbind across the keymap module is empty (exit 1) — `crates/warpui_core/src/keymap/matcher.rs:49-53` _(both)_
- push_keystroke early-returns on the first exact match (len-equal :329-331) and only retains Pending for longer candidates (:332-335 -> :341); no-candidate -> None (:344); this is the short-shadows-chord path — `crates/warpui_core/src/keymap/matcher.rs:324-346` _(code)_
- bindings() yields editable bindings first (LIFO via editable_bindings() :441-447) then fixed in reverse, so editable always beats a same-key fixed via the first-match early return — `crates/warpui_core/src/keymap.rs:441-447,454-464` _(both)_
- In the two resolver tests the chord 'a b' is only resolved in ctx_b where the short 'a' binding's predicate id!("a") is false (ctx_b holds only "b"), so the same-context shadow conflict is never asserted — `crates/warpui_core/src/keymap/matcher_tests.rs:13-17,38-46,99-109,130-138` _(code)_
- test_matcher is fixed-only (FixedBinding::new) and test_editable_binding_matching is editable-only (register_editable_bindings); neither mixes an editable+fixed binding on one trigger+context, so editable-vs-fixed precedence is never exercised through the resolver — `crates/warpui_core/src/keymap/matcher_tests.rs:13-17 vs :99-109` _(code)_
- Precedence/disable/custom-trigger are tested only at the list level in keymap_tests.rs (iteration order, enabled-filter, trigger override) via map.bindings(); none call push_keystroke and none cover conflict/shadow/Unbound — `crates/warpui_core/src/keymap_tests.rs:123-238 (test_keymap_bindings_list, distinct triggers), :344-403 (test_disabled_bindings, with_enabled filter), :253-281 (test_custom_triggers, update_custom_trigger)` _(code)_
- Harness primitives that exist: test_keystroke helper, get_bindings enumerator, bindings_for_context (used by an existing test), and validate_bindings scaffolding — `crates/warpui_core/src/keymap/matcher_tests.rs:230-241; crates/warpui_core/src/keymap/matcher.rs:303; matcher_tests.rs:200-226; matcher.rs:181-203` _(code)_

- _Unblocks:_ Lets the design doc state, with evidence, that the precedence/Unbound/chord rewrite currently ships on faith: it must land a table-driven golden suite (seeded with the verified short-shadows-chord and editable-vs-fixed fixtures and covering all three MatchResult paths plus a new Unbound) before touching push_keystroke/bindings(), and it can reuse the existing test_keystroke helper, context macros, and get_bindings enumerator as the harness foundation.
- _Residual / needs decision:_ The concrete shape of the NEW harness is a design decision, not a code fact: (a) table schema for (keymap layers, active context, keystroke sequence) -> expected MatchResult; (b) whether to add a MatchResult::Unbound variant so unbind can be asserted (none exists today); (c) whether to extend test_keystroke to surface Pending/None distinctly (it currently collapses both to None at :238); (d) unit-fixture suite vs the full-keymap startup snapshot of A3-Q17 (which would reuse get_bindings()/bindings_for_context and the #[cfg(debug_assertions)] validate_bindings scaffolding at matcher.rs:181-203, which I confirmed exists); (e) exact seed fixtures pinning the current short-shadows-chord and editable-beats-fixed behaviors as intentional-change baselines. These are author/product/test-strategy choices the design doc must settle. Not verified: the app/-side audit of register_fixed_bindings vs register_editable_bindings under user-facing contexts (out of scope of the test-harness question; graph covers only warpui_core/editor/vim).
- _Verification:_ Independently opened all cited files and one uncited location (matcher.rs:181-203 validate_bindings). Verified: (1) wc -l = 251 exactly; grep of #[test]/fn = exactly 3 tests at :5,:89,:179 plus test_keystroke helper at :230 (no #[test] attribute, lives in impl Matcher, returns Option<Arc<dyn Action>>, `_ => None` at :238 collapses both Pending and None). (2) Keyword scan returns nothing (exit 1) — re-ran myself. (3) MatchResult at :49-53 is exactly None/Pending/Action; grep unbound|unbind across keymap.rs + keymap/ = empty (exit 1); broad warpui_core scan only hits unrelated UI DropShadow/layout-'unbounded'. (4) push_keystroke logic at :307-346 matches the description precisely (early return :329-331, retain_pending :333-334 -> Pending :341, None :344). (5) bindings() order at keymap.rs:454-464 with editable_bindings LIFO at :441-447 confirmed. (6) Read both resolver tests in full: chord/context claim is exactly right — short binding predicate id!(\"a\") is false in ctx_b for both the fixed test (:14-16) and editable test (:100-108); test_matcher is FixedBinding-only, the editable test is register_editable_bindings-only; neither mixes layers on one trigger. (7) keymap_tests.rs: grep confirmed NO push_keystroke calls; test_keymap_bindings_list uses distinct triggers and map.bindings(); test_disabled_bindings uses with_enabled filter; test_custom_triggers uses update_custom_trigger. (8) Confirmed no other matcher test files exist. Only nits, both immaterial and within cited ranges: test_disabled_bindings #[test] is at :343 / fn at :344 (answer cited :343-403); test_custom_triggers #[test] at :252 / fn at :253 (answer cited :253). All counts exact, all line refs accurate, no overgeneralization, no app/-vs-crates confusion. Every evidence claim holds.

### A3-Q17 — CORRECTED · confidence: high

**Q.** Beyond synthetic unit fixtures (Q12), is there a full-keymap resolution snapshot — captured at startup over the real ~265 registration sites as (context, keystroke)→action — that can be diffed before/after the refactor to prove no real binding changed winner unintentionally?

**A.** No. There is no full-keymap resolution snapshot anywhere in the codebase — nothing captures a (context, keystroke)→winning-action table at startup that could be diffed before/after the refactor. The only startup-time scaffolding over the real bindings is the debug-only validate_bindings() pass, which runs PER-BINDING yes/no validity predicates and panics on failure. (Correction to the original answer: it is NOT "just is_binding_cross_platform" — validate_bindings also runs context-registered is_binding_pty_compliant validators for RootView, PaneGroup, TerminalView and Workspace via register_binding_validator. Both kinds are validity predicates, not winner-resolution, so the conclusion stands and is reinforced.) It never resolves a winner per (context, keystroke) and emits no diffable table. The building blocks (get_bindings(), bindings_for_context(), the startup validate_bindings hook) exist; the snapshot harness itself does not.

CODE FACT — there is no characterization/golden snapshot of full-keymap resolution. Verdict: corrected (central conclusion fully confirmed; one supporting detail about the validator inventory fixed, plus a minor file-count nuance).

1) No snapshot infrastructure or test exists. Grepping snapshot/golden/characterization across app/ and crates/ yields nothing tied to keybinding resolution. The lone 'snapshot' hit (app/src/terminal/view/init.rs:1047 "Write current codebase index snapshot") is an unrelated codebase-index action description. `insta` is NOT a dependency — the only matches are the time crate `instant` (e.g. app/Cargo.toml:135) and `app-installation-detection`; the original answer correctly distinguished these. matcher_tests.rs is 251 lines with exactly three tests (test_matcher:5, test_editable_binding_matching:89, test_bindings_for_context:179) plus a test_keystroke helper (:230) — none enumerate contexts or assert a (context,keystroke)→action table.

2) The closest "over all real bindings at startup" mechanism is validate_bindings(), and it is NOT a resolution snapshot:
   - crates/warpui_core/src/keymap/matcher.rs:181-203 (validate_bindings, #[cfg(debug_assertions)]) iterates bindings_for_context(context) for EACH registered (context, validator) pair (matcher.rs:184) and get_bindings() for the default validator, pushing any binding for which the validator returns IsBindingValid::No and panicking if any fail.
   - It is wired to run once after all registrations load: crates/warpui_core/src/platform/app.rs:85 calls ctx.validate_bindings() right after init; the App wrapper is crates/warpui_core/src/core/app.rs:1609-1611 (debug) / :1615-1616 (release no-op).
   - CORRECTION to the original answer's "today just is_binding_cross_platform": there are actually TWO families of registered validators. (a) The single DEFAULT validator is is_binding_cross_platform (app/src/lib.rs:1395 → app/src/util/bindings.rs:918-933), asserting non-mac bindings don't use `cmd`. (b) FOUR context-scoped validators registered via register_binding_validator with is_binding_pty_compliant: app/src/root_view.rs:262 (RootView), app/src/pane_group/mod.rs:318 (PaneGroup), app/src/terminal/view/init.rs:83 (TerminalView), app/src/workspace/mod.rs:82 (Workspace). is_binding_pty_compliant (bindings.rs ~:910-913) is also a per-binding validity predicate (PTY-compliance of keystroke/action). Neither family picks a first-match per keystroke, enumerates the keystroke space, nor produces a stable serializable output to diff. So the additional validators do NOT change the conclusion — they reinforce it (all startup scaffolding is per-binding validity, none is resolution).

3) The resolution machinery a snapshot would have to capture (and which the refactor touches):
   - Winner ordering: keymap.rs:454-464 bindings() = editable bindings first (reverse-registration LIFO via editable_bindings() at :441-447, which calls .iter().rev()) then fixed bindings (.iter().rev() at :458-462). "Winner" = the FIRST match in this iteration.
   - Consumers that depend on first-match: push_keystroke (matcher.rs:307-346; early exact-match return at :329-331), match_standard (:350-358), match_custom (:363-369), bindings_for_context (:282-286), get_bindings (:303-305). A (layer, recency) reorder changes the head of this iterator, so any winner-flip is exactly what a snapshot would catch — and nothing asserts it today.

4) Scale of what's uncovered (counts re-run and confirmed): 266 register_* calls (146 register_fixed_bindings + 120 register_editable_bindings) plus 397 FixedBinding::new objects feed bindings(). 242 of 266 register_* calls live under app/ (fixed app/=132 crates/=14; editable app/=110 crates/=10), with 98 distinct files under app/. (Minor count nuance: the original answer's "108 files / 98 of 108 under app/" is slightly soft — re-running gives 98 app/ files and ~10-12 crates/ files depending on whether the wrapper/definition files in core/app.rs, matcher.rs and keymap.rs are counted; total ~110, vs the doc's stated "~265 sites / 109 files." Immaterial to the conclusion.) Because app/ holds the bulk of registrations and the knowledge graph is BLIND to app/, any snapshot built only from crates/warpui_core or from the graph would miss most production bindings; a real snapshot must boot/assemble the full Matcher after app-level registration.

5) Feasibility (for the design doc): a snapshot harness is buildable from existing parts but is net-new work. bindings_for_context(Context) (matcher.rs:282) returns bindings in precedence order filtered by a concrete Context, so per representative context you can take the first binding per distinct trigger to get the winner, then serialize a sorted (context, trigger)→action table and diff master vs refactor. Two gaps confirm it doesn't exist yet: (a) no enumeration of representative Contexts (predicates evaluate against concrete Context values that must be hand-constructed); (b) no keystroke-space enumeration nor stable Action serialization, so "winner per keystroke" reduces to "winner per registered trigger." The validate_bindings startup hook (matcher.rs:181-203 / platform/app.rs:85) is the natural anchor for such a dump.

Bottom line: the regression net the question asks about is absent; only per-binding validity checks exist (cross-platform + PTY-compliance). Whether/how to build the snapshot is a design choice.

_Evidence:_
- No keymap resolution snapshot/golden/characterization test or snapshot infra exists; the one 'snapshot' hit is an unrelated codebase-index action label; insta is not a dependency (only the time crate 'instant' and 'app-installation-detection' match). — `grep -rniE 'snapshot|golden|characterization' app/ crates/ → only app/src/terminal/view/init.rs:1047 (codebase index); insta absent from all Cargo.toml; app/Cargo.toml:135 instant` _(code)_
- matcher_tests.rs is 251 lines with exactly three tests (no context/keystroke table, no snapshot) plus a test_keystroke helper. — `crates/warpui_core/src/keymap/matcher_tests.rs:5,89,179,230 (251 lines)` _(code)_
- validate_bindings is a debug-only per-binding yes/no validity pass: it loops (context,validator) pairs over bindings_for_context and the default validator over get_bindings, panicking on failures — not a winner-resolution snapshot. — `crates/warpui_core/src/keymap/matcher.rs:181-203 (loop at :184-190)` _(code)_
- validate_bindings runs once at startup after all registrations are loaded (the natural snapshot hook point); App wrapper is debug-only with a release no-op. — `crates/warpui_core/src/platform/app.rs:85; crates/warpui_core/src/core/app.rs:1609-1611,1615-1616` _(code)_
- CORRECTION: validate_bindings runs MORE than one validator. The only DEFAULT validator is is_binding_cross_platform (non-mac cmd check), but four context-scoped is_binding_pty_compliant validators are also registered. Both are per-binding validity predicates, not resolution comparisons. — `default: app/src/lib.rs:1395, app/src/util/bindings.rs:918-933; context validators: app/src/root_view.rs:262, app/src/pane_group/mod.rs:318, app/src/terminal/view/init.rs:83, app/src/workspace/mod.rs:82 (is_binding_pty_compliant, bindings.rs:910-913)` _(code)_
- Winner ordering (what a snapshot must pin) = editable LIFO (.iter().rev()) then fixed reverse-registration (.iter().rev()), first-match wins; this is exactly what the refactor reorders. — `crates/warpui_core/src/keymap.rs:441-447,454-464` _(both)_
- Resolution consumers depend on first-match in bindings(): push_keystroke early-return at :329-331, match_standard, match_custom. — `crates/warpui_core/src/keymap/matcher.rs:307-346 (esp. 329-331),350-358,363-369` _(code)_
- bindings_for_context returns precedence-ordered bindings for a concrete Context (reusable to compute a per-context winner table); get_bindings is the unfiltered full ordering. — `crates/warpui_core/src/keymap/matcher.rs:282-286,303-305` _(code)_
- 266 registration calls (146 register_fixed_bindings + 120 register_editable_bindings) feed bindings(); 397 FixedBinding::new objects — consistent with the doc's ~265 sites. — `grep -rn over app/ crates/ (excluding defs): fixed=146, editable=120, FixedBinding::new=397` _(code)_
- 242 of 266 register_* calls live under app/ (fixed app/=132 crates/=14; editable app/=110 crates/=10), across 98 app/ files; the graph is blind to app/, so a graph-only or crates-only snapshot would miss most production bindings. (File-count total ~110, vs the answer's stated 108 and the doc's 109 — immaterial to the conclusion.) — `grep -rn / grep -rln breakdown over app/ and crates/` _(code)_

- _Unblocks:_ Confirms Axis-3's regression-net design must include building a net-new characterization snapshot harness (anchored on the startup validate_bindings hook, reusing bindings_for_context over hand-built representative contexts) rather than relying on anything that exists today — and that it must boot the full app-level keymap (242/266 sites live in app/, invisible to crates-only tests and the knowledge graph) to actually catch winner-flips from the fixed-vs-editable-LIFO → (layer, recency) reorder.
- _Residual / needs decision:_ Whether to build the snapshot and its shape remain design/product decisions, not code facts: (a) which representative Contexts to enumerate (CodeEditorView, terminal EditorView, each Vim/modal mode) — no canonical context list exists; (b) how to reduce (context,keystroke)→action to a stable diffable key (Actions are Arc<dyn Action> with no guaranteed stable identity/serialization today — would need a name/debug projection); (c) per-trigger winners via bindings_for_context vs a true keystroke-space walk including chord/prefix and push_keystroke early-return semantics (matcher.rs:329-331); (d) whether to gate it behind the existing #[cfg(debug_assertions)] validate_bindings hook or a dedicated test binary. Not decidable from code alone. Also unverified at runtime: whether all 4 is_binding_pty_compliant registrations and the default validator are always active in a given build/feature-flag configuration (read statically, not executed).
- _Verification:_ Independently opened every cited file:line. CONFIRMED exact: matcher.rs:181-203 (validate_bindings), :282-286 (bindings_for_context), :303-305 (get_bindings), :307-346 with early-return :329-331 (push_keystroke), :350-358 (match_standard), :363-369 (match_custom); keymap.rs:441-447 (editable .iter().rev() LIFO) and :454-464 (bindings() = editable then fixed .iter().rev()); platform/app.rs:85 (validate_bindings call); core/app.rs:1609-1611/1615-1616 (debug/release wrapper); lib.rs:1395 (set_default_binding_validator(is_binding_cross_platform)); bindings.rs:918-933 (is_binding_cross_platform). matcher_tests.rs = 251 lines, three tests at 5/89/179 + test_keystroke at 230 — confirmed. Re-ran all grep counts myself: 146 fixed + 120 editable = 266 calls, 397 FixedBinding::new, app/=242 (132+110), crates/=24 (14+10), 98 app/ files — all match. ONE FIX (reason for 'corrected' not 'confirmed'): I checked a location the answer did not cite — grep for register_binding_validator — and found FOUR additional context-scoped validators (is_binding_pty_compliant for RootView/PaneGroup/TerminalView/Workspace at root_view.rs:262, pane_group/mod.rs:318, terminal/view/init.rs:83, workspace/mod.rs:82). So the short_answer's 'today just is_binding_cross_platform' understates the validator inventory; validate_bindings runs is_binding_cross_platform (default) PLUS is_binding_pty_compliant (4 contexts). This does NOT weaken the answer: both are per-binding validity predicates, not winner-resolution, so the central claim (no full-keymap resolution snapshot exists) is fully confirmed and reinforced. Also noted a minor, immaterial file-count wobble (108 in the prose vs ~110 measured vs the doc's 109). The insta-vs-instant distinction the answer made is correct. The lone 'snapshot' grep hit is an unrelated codebase-index label. No evidence claim was materially wrong; the resolution machinery and first-match-wins ordering are exactly as described.

---

