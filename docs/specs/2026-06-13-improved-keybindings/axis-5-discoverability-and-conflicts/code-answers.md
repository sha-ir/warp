[← Back to index](../README.md)

## Axis 5 — Discoverability & truthful conflicts

### A5-Q2 — CONFIRMED · confidence: high

**Q.** scope

**A.** priv

CONFIRMED. Making a binding's scope visible/conflict-keyable is necessarily an engine change, not a UI-only or group-proxy change. Every premise in the answer is true in source: (1) EditableBindingLens.context is private at keymap.rs:311 (no pub keyword) while siblings name, description, action, trigger, original_trigger, group, id are pub; the backing EditableBinding.context_predicate (keymap.rs:297) and Lens.enabled (keymap.rs:312) are also private. (2) The only scope-shaped public API on the lens is in_context(&Context)->bool, a boolean point-eval via self.context.eval at keymap.rs:787-789; the predicate itself is never handed out (the only methods passing context — as_binding at keymap.rs:766 and as_lens at keymap.rs:741 — are private). (3) CommandBinding (app/src/util/bindings.rs:531-541) has fields name, description, trigger, action, group:Option<BindingGroup>, id — no scope/context field. (4) from_editable_lens (bindings.rs:762-771) copies description, trigger, action, name, group, id and never references lens.context; from_lens (bindings.rs:749-758) likewise discards it. (5) BindingGroup (bindings.rs:797-810) is exactly 12 SEMANTIC buckets (Settings, Close, Navigation, WarpAi, Workflow, Notebooks, Folders, KeyboardShortcuts, AutoUpdate, Notifications, EnvVarCollection, Terminal), and the lens group is an optional &'static str (keymap.rs:316) so many bindings are None and code-editor vs vim-normal are not separated by group — so group cannot key per-context collisions. (6) The conflict map confirms scope is absent end-to-end: ConflictMap is HashMap<Keystroke,usize> (keybindings.rs:104-107), populated from binding.trigger.clone() only (keybindings.rs:795-800), built from the editable list produced by from_editable_lens via ctx.editable_bindings() (keybindings.rs:754-758); filter_bindings_including_keystroke reads only binding.trigger and binding.description.in_context — NOT binding.name (bindings.rs:625-687). (7) ContextPredicate (keymap/context.rs:9-18) is an arbitrary 7-variant boolean tree (Identifier, Equal, NotEqual, Not, And, Or, Just) with no enumerable scope id. The editor distinction is real: app/src/editor (terminal) and app/src/code/editor both exist and differ only as different ContextPredicates on their bindings — exactly the scope being discarded. Additional independent check the answer did not cite: the sibling BindingLens.context_predicate (keymap.rs:246) is ALSO private, so there is no alternate public scope path on either lens — this strengthens, not weakens, the conclusion that Option A (thread real scope) requires an engine change (make context pub/add accessor at keymap.rs:311, add CommandBinding.scope, populate in from_editable_lens) and Option B (group proxy) is provably wrong. So context-aware conflicts (R6) and the catalog scope column (R9) are an engine refactor.

_Evidence:_
- EditableBindingLens.context is private (no pub) while sibling fields name/description/action/trigger/original_trigger/group/id are pub — `crates/warpui_core/src/keymap.rs:311` _(code)_
- Lens exposes scope only as in_context(&Context)->bool point-eval via self.context.eval, not as a predicate or scope id — `crates/warpui_core/src/keymap.rs:787` _(code)_
- CommandBinding has fields name/description/trigger/action/group/id and no scope/context field — `app/src/util/bindings.rs:531` _(code)_
- from_editable_lens copies description/trigger/action/name/group/id and never reads lens.context — `app/src/util/bindings.rs:762` _(code)_
- BindingGroup is exactly 12 semantic categories (Settings..Terminal), not context scopes — `app/src/util/bindings.rs:797` _(code)_
- ConflictMap is HashMap<Keystroke,usize> populated from binding.trigger only, built from from_editable_lens output — `app/src/settings_view/keybindings.rs:104` _(code)_
- Conflict map populated from binding.trigger.clone() with no context — `app/src/settings_view/keybindings.rs:795` _(code)_
- filter_bindings_including_keystroke matches description and trigger only, does not read binding.name — `app/src/util/bindings.rs:625` _(code)_
- ContextPredicate is an arbitrary 7-variant boolean tree with no enumerable scope id — `crates/warpui_core/src/keymap/context.rs:9` _(code)_
- Sibling BindingLens.context_predicate is also private, closing any alternate public scope path; from_lens discards it — `crates/warpui_core/src/keymap.rs:246` _(code)_

- _Unblocks:_ eng
- _Residual / needs decision:_ Which option to adopt (expose ContextPredicate raw and reduce to a comparable scope id — needing an overlap primitive per A5-Q5; vs expose a derived opaque scope id; vs rejected group proxy) is a non-code engine-ownership decision. Unaudited at runtime: how many distinct ContextPredicates exist across editable_bindings() and whether two real scopes share a predicate spelling (needs a startup walk). Fixed/custom-action binding context plumbing is A5-Q6. Note: BindingLens (non-editable path) also carries context_predicate but it is private and from_lens discards it; the settings page uses the editable path regardless, so this does not open a UI-only path.
- _Verification:_ Opened all cited files directly. keymap.rs:307-318 — context at 311 has no pub; name/description/action/trigger/original_trigger/group/id are pub; enabled (312) and backing EditableBinding.context_predicate (297) are private. keymap.rs:787-789 — in_context returns bool via self.context.eval; grep of the EditableBindingLens impl confirms in_context is the only context-touching public method (as_lens/as_binding are private). bindings.rs:531-541 — CommandBinding has exactly name/description/trigger/action/group:Option<BindingGroup>/id, no scope. bindings.rs:749-771 — both from_lens and from_editable_lens omit any context read. bindings.rs:797-810 — counted exactly 12 BindingGroup variants matching the listed names. bindings.rs:625-687 — filter_bindings reads binding.trigger (638) and binding.description.in_context (651) only; the anchor's hint "does it read name?" answered NO, consistent with the answer's "description text and trigger only". keybindings.rs:104-107 ConflictMap=HashMap<Keystroke,usize>; :795-800 populated from binding.trigger.clone(); :754-758 built from ctx.editable_bindings()+from_editable_lens. context.rs:9-18 — exactly 7 ContextPredicate variants. Verified both editor dirs exist (app/src/editor, app/src/code/editor). Checked a NON-cited location: BindingLens struct (keymap.rs:242-255) — its context_predicate (246) is also private, so no alternate public scope path exists on either lens; this reinforces the answer. No shifted line refs, no off counts, no app/-vs-crates confusion, no overgeneralization (answer correctly scopes to the editable-bindings settings path and notes editor distinction). Every grep re-run matched.

### A5-Q3 — CORRECTED · confidence: high

**Q.** How do read-only FIXED bindings get a stable, persistable catalog id? They are enumerable via get_key_bindings(), but FixedBinding::as_lens hardcodes name: Default::default() and the only identifier is a process-local atomic BindingId assigned in registration order. Do we add a name/stable-id field to FixedBinding (~146 register sites) or synthesize one (e.g. from group+trigger+description)?

**A.** Confirmed core thesis with one count correction. Fixed bindings get NO stable, persistable catalog id today: FixedBinding has no name field, FixedBinding::as_lens hardcodes name: Default::default() (empty &str, keymap.rs:633), and the only id is BindingId(pub usize) minted from a process-local atomic counter (keymap.rs:262-272) that derives no Serialize/Deserialize and is never persisted. The entire edit/persist/editor layer is name-keyed (editable_bindings() only; set_custom_keybinding/update_custom_trigger match by name; UI guards !name.is_empty()), so an empty-name fixed binding cannot be anchored, and from_lens additionally drops every description-less fixed binding. CORRECTION: the constructor-site edit surface is 469, not 456 — the answer omitted FixedBinding::new_per_platform (13 sites, delegates to ::new so also description-less), so ~404 (not 391) fixed bindings would be dropped by from_lens; the 65 survivors (44 custom + 21 empty), the 93-file count, and the 146 register-call count are unchanged. Synthesis from current fields (group=1-of-12, platform/custom-mutated trigger, absent description, build-unstable type_name) is not viable.

VERDICT: corrected. The answer's substance is fully verified by direct reading of the cited files; I fix one undercount that enlarges (not contradicts) the blast radius.

WHAT I VERIFIED (all true):
1) FixedBinding has no name field — fields are trigger, action, command_description: Option<BindingDescription>, context_predicate, enabled_predicate: Option<EnabledPredicate>, group: Option<&'static str>, id: BindingId (keymap.rs:277-286). Constructors new(:511), empty(:536), standard(:553), custom(:573) take no name; all set group:None; new+standard set command_description:None, empty+custom set Some. No with_name builder.
2) as_lens hardcodes name: Default::default() (keymap.rs:633, inside 631-642); BindingLens.name is &str (keymap.rs:243), so Default is the empty string.
3) BindingId(pub usize) (keymap.rs:262) is minted from static NEXT_BINDING_ID.fetch_add (keymap.rs:264-272) in construction order; derive at :261 is Copy/Clone/Debug/Hash/PartialEq/Eq/PartialOrd/Ord — NO Serialize/Deserialize/FromStr. The Serialize import in this file is consumed by Keystroke at :320, not BindingId. Order-assigned => not stable across builds; not persisted anywhere.
4) Enumeration path confirmed: get_key_bindings (crates/warpui_core/src/core/app.rs:1699 — evidence item 6's path is correct) -> KeyMatcher::get_bindings (matcher.rs:303) -> Keymap::bindings (keymap.rs:454-462) which yields editable lenses first, then fixed_bindings.iter().rev().filter(is_enabled).map(FixedBinding::as_lens). (Answer's snippet omitted the harmless .filter(is_enabled); immaterial.)
5) Name-keyed edit/persist model: editor list built only from ctx.editable_bindings() (keybindings.rs:754) — fixed bindings never enter. set_custom_keybinding -> ctx.set_custom_trigger(name) + write_custom_keybinding(name) (bindings.rs:491-505). update_custom_trigger filters editable_custom_action_bindings + editable_bindings by b.name == name (keymap.rs:422-433) — no fixed path. update_binding_list guards !name.is_empty() && binding.name == name (keybindings.rs:939).
6) from_lens (bindings.rs:749-758) — the only fixed-lens bridge — returns None when lens.description is None and otherwise copies name verbatim (lens.name.to_string() = "") plus the volatile id.
7) Synthesis ingredients weak: BindingGroup has exactly 12 variants (bindings.rs:797-810); trigger is cmd<->ctrl platform-resolved (keymap.rs:914-919) and custom->keystroke mutated at registration (matcher.rs:96); description absent for the description-less constructors; Action::type_name uses std::any::type_name (action.rs:16-18), build-unstable.

GREP COUNTS REPRODUCED EXACTLY: 456 (new 384 / standard 7 / custom 44 / empty 21), 146 register callers, 93 files, 0 .with_command_description.

CORRECTION (the one defect): the answer treats "456 FixedBinding constructor call sites" as the full field-addition surface, but there is a 5th constructor it ignores: FixedBinding::new_per_platform (keymap.rs:497), which has 13 call sites and delegates to Self::new (so command_description:None). Including it, the true constructor surface is 469 sites (still 93 files; the 13 sites are in already-counted files), and the from_lens-dropped (description-less) count is ~404 (391 + 13), not 391. Survivors (65 = custom 44 + empty 21), file count (93), and register-call count (146) are unchanged. This correction strictly enlarges the blast radius and reinforces the conclusion.

CONCLUSION (unchanged): fixed bindings are enumerable but nameless (empty name) with only a volatile, non-serializable BindingId; the name-keyed persistence/editor/conflict layer cannot anchor them; description-gated from_lens drops the vast majority. A stable catalog id requires a deliberate change — add an explicit name/stable-id field (~469 constructor sites / 146 register calls / 93 files) — because synthesis from existing fields is not stable+unique+persistable.

_Evidence:_
- FixedBinding has no name field; fields are trigger/action/command_description:Option/context_predicate/enabled_predicate:Option/group:Option<&'static str>/id:BindingId — `crates/warpui_core/src/keymap.rs:277-286` _(code)_
- Four named constructors (new:511, empty:536, standard:553, custom:573) take no name; all set group:None; new+standard set command_description:None, empty+custom set Some — `crates/warpui_core/src/keymap.rs:511-593` _(code)_
- FixedBinding::as_lens hardcodes name: Default::default() (empty &str) — `crates/warpui_core/src/keymap.rs:633` _(both)_
- BindingLens.name is &str, so Default::default() is the empty string — `crates/warpui_core/src/keymap.rs:243` _(code)_
- BindingId(pub usize) minted from static NEXT_BINDING_ID.fetch_add in construction order; derive line lacks Serialize/Deserialize (Serialize import is used by Keystroke at :320, not BindingId) — `crates/warpui_core/src/keymap.rs:261-272` _(both)_
- Enumeration: Keymap::bindings yields editable lenses then fixed_bindings.iter().rev().filter(is_enabled).map(FixedBinding::as_lens) — `crates/warpui_core/src/keymap.rs:454-462` _(code)_
- get_key_bindings entry point is in warpui_core, returns BindingLens iterator — `crates/warpui_core/src/core/app.rs:1699` _(code)_
- get_bindings delegates to keymap.bindings() — `crates/warpui_core/src/keymap/matcher.rs:303` _(code)_
- Settings editor list built only from ctx.editable_bindings(); fixed bindings never enter — `app/src/settings_view/keybindings.rs:754` _(code)_
- Persistence name-keyed: set_custom_keybinding -> set_custom_trigger(name) + write_custom_keybinding(name) — `app/src/util/bindings.rs:491-505` _(code)_
- update_custom_trigger matches editable bindings by b.name == name; no fixed-binding path — `crates/warpui_core/src/keymap.rs:422-433` _(code)_
- UI guards !name.is_empty() && binding.name == name before mutating a binding — `app/src/settings_view/keybindings.rs:939` _(code)_
- from_lens returns None when lens.description is None and otherwise copies empty name + volatile id — `app/src/util/bindings.rs:749-758` _(code)_
- BindingGroup has exactly 12 variants — not unique enough for an id — `app/src/util/bindings.rs:797-810` _(code)_
- Action::type_name uses std::any::type_name (build-unstable) — `crates/warpui_core/src/core/action.rs:16-18` _(code)_
- Triggers platform-resolved cmd<->ctrl (keymap.rs:914-919) and custom->keystroke mutated at registration (matcher.rs:96) — `crates/warpui_core/src/keymap/matcher.rs:89-106` _(code)_
- Grep reproduced: 456 constructor sites (new 384/standard 7/custom 44/empty 21), 146 register callers, 93 files, 0 with_command_description — `grep -rEn 'FixedBinding::(new|standard|custom|empty)' --include=*.rs | wc -l = 456` _(code)_
- CORRECTION: a 5th constructor FixedBinding::new_per_platform (delegates to ::new, command_description:None) has 13 call sites omitted by the answer, raising the constructor surface to 469 and the from_lens-dropped count to ~404; files (93) and register calls (146) unchanged — `crates/warpui_core/src/keymap.rs:497-508; grep 'FixedBinding::new_per_platform' = 13` _(code)_

- _Unblocks:_ Lets the design doc state plainly that fixed bindings have NO stable persistable id today (empty name + volatile non-serializable BindingId), so any feature that names/searches/dedupes/conflict-names fixed bindings or lets the change-keybinding agent reference them MUST first add a stable identity. It sizes that work (456 constructor sites / 146 register calls / 93 files) and rules out cheap synthesis, forcing an explicit name/id-field decision before Axis-5 discoverability and truthful-conflict features (G1/R6) can include fixed bindings.
- _Residual / needs decision:_ Did not enumerate how many of the 469 constructor sites are behind cfg/platform gates (could change the practical edit count). The strategy choice (add explicit name/id field vs synthesize) remains a design decision, not a code fact; synthesis is ruled out from existing fields but I did not prototype a hybrid that adds a single new stable token. Whether the catalog id must be globally unique across fixed+editable+custom-action universes (ties to A5-Q6/Q8) is undecided in code.
- _Verification:_ Opened every cited file at the exact lines plus uncited locations (register_fixed_bindings at keymap.rs:391; new_per_platform at keymap.rs:497; line 320 to identify what actually derives Serialize). Re-ran all five grep counts — all matched the answer exactly (456/384-7-44-21/146/93/0). Verified BindingId derive line (261) has no Serialize and the file's Serialize import serves Keystroke (line 320), not BindingId. Verified the get_key_bindings path resolves to crates/warpui_core/src/core/app.rs:1699 (evidence item 6 cites that path correctly; my first sed's app/src/core/app.rs branch silently failed and the || fallback to the crates path produced the output). Confirmed name-keyed persistence/editor/guard claims and from_lens None-on-no-description. The only defect: the answer's constructor-site count (456) excludes FixedBinding::new_per_platform (13 sites, command_description:None via Self::new), so the real field-addition surface is 469 and ~404 (not 391) fixed bindings would be dropped by from_lens; this enlarges the blast radius and does not undermine the conclusion. The harmless omission of .filter(is_enabled) in the answer's bindings() snippet is immaterial. Marked corrected (not confirmed) solely because the stated edit-surface count was an undercount.

### A5-Q4 — CONFIRMED · confidence: high

**Q.** Are the binding validators usable on the release-build edit path? is_binding_pty_compliant / is_binding_cross_platform run only through validate_bindings(), which (with register/set_default_binding_validator) is entirely #[cfg(debug_assertions)] (matcher.rs:159-203) and runs at registration, not on set_custom_trigger. Do we lift them into a release-safe AppContext::validate_trigger(name, trigger) reusing the registered validators, and is the inline warning advisory or blocking?

**A.** The validator PREDICATE FUNCTIONS (is_binding_pty_compliant at app/src/util/bindings.rs:894, is_binding_cross_platform at :918) are plain non-cfg-gated pub fns that compile and run in release, so their checking LOGIC is reusable. But the REGISTRY + DISPATCH around them (register_binding_validator / set_default_binding_validator / validate_bindings) is entirely #[cfg(debug_assertions)] at BOTH the AppContext layer (real impl + empty release no-op twin) and the Matcher layer (debug-only, no release twin), so in release the registry is never populated and validate_bindings() is a {} no-op. validate_bindings runs exactly once at startup (platform/app.rs:85) and PANICS on failure; it never runs on the edit path. The save path (confirm_keystroke_editing -> set_custom_keybinding -> set_custom_trigger) performs no validation, and there is no inline pty/cross-platform warning today and no AppContext::validate_trigger anywhere — so advisory-vs-blocking is an unimplemented product decision, and R10 is a real-but-bounded wire-up, not a no-op nor a from-scratch validator engine. Verified independently; the original answer holds.

VERDICT: CONFIRMED. I independently opened every cited file plus one uncited location and every materially load-bearing claim holds; line refs are exact. Only one cosmetic imprecision noted (does not change the verdict).

VERIFIED CODE FACTS:

1) Predicate fns are release-safe. is_binding_pty_compliant (app/src/util/bindings.rs:894) and is_binding_cross_platform (:918) are ordinary `pub fn`, no cfg gate on definition or body, taking `BindingLens` and returning `IsBindingValid`. Bodies are pure: read `binding.original_trigger.unwrap_or(binding.trigger)`, call `trigger_to_keystroke`; pty runs a regex (CONTROL_CHARACTER_KEY_REGEX.is_match) AND an allow-list check (is_pty_non_compliant_binding_allowed, bindings.rs:909); cross-platform returns Yes early when is_mac() (line 919) then checks `keystroke.cmd`. Compile and execute identically in release. IsBindingValid/BindingLens are imported from warpui_core (bindings.rs:13-14). So R10 does NOT need the checking LOGIC re-authored.

2) Registration + run machinery is debug-only at BOTH layers:
   - AppContext::register_binding_validator: real #[cfg(debug_assertions)] impl (crates/warpui_core/src/core/app.rs:1567-1575), empty #[cfg(not(debug_assertions))] twin (1577-1582).
   - AppContext::set_default_binding_validator: real (1587-1594) vs empty no-op (1599-1604).
   - AppContext::validate_bindings: real (1608-1611) vs `{}` no-op (1615-1616).
   - Matcher::register_binding_validator (matcher.rs:160, cfg at :159), set_default_binding_validator (:172, cfg :171), validate_bindings (:182, cfg :181) are each #[cfg(debug_assertions)] with NO release twin — they do not exist in release.
   - Matcher fields default_binding_validator (matcher.rs:19) and binding_validators (:22) are NOT cfg-gated (struct always has them), but because release registration is a no-op the registry is always EMPTY in release. Every validator registered at app-init is dead in release.

3) The registered validators are an assertion harness, not a UI warning. The ONLY call to validate_bindings() is at startup in initialize_app (crates/warpui_core/src/platform/app.rs:85), right after init_fn runs registration. It iterates every matching binding and PANICS on the first failure (matcher.rs:200-202: `panic!("Bindings failed validation ...")`). Registration-time, debug-only, fatal — not advisory, not on the edit path.

4) Registration call sites (re-ran grep; all app-init, all no-op in release): default cross-platform validator at app/src/lib.rs:1395; per-view pty-compliant validators at app/src/root_view.rs:262, app/src/pane_group/mod.rs:318, app/src/terminal/view/init.rs:83, app/src/workspace/mod.rs:82. Exactly 4 pty + 1 cross-platform, matching the answer.

5) Edit/save path does NO validation. confirm_keystroke_editing (app/src/settings_view/keybindings.rs:669-699): when `unsaved_binding` is Some it unconditionally calls set_custom_keybinding(&row.binding.name, &key, ctx) — no has_conflict gate, no validator. set_custom_keybinding (app/src/util/bindings.rs:491-506) calls ctx.set_custom_trigger(name, Trigger::Keystrokes(vec![keystroke])) -> Matcher::set_custom_trigger (matcher.rs:137-141) -> keymap.update_custom_trigger, and persists via write_custom_keybinding. No validator consulted. grep for `validate_trigger` across app/ and crates/ returns ZERO hits — that method does not exist.

6) No inline pty/cross-platform warning exists. The only edit-time feedback is the same-keystroke ConflictMap (keybindings.rs:104-133, HashMap<Keystroke,usize>); has_conflict (:123, count>1) is read only at keybindings.rs:1064 to compute has_conflicting_binding, which feeds render code (render_clicked :219, render_summary :221, conflict_warning :313). It never gates confirm_keystroke_editing. So even the conflict signal is purely advisory/non-blocking; pty/cross-platform validity is surfaced nowhere on save.

CONSEQUENCE FOR R10 (design decision, accurately scoped by the answer): the pure predicate fns are reusable as-is but have signature `fn(BindingLens) -> IsBindingValid`, so calling them on a candidate keystroke needs a tiny signature adaptation / a transient BindingLens. To instead reuse the per-view REGISTERED validators in release you must lift register_binding_validator / set_default_binding_validator out of #[cfg(debug_assertions)] (the struct fields already exist in release) and add a non-panicking per-trigger query (e.g. validate_trigger returning IsBindingValid) invoked in confirm_keystroke_editing before set_custom_keybinding. R10 logic = trivial; R10 wiring = real but bounded — not a free wire-up nor a from-scratch engine.

_Evidence:_
- is_binding_pty_compliant and is_binding_cross_platform are plain pub fns with no cfg gate; bodies pure (original_trigger.unwrap_or(trigger) + trigger_to_keystroke; pty=regex+allow-list, cross-platform=is_mac early-return then keystroke.cmd); run in release — `app/src/util/bindings.rs:894-933` _(code)_
- AppContext::register_binding_validator has a real #[cfg(debug_assertions)] impl and an empty #[cfg(not(debug_assertions))] no-op twin — `crates/warpui_core/src/core/app.rs:1567-1582` _(code)_
- AppContext::set_default_binding_validator (real 1587-1594 / empty 1599-1604) and validate_bindings (real 1608-1611 / {} 1615-1616) are cfg-split with release no-ops — `crates/warpui_core/src/core/app.rs:1587-1616` _(both)_
- Matcher::register_binding_validator (:160), set_default_binding_validator (:172), validate_bindings (:182) are each #[cfg(debug_assertions)] (cfg at 159/171/181) with NO release twin, so they do not exist in release — `crates/warpui_core/src/keymap/matcher.rs:159-203` _(both)_
- validate_bindings collects failures across binding_validators and default_validator and panics on the first non-empty set — fatal debug assertion, not advisory — `crates/warpui_core/src/keymap/matcher.rs:200-202` _(code)_
- Matcher fields default_binding_validator (:19) and binding_validators (:22) are NOT cfg-gated so they exist in release, but stay empty because release registration is a no-op — `crates/warpui_core/src/keymap/matcher.rs:19-22` _(code)_
- validate_bindings() is invoked exactly once, at startup in initialize_app after init_fn runs registration; grep shows no other call site in app/ or crates/ — `crates/warpui_core/src/platform/app.rs:85` _(both)_
- Registration sites are all app-init: cross-platform default at lib.rs:1395; per-view pty at root_view.rs:262, pane_group/mod.rs:318, terminal/view/init.rs:83, workspace/mod.rs:82 (re-ran grep: 4 pty + 1 cross-platform); all no-op in release — `app/src/lib.rs:1395; app/src/root_view.rs:262; app/src/pane_group/mod.rs:318; app/src/terminal/view/init.rs:83; app/src/workspace/mod.rs:82` _(code)_
- confirm_keystroke_editing unconditionally calls set_custom_keybinding when unsaved_binding is Some, with no validation and no has_conflict gating — `app/src/settings_view/keybindings.rs:669-699` _(code)_
- set_custom_keybinding -> ctx.set_custom_trigger(Trigger::Keystrokes(vec![keystroke])) -> Matcher::set_custom_trigger -> update_custom_trigger; persists via write_custom_keybinding; no validator — `app/src/util/bindings.rs:491-506; crates/warpui_core/src/keymap/matcher.rs:137-141` _(code)_
- No AppContext::validate_trigger (or any validate_trigger) exists; grep across app/ and crates/ returns zero hits — `grep -rn validate_trigger app/ crates/ (no matches)` _(code)_
- Edit-time conflict feedback is the same-keystroke ConflictMap (HashMap<Keystroke,usize>); has_conflict (:123) is read only at keybindings.rs:1064 feeding render code, never gating confirm — `app/src/settings_view/keybindings.rs:104-133; :1064` _(code)_

- _Unblocks:_ Lets the design size R10 ('validators on the edit path') correctly: the pty/cross-platform CHECKING LOGIC is already release-safe and reusable (small), but the validator DISPATCH/REGISTRY is compiled out in release and currently fatal-only at startup, so wiring a non-fatal, release-safe validate_trigger(name, trigger) into confirm_keystroke_editing is a real-but-bounded refactor (un-gate registration + add a non-panicking query + call it before set_custom_keybinding) rather than either a free wire-up or a from-scratch validator engine.
- _Residual / needs decision:_ Whether a future inline validity warning should be advisory or blocking is a product/UX decision, not a code fact — today nothing warns or blocks on save (only same-keystroke conflicts are surfaced, and even those are purely render-side / non-blocking). Design choice not resolvable from code: whether R10 reuses the two pure predicate fns directly (constructing a transient BindingLens / adapting the signature to (name, keystroke)) versus un-gating the per-view registered-validator registry (remove #[cfg(debug_assertions)] on register/set_default_binding_validator — fields already exist in release — and add a non-panicking validate_trigger query instead of validate_bindings()'s collect-and-panic). Not verified: whether any per-view validator beyond pty/cross-platform would need to run on the edit path, and the best way to build a transient BindingLens for an un-persisted candidate keystroke.
- _Verification:_ I opened all cited files at the exact lines and re-ran the grep counts myself. Matcher layer: cfg(debug_assertions) on register (:159/:160), set_default (:171/:172), validate_bindings (:181/:182), panic at :200-202, fields at :19/:22 (not cfg-gated) — all exactly as claimed; no release twin for the Matcher methods. AppContext layer: real/no-op cfg-split confirmed at 1567-1616. Predicate fns at bindings.rs:894 and :918 are plain pub fns, no gate — confirmed. set_custom_keybinding 491-506 and set_custom_trigger 137-141 confirmed; no validation. confirm_keystroke_editing 669-699 calls set_custom_keybinding unconditionally with no conflict gate — confirmed. Grep: validate_trigger has ZERO hits; validate_bindings called only at platform/app.rs:85; register sites are exactly the 4 pty + 1 cross-platform listed. UNCITED LOCATION I checked: the full has_conflict/conflict_map usage map across keybindings.rs — its only read is at :1064 feeding render_clicked/render_summary/conflict_warning, confirming the save path never consults conflict (or any) validity. One cosmetic imprecision in the original: it describes the pty body as 'run a regex against an allow-list' when it actually runs a regex (CONTROL_CHARACTER_KEY_REGEX) AND a separate allow-list check (is_pty_non_compliant_binding_allowed) combined with &&; substance unchanged. Every line ref is correct (no shifts), no app-vs-crates scope confusion, counts match. Nothing refuted.

### A5-Q6 — CONFIRMED · confidence: high

**Q.** Should conflict detection scan fixed bindings and custom-action bindings, not just editable ones? ConflictMap is built only from the editable list shown in the editor, so a user rebind that collides with a FixedBinding (get_key_bindings) or a custom-action binding (custom_action_bindings) reports 'no conflict' — the opposite of truthful.

**A.** Confirmed. Today the conflict map's universe is editable bindings ONLY. It is built and mutated exclusively from ctx.editable_bindings() (which iterates the keymap's editable_bindings Vec, then deduplicates by name+description). Fixed bindings (get_key_bindings) and custom-action bindings (custom_action_bindings) live in separate keymap collections that the conflict map never reads, so a rebind colliding with either reports has_conflict()==false. Whether it SHOULD scan them is a design decision; the code-verifiable part is that it currently does not, and fixed lenses cannot even name a collision because their lens name is empty (name: Default::default()).

I independently opened every cited file and re-ran the greps; all substantive claims hold.

The keymap stores FOUR disjoint binding collections (crates/warpui_core/src/keymap.rs:25-37): fixed_bindings (:26), editable_bindings (:27), fixed_custom_action_bindings (:36), editable_custom_action_bindings (:37). Three accessors expose three different universes:
1. editable only — editable_bindings() (keymap.rs:441-447) iterates ONLY self.editable_bindings, filtered to enabled. Reached via matcher.rs:294-296 and AppContext::editable_bindings (core/app.rs:1788-1790).
2. editable + fixed — bindings()/get_bindings (keymap.rs:454-464) chains editable_bindings() with fixed_bindings; exposed by get_key_bindings (core/app.rs:1699-1701 -> matcher.rs:303-305). FixedBinding is documented "This action can't be reconfigured with a custom key binding trigger" (keymap.rs:275-277).
3. custom-action — custom_action_bindings() (keymap.rs:474-484) chains editable+fixed custom action collections, exposed by core/app.rs:1911-1913.

The settings-view ConflictMap is fed from universe #1 only:
- Initial build: on_page_selected snapshots ctx.editable_bindings() (keybindings.rs:754) into self.bindings (after sort+dedup_by name/description at :759-783), then builds conflict_map from self.bindings ... binding.trigger.clone() (keybindings.rs:795-800) via the FromIterator<Option<Keystroke>> impl (keybindings.rs:135-146), which just counts identical Keystrokes.
- All incremental mutations pass only editable-row triggers: remove_keystroke (:596), reset_to_default_keystroke (:620-621), cancel_keystroke_editing (:647-650), set_temporary_keystroke_state (:717-718). conflict_map.update (:110-121) increments/decrements counts; has_conflict returns count>1 (:123-132).
- Consumed at render: has_conflict(&row.binding.trigger) (:1064), gating only the generic warning. A fixed or custom-action collision contributes 0 to the count for that keystroke, so the UI shows "no conflict."

Truthfulness/naming is doubly broken: (a) the universe is wrong, and (b) even extending the scan to fixed bindings cannot name the colliding action because FixedBinding::as_lens carries name: Default::default() i.e. empty string (keymap.rs:631-642, inside impl FixedBinding which begins at :277, before impl EditableBinding at :645). The warning is generic: SHORTCUT_CONFLICT_WARNING_TEXT = "This shortcut conflicts with other keybinds" (keybindings.rs:48).

No existing code does a cross-universe conflict scan: filter_bindings_including_keystroke (bindings.rs:625-687) only matches on binding.trigger (:638) and binding.description (:651) of whatever &CommandBinding iterator the caller supplies (the settings view supplies editable-only self.bindings); it is search, not conflict, and never reads name.

Magnitude (re-verified): grep counts reproduce exactly — 397 FixedBinding::new construction sites and 146 register_fixed_bindings call sites across app/+crates/. Spot-checked user-facing sites: modal.rs:102 (app.register_fixed_bindings(vec![FixedBinding::new(...)) and root_view.rs:426 (app.register_fixed_bindings([) confirmed; menu.rs FixedBinding::new at :1749 (the detailed answer's illustrative "menu.rs:1748" is off by one — not a hard evidence claim). Every one of these is invisible to conflict detection today.

Design residue: to be truthful, conflict detection must union all three universes and give fixed/custom lenses real identity (name/label) so the colliding action can be displayed.

_Evidence:_
- Keymap has 4 disjoint binding collections: fixed_bindings, editable_bindings, fixed_custom_action_bindings, editable_custom_action_bindings — `crates/warpui_core/src/keymap.rs:25-37` _(code)_
- editable_bindings() iterates ONLY self.editable_bindings, filtered to enabled (matcher.rs:294-296 forwards it) — `crates/warpui_core/src/keymap.rs:441-447` _(both)_
- get_bindings/bindings() = editable_bindings() chained with fixed_bindings; this is what get_key_bindings exposes — `crates/warpui_core/src/keymap.rs:454-464` _(code)_
- AppContext::get_key_bindings exposes editable+fixed via keystroke_matcher.get_bindings — `crates/warpui_core/src/core/app.rs:1699-1701` _(code)_
- AppContext::custom_action_bindings exposes the custom-action universe (editable+fixed custom) — `crates/warpui_core/src/core/app.rs:1911-1913` _(code)_
- AppContext::editable_bindings only forwards the editable universe (used by settings view) — `crates/warpui_core/src/core/app.rs:1788-1790` _(code)_
- ConflictMap is built from self.bindings (sourced from ctx.editable_bindings() at :754, sort+dedup at :759-783) triggers only — `app/src/settings_view/keybindings.rs:754-800` _(code)_
- ConflictMap FromIterator just counts identical Keystrokes; has_conflict = count>1; update inc/dec counts — `app/src/settings_view/keybindings.rs:104-146` _(code)_
- All incremental conflict_map.update calls feed only editable-row triggers (remove :596, reset :620-621, cancel :647-650, temp edit :717-718) — `app/src/settings_view/keybindings.rs:596,620-621,647-650,717-718` _(code)_
- Conflict consulted only against editable row trigger at render: has_conflict(&row.binding.trigger) — `app/src/settings_view/keybindings.rs:1064` _(code)_
- FixedBinding (doc: 'can't be reconfigured', :275-277) lens has empty name (name: Default::default()), so a fixed collision cannot be named — `crates/warpui_core/src/keymap.rs:631-642` _(code)_
- Conflict warning text is generic and names no action — `app/src/settings_view/keybindings.rs:48` _(code)_
- filter_bindings_including_keystroke matches only binding.trigger (:638) and binding.description (:651) of caller-supplied iterator; no cross-universe scan, never reads name — `app/src/util/bindings.rs:625-687` _(code)_
- 397 FixedBinding::new sites and 146 register_fixed_bindings call sites across app/+crates/; spot-checked user-facing modal.rs:102 and root_view.rs:426 register sites (menu.rs FixedBinding::new at :1749, detailed text's :1748 is off by one) — `grep -rn FixedBinding::new --include=*.rs app/ crates/ | wc -l => 397; grep -rn register_fixed_bindings --include=*.rs app/ crates/ | grep -v 'fn register_fixed_bindings' | wc -l => 146` _(code)_

- _Unblocks:_ Lets the design doc fix the conflict-universe decision: conflict detection today is editable-only and silently passes rebinds that collide with fixed or custom-action bindings. To make conflicts truthful (Axis 5 / R6), the engine must union all three keymap collections AND attach displayable identity to fixed/custom lenses so the colliding action can be named — sequencing this as a prerequisite before any "names the colliding action" UI work.
- _Residual / needs decision:_ The 'should' is a product/design decision, not a code fact: the design doc must decide (a) whether to union fixed and custom-action bindings into the conflict universe, and (b) whether collisions with non-editable bindings surface as hard conflicts vs. informational warnings (the user cannot rebind a FixedBinding — 'can't be reconfigured', keymap.rs:275). Truthful conflicts require a data-model change: fixed/custom lenses lack a name/label (FixedBinding::as_lens name is empty), so naming the colliding action is impossible without giving those bindings stable user-facing identity. Context-overlap (A5-Q5/Q11) and chord/prefix-shadow (A5-Q7) are separate axes; this covers only universe-membership. Not independently verified: whether any non-settings code path also builds a ConflictMap (I confirmed the only ConflictMap construction sites are the FromIterator collect at keybindings.rs:795-800 plus the update mutators; no other builders observed in the settings view, but I did not exhaustively grep the whole app/ for alternate conflict-detection implementations).
- _Verification:_ I opened and independently verified every cited file:line. keymap.rs:25-37 (4 collections), :441-447 (editable-only iter), :454-464 (editable+fixed), :474-484 (custom-action), :275-277 (FixedBinding doc), :631-642 (FixedBinding::as_lens with name: Default::default() — confirmed this as_lens sits inside impl FixedBinding, since impl EditableBinding only begins at :645). app/core/app.rs:1699-1701, :1788-1790, :1911-1913 accessors all confirmed. matcher.rs:294-296/:303-305 forwarders confirmed (location the answer cited only indirectly — I opened it). Settings view: keybindings.rs:48 (warning text), :104-146 (ConflictMap impl), :754-800 (build from editable bindings + dedup), :596/:620-621/:647-650/:717-718 (all four incremental updates use row.binding.trigger), :1064 (render). bindings.rs:625-687 (filter reads trigger :638 and description :651, never name). I re-ran both greps myself: 397 and 146 reproduce exactly. Spot-checked three magnitude sites the evidence cited only in aggregate: modal.rs:102 and root_view.rs:426 are register_fixed_bindings call sites; menu.rs:1749 is a FixedBinding::new (detailed answer said :1748 — a harmless off-by-one in an illustrative list, absent from the evidence array). One additional nuance I found that does NOT refute the answer: even within the editable universe, the conflict map is built from the deduplicated self.bindings (dedup_by name+description at :782), so the blind spot is if anything slightly larger than 'all editable', never smaller. No claim was overgeneralized to terminal-vs-code-editor scope; no app/-vs-crates confusion. The core fact — conflict universe is editable-only and fixed/custom collisions report no conflict, with fixed lenses unable to name the collision — is fully supported by the code.

### A5-Q7 — CONFIRMED · confidence: high

**Q.** How does conflict detection handle chords and prefix-shadowing once chord capture (G4/R8) lands? ConflictMap keys on a single Keystroke and update() takes one Keystroke, but chords are Vec<Keystroke> and the matcher is first-match-wins so a complete short binding silently shadows any longer chord sharing its prefix. Is prefix-shadow a 'conflict' the UI must surface, and how is ConflictMap rekeyed to Vec<Keystroke> + prefix relationships?

**A.** Confirmed. Today there is zero chord/prefix awareness in conflict handling: the settings-UI ConflictMap is keyed on a single Keystroke (HashMap<Keystroke, usize>, keybindings.rs:104-132) and is fed from CommandBinding.trigger, which collapses any chord to only its FIRST keystroke (trigger_to_keystroke -> keys.first().cloned(), bindings.rs:236). So once real chords exist the map is unsound both ways: distinct prefix-sibling chords (K-C vs K-D) collapse to the same key and are FALSELY flagged as conflicts, while genuine first-match prefix-shadow is never surfaced as a distinct relationship. There is no engine-level conflict/prefix detector at all (matcher only does first-match-wins via starts_with; validate_bindings runs per-binding, debug-only validators). Whether prefix-shadow is a hard conflict vs a warning is a product call; the code fact is the map must be rekeyed from Keystroke to the full Vec<Keystroke> trigger plus an explicit strict-prefix index mirroring the matcher's starts_with check (matcher.rs:326). All evidence line refs verified accurate.

VERDICT: confirmed. I independently opened every cited file:line plus uncited locations; all claims hold exactly.

CURRENT STRUCTURE (settings UI is the ONLY conflict detector in the codebase):
- ConflictMap is `map: HashMap<Keystroke, usize>` — a single Keystroke key (app/src/settings_view/keybindings.rs:104-107). `update(&mut self, old: &Option<Keystroke>, new: Option<Keystroke>)` decrements the old key and increments the new (110-121). `has_conflict` returns true iff the count for that exact Keystroke is > 1 (123-132, predicate at 128). Rebuilt at page open from `binding.trigger.clone()` via `FromIterator<Option<Keystroke>>` (135-146; rebuild loop 794-800) and rendered as a warn-colored border (270-276) / bold "shortcut conflict" text (313-324) when `has_conflict(&row.binding.trigger)` is true (1064).
- The model it keys on cannot hold a chord. `CommandBinding.trigger: Option<Keystroke>` is a SINGLE keystroke (app/src/util/bindings.rs:534). It is filled by `from_editable_lens` -> `trigger: trigger_to_keystroke(lens.trigger)` (bindings.rs:765). `trigger_to_keystroke` for `Trigger::Keystrokes(keys)` returns `keys.first().cloned()` (bindings.rs:236) — every keystroke after the first is DISCARDED.
- The engine's real chord type is `Trigger::Keystrokes(Vec<Keystroke>)` (crates/warpui_core/src/keymap.rs:45); `Keystroke` is one key + 5 modifier bools (keymap.rs:320-328). A chord lives in the engine as a Vec but is flattened to its head keystroke the moment it crosses into the settings model.
- The settings UI can also only CREATE single-keystroke triggers today: `set_custom_keybinding` always wraps in a 1-element vec `Trigger::Keystrokes(vec![keystroke.clone()])` (bindings.rs:492-494) and capture is a single `on_keydown` keystroke (keybindings.rs:294-300). Chord capture is the G4/R8 work that does not exist yet.

WHY IT BREAKS UNDER CHORDS (two-sided unsoundness):
- False positives: two distinct chords sharing a first key, e.g. `K C` and `K D`, both collapse to Keystroke `K` via `keys.first()`, so the map counts 2 for `K` and `has_conflict` flags BOTH even though they never collide at runtime.
- False negatives / wrong framing for true shadowing: the runtime matcher is first-match-wins with a prefix early-return — `push_keystroke` iterates ALL bindings, and on `keystrokes.starts_with(&pending.keystrokes) && binding.context_predicate.eval(ctx)` it returns the action immediately when `keystrokes.len() == pending.keystrokes.len()` (matcher.rs:324-345; starts_with at 326; equal-length return at 329-331). I traced this: because the loop visits every binding and only a complete-length match returns, a complete short binding `K` is always reached and returns Action(K), so it ALWAYS wins over a longer chord `K C` regardless of iteration order, leaving `K C` unreachable (silent prefix-shadow). The current ConflictMap cannot represent or label this prefix relationship.

NO ENGINE-LEVEL HELP EXISTS: there is no cross-binding conflict or prefix detector anywhere. `validate_bindings` (matcher.rs:181-203) only runs per-binding validators registered via `register_binding_validator`/`set_default_binding_validator` (matcher.rs:160-177), all `#[cfg(debug_assertions)]` and panic-on-fail (panic at 201). The two real validators, `is_binding_pty_compliant` (bindings.rs:894-907) and `is_binding_cross_platform` (bindings.rs:918-933), each take a single `BindingLens`, call `trigger_to_keystroke` (lines 896, 924 — they too see only the first keystroke), and have zero awareness of any other binding. My independent grep for `conflict` across crates/warpui_core/src and app/src (excluding settings_view/keybindings.rs) returns only unrelated uses (cloud_object sync ConflictStatus, presenter position naming, borrow-conflict comments) — confirming the settings ConflictMap is the only keybinding conflict logic.

DESIGN IMPLICATIONS (code-verifiable shape of the fix):
1. Rekey the map off the full trigger, not a head Keystroke: `HashMap<Vec<Keystroke>, usize>` (or a normalized `Trigger`), and stop routing chords through `trigger_to_keystroke`/`Option<Keystroke>` in CommandBinding for conflict purposes.
2. Add an explicit prefix index. Exact-duplicate trigger (same Vec) = the existing count>1 hard conflict. Prefix-shadow = binding A is a STRICT prefix of binding B; detect with the same `starts_with` predicate the matcher already uses (matcher.rs:326), and surface it because it is precisely the silent override this axis targets.
3. Correctness also requires context-predicate awareness, which the map entirely lacks today (it keys on keystroke only, ignoring `context_predicate`; the matcher already gates on `context_predicate.eval(ctx)` at matcher.rs:327). That is the Axis-2/R6 dependency (see A5-Q11).

No code-editor vs terminal-editor divergence here: both share the same settings ConflictMap and the same `push_keystroke` matcher.

_Evidence:_
- ConflictMap is keyed on a single Keystroke (HashMap<Keystroke, usize>); update() takes one Option<Keystroke> and decrements old / increments new; has_conflict is count>1 for that exact key (predicate at :128) — `app/src/settings_view/keybindings.rs:104-132` _(code)_
- ConflictMap is rebuilt at page open from binding.trigger (Option<Keystroke>) via FromIterator (:135-146 def, :794-800 rebuild), and rendered via has_conflict(&row.binding.trigger) (:1064) into a warn border (:270-276) and bold conflict text (:313-324) — `app/src/settings_view/keybindings.rs:135-146,270-276,313-324,794-800,1064` _(code)_
- CommandBinding.trigger is a single Option<Keystroke> (:534), set from trigger_to_keystroke(lens.trigger) in from_editable_lens (:765) — `app/src/util/bindings.rs:534,765` _(code)_
- trigger_to_keystroke collapses a chord Trigger::Keystrokes(keys) to keys.first().cloned() — dropping all but the first keystroke — `app/src/util/bindings.rs:236` _(code)_
- The settings UI can only create single-keystroke triggers today: set_custom_keybinding always wraps a 1-element vec Trigger::Keystrokes(vec![keystroke.clone()]) — `app/src/util/bindings.rs:491-495` _(code)_
- A chord is Trigger::Keystrokes(Vec<Keystroke>) (:45) and Keystroke is one key + 5 modifier bools (:320-328) — `crates/warpui_core/src/keymap.rs:44-49,320-328` _(both)_
- Matcher is first-match-wins: push_keystroke iterates ALL bindings, on starts_with (:326) + context_predicate.eval (:327) returns the action immediately when lengths are equal (:329-331); traced to confirm a complete short binding always shadows a longer chord sharing its prefix regardless of iteration order — `crates/warpui_core/src/keymap/matcher.rs:307-346` _(both)_
- No cross-binding conflict/prefix detection in the engine: validate_bindings only runs per-binding validators registered via register/set_default_binding_validator, all #[cfg(debug_assertions)], panic on fail (:201) — `crates/warpui_core/src/keymap/matcher.rs:159-203` _(code)_
- Both registered validators take a single BindingLens and route through trigger_to_keystroke (:896, :924), so they see only the first keystroke and have no cross-binding awareness — `app/src/util/bindings.rs:894-933` _(code)_
- Independent grep for 'conflict' across crates/warpui_core/src and app/src excluding settings_view/keybindings.rs returns only unrelated uses (cloud_object ConflictStatus, presenter naming, borrow comments) — confirming the settings ConflictMap is the only keybinding conflict logic; starts_with appears exactly once in matcher.rs (line 326) — `crates/warpui_core/src + app/src (grep)` _(code)_
- Uncited cross-check: filter_bindings_including_keystroke also reads only the single binding.trigger keystroke, reinforcing the single-keystroke model pervasiveness (not a conflict detector) — `app/src/util/bindings.rs:625-687` _(code)_

- _Unblocks:_ Lets the design doc specify the chord-era conflict model concretely: rekey ConflictMap from HashMap<Keystroke,usize> to the full Vec<Keystroke> trigger, add a strict-prefix index mirroring the matcher's starts_with (matcher.rs:326), stop flattening chords through trigger_to_keystroke for conflict purposes, and declare prefix-shadow surfacing (conflict vs warning) plus its dependence on Axis-2 context scoping.
- _Residual / needs decision:_ Two product/UX decisions are not settleable from code: (a) whether prefix-shadow is presented as a hard conflict (same warn border / blocking as exact duplicates) or a softer 'this longer chord is unreachable' warning; and (b) user-facing copy distinguishing exact-duplicate vs prefix-shadow. There is also a sequencing dependency: a fully truthful chord conflict map needs context-predicate awareness (the current ConflictMap ignores context entirely, while the matcher already gates on context_predicate.eval at matcher.rs:327), which is the Axis-2/R6 work tracked in A5-Q11; chord rekeying alone will still over/under-report across distinct contexts until that lands. Not investigated: whether any built-in/default (non-editable) bindings are already multi-keystroke chords today — if so, the false-positive collapse could already be latent rather than purely future, but no such default was surfaced and the settings creation path emits only single-keystroke triggers.
- _Verification:_ Attempted to refute on every axis the mandate flagged; the answer survived all checks. (1) Line refs: each citation is accurate — ConflictMap struct/update/has_conflict at 104-132, FromIterator 135-146, rebuild 794-800, render 1064, warn border 270-276, conflict text 313-324; trigger_to_keystroke keys.first() at 236; CommandBinding.trigger at 534; from_editable_lens at 765; set_custom_keybinding 1-elem vec at 492-494; Trigger::Keystrokes at keymap.rs:45; Keystroke struct 320-328; matcher push_keystroke 307-346 with starts_with at 326 and equal-length return 329-331; validate_bindings 181-203; validators 160-177; bindings validators 894-933. (2) Overgeneralization: the 'always shadows regardless of iteration order' claim is the strongest assertion, so I hand-traced push_keystroke — the loop visits every binding and only an equal-length match returns, so a complete short binding is always reached and returns Action; a longer chord only sets retain_pending. The claim holds. (3) app/-vs-crates scope: correctly attributed — ConflictMap and trigger_to_keystroke/CommandBinding live in app/, matcher/Trigger/Keystroke in crates/warpui_core. No confusion. (4) Counts/grep: re-ran the conflict grep myself; outside settings_view/keybindings.rs there is no keybinding conflict logic, only unrelated 'conflict' tokens. starts_with occurs exactly once in matcher.rs. (5) Graph-vs-code: the answer notes the graph covers only warpui_core/editor/vim and reads app/ directly; I verified app/ claims by direct read. The only soft framing (false-positive K-C/K-D scenario) is explicitly conditioned on 'once real chords exist', which is correct since set_custom_keybinding currently emits only 1-element vecs. No material error found.

### A5-Q8 — CONFIRMED · confidence: high

**Q.** How does the catalog/search represent one action NAME bound in multiple contexts? A name maps to several bindings with distinct ContextPredicates (editable_bindings_by_name: HashMap<&str, Vec<usize>>, keymap.rs:30); the editor's dedup_by collapses same-name+same-description rows but NOT same-name/different-description ones (keybindings.rs:782, comment 766-781). Is each (name, context) a separate catalog entry, or one entry with N scopes?

**A.** Neither. The catalog has NO scope/context dimension today — CommandBinding drops the ContextPredicate. The de-facto entry key is (name, description): the settings view dedup_by collapses same-name+same-description context variants into ONE scope-less row, while same-name/different-description variants survive as N separate scope-less rows (linked by name). The resource-center catalog dedups by description only. Search matches on description text and keystroke, never on name.

CONFIRMED against the code. ENGINE (warpui_core/keymap.rs) models name->many-contexts, but the UI flattens it before display:\n- `editable_bindings_by_name: HashMap<&'static str, Vec<usize>>` (keymap.rs:30, exact) maps one name to a Vec of indices of context-distinct editable bindings, populated per-registration at keymap.rs:414-417.\n- `get_binding_by_name` (keymap.rs:379-386) returns only the earliest-registered currently-enabled binding for a name (find_map over the index Vec), not the set.\n- `update_custom_trigger` (keymap.rs:422-433) writes the new trigger to ALL editable bindings whose name matches (filters both editable_custom_action_bindings and editable_bindings by `b.name == name`). With `set_custom_keybinding` (bindings.rs:491-506) persisting `Trigger::Keystrokes(vec![keystroke])` (single-element), remapping is name-keyed and uniform across every context variant — one keystroke per name, not per (name, context).\n\nTHE CATALOG ROW HAS NO SCOPE FIELD. `CommandBinding` (bindings.rs:531-541) carries only name, description, trigger, action, group, id — no ContextPredicate. Both constructors drop the predicate: `from_lens` (bindings.rs:749-758) and `from_editable_lens` (bindings.rs:762-771) read description/trigger/action/name/group/id only. So one-entry-with-N-scopes is not representable today.\n\nSETTINGS-VIEW ENTRY GRANULARITY = (name, description). `on_page_selected` (keybindings.rs:754-784) builds one CommandBinding per editable lens, sorts by description then name (759-765), then `dedup_by(|a,b| a.name == b.name && a.description == b.description)` (782). Consequence (comment 766-781): context variants sharing both name AND description collapse to ONE row; variants sharing a name but differing in description (e.g. find in block vs find in terminal) remain SEPARATE rows. editable_bindings() iterates reverse-registration order (keymap.rs:441-447, `.rev()`).\n\nCONFLICT DETECTION is keyed purely on the keystroke, context-blind: `ConflictMap { map: HashMap<Keystroke, usize> }` (keybindings.rs:104-133); a row is flagged via `conflict_map.has_conflict(&row.binding.trigger)` (keybindings.rs:1064).\n\nSEARCH IS BY DESCRIPTION + KEYSTROKE, NOT NAME. `filter_bindings_including_keystroke` (bindings.rs:625-687) scores against `binding.description.in_context(...)` fuzzy (651) and `binding.trigger` keystroke (638); it never reads `binding.name`.\n\nRESOURCE-CENTER CATALOG is coarser: `build_bindings` (keybindings_page.rs:136-153) filters `trigger.is_some() && non-empty description` (140-146), sorts by description (147-151), then `dedup_by(|a,b| a.description == b.description)` (152) — description-ONLY, so even two different names with the same description collapse. `get_bindings_by_section` lives in keybindings_page.rs:229-284 (NOT utils.rs) and filters by `binding.name` membership (line 248) in static name-only arrays. Those arrays are at utils.rs:8-114 (BLOCKS/INPUT_EDITOR/TERMINAL/FUNDAMENTALS); the 5th, PRIORITIZED_KEYBINDINGS used for the Essentials section, is imported from command_palette (keybindings_page.rs:26), not utils.rs.\n\nNet: there is currently no (name, context) entry and no one-entry-with-N-scopes entry. Entries are (name, description) tuples (description-only in the resource center) with scope dropped; multiple contexts either silently merge (same description) or appear as parallel scope-less rows (different description) that all remap together by name.

_Evidence:_
- Engine maps one name to a Vec of context-distinct editable bindings (HashMap<&'static str, Vec<usize>>) — `crates/warpui_core/src/keymap.rs:30` _(code)_
- Name index populated per-registration (idx pushed onto by-name Vec via entry().or_default().push) — `crates/warpui_core/src/keymap.rs:414-417` _(code)_
- get_binding_by_name returns only the earliest-registered enabled binding for a name (find_map over indices), not the set — `crates/warpui_core/src/keymap.rs:379-386` _(code)_
- Remapping is name-keyed: update_custom_trigger sets the trigger on ALL editable bindings with b.name == name (both vectors) — `crates/warpui_core/src/keymap.rs:422-433` _(code)_
- editable_bindings() iterates reverse-registration order (.rev()), most-recent highest precedence — `crates/warpui_core/src/keymap.rs:441-447` _(code)_
- set_custom_keybinding persists one keystroke per name via Trigger::Keystrokes(vec![keystroke]) (single element) — `app/src/util/bindings.rs:491-506` _(code)_
- CommandBinding has no context/scope field (name, description, trigger, action, group, id only) — `app/src/util/bindings.rs:531-541` _(code)_
- from_lens and from_editable_lens drop the ContextPredicate; read only desc/trigger/action/name/group/id — `app/src/util/bindings.rs:749-771` _(code)_
- Settings catalog dedup key is (name, description): sort by description then name, dedup_by(name==name && description==description) at :782 — `app/src/settings_view/keybindings.rs:754-784` _(code)_
- Comment documents that different-description same-name rows intentionally survive (find in block vs find in terminal) — `app/src/settings_view/keybindings.rs:766-781` _(code)_
- Conflict map is keyed only on Keystroke (HashMap<Keystroke,usize>), context-blind — `app/src/settings_view/keybindings.rs:104-133` _(code)_
- Conflict flag computed from row.binding.trigger keystroke only — `app/src/settings_view/keybindings.rs:1064` _(code)_
- Search scores on binding.trigger keystroke (:638) and binding.description.in_context fuzzy (:651), never reads binding.name — `app/src/util/bindings.rs:625-687` _(code)_
- Resource-center catalog dedups by description ONLY (:152) and filters trigger.is_some() && non-empty description — `app/src/resource_center/keybindings_page.rs:136-153` _(code)_
- Resource-center sectioning filters by binding.name membership (line 248) over static name-only arrays; arrays at utils.rs:8-114 (function get_bindings_by_section itself is at keybindings_page.rs:229-284, not utils.rs) — `app/src/resource_center/utils.rs:8-114` _(code)_

- _Unblocks:_ Lets the design doc state the catalog/search entry model truthfully: today an action name renders as scope-less (name, description) rows (description-only in the resource center), context variants either silently merge or appear as parallel name-linked rows, and conflicts plus search are context-blind. This grounds the R6/L7 decision on whether to add a scope field to CommandBinding and thread ContextPredicate through (Axis-2 dependency) so the catalog can honestly show "its real binding + scope" and so search-by-name returns the right 1-or-N hits.
- _Residual / needs decision:_ The question's "How to answer" asks to DECIDE entry granularity and align it with Q1's scope field — a design choice, not a code fact. Code fact: today granularity is (name, description) with no scope (description-only in the resource center). The design must choose among: (a) keep name-as-unit with N scope sub-rows (requires adding a ContextPredicate/scope field to CommandBinding, which from_lens/from_editable_lens currently discard, plus surfacing ContextPredicate from the engine — the Axis-2/R6 dependency); (b) make each (name, context) a first-class entry (breaks name-keyed save/load and the uniform remap in update_custom_trigger); or (c) keep (name, description) tuples but add a human-readable scope label. Whether search should index `name` (it does not today) is also a UX decision. I verified only the keybindings-catalog search path (filter_bindings_including_keystroke); a separate command-palette search path could differ but is out of scope for this question. None settleable from current code.
- _Verification:_ I opened all cited files and verified each evidence claim literally, plus one location the answer did not directly cite (get_bindings_by_section body at keybindings_page.rs:229-284). Grep re-run: `**A5-Q8.` is a single hit at line 811 of the spec, matching the question text. All 14 evidence claims hold exactly at the cited file:line. Found only two trivial imprecisions, neither material: (1) The detailed_answer's parenthetical "get_bindings_by_section (229-284)" refers to keybindings_page.rs, not utils.rs — utils.rs is only 146 lines so the range cannot be there; the evidence claim correctly cites utils.rs:8-114 for the static arrays and keybindings_page.rs:136-153 for build_bindings, so the file attribution is right in the evidence even though the prose is terse. (2) The "static name-only arrays (utils.rs:8-114)" covers 4 of the 5 section arrays (BLOCKS/INPUT_EDITOR/TERMINAL/FUNDAMENTALS); the 5th, PRIORITIZED_KEYBINDINGS used for the Essentials section, is imported from command_palette (keybindings_page.rs:26), not utils.rs — a minor incompleteness that does not change the substance (sectioning IS name-membership over static name-only arrays, confirmed at line 248). No overgeneralization, no app/-vs-crates scope confusion, no shifted line refs of consequence. Conflict map (ConflictMap.update/has_conflict) and all row mutations consistently use row.binding.trigger (Option<Keystroke>) with no context — confirmed context-blind. Search function body contains zero references to binding.name — confirmed.

### A5-Q9 — CONFIRMED · confidence: high

**Q.** How are actions with no keyboard trigger represented in the catalog, search, conflict, and the Unbound filter? CommandBinding.trigger is Option<Keystroke> and trigger_to_keystroke (bindings.rs:234) returns None for Standard/Custom/Empty triggers, so Standard-action and currently-unbound editable bindings have trigger=None. Do they appear in the catalog, are they searchable, and do they (correctly) never conflict?

**A.** Triggerless bindings (trigger=None) behave differently per surface. They are INCLUDED in the settings keybindings catalog (built from editable_bindings, no trigger filter) and remain searchable there by description text, and they correctly never conflict (None is flattened out of the keystroke-keyed ConflictMap and has_conflict(&None) is false). But the cheatsheet/resource-center catalog (built from get_key_bindings = fixed+editable) explicitly drops them via `.filter(|a| a.trigger.is_some() && !desc.is_empty())` at keybindings_page.rs:140-146. There is NO "Unbound" filter today; it is an unimplemented R7 feature, though the data (trigger.is_none()) exists to build it. Verified independently — answer is accurate.

VERIFIED CONFIRMED. I opened every cited file and confirmed each evidence claim at the stated line; I also read two locations the answer did not cite (get_key_bindings/editable_bindings definitions in warpui_core, and from_lens) and they corroborate rather than contradict.\n\nTwo distinct catalogs, opposite policies on triggerless/unbound actions:\n\nSETTINGS catalog (app/src/settings_view/keybindings.rs:748-784): built from `ctx.editable_bindings()` -> `CommandBinding::from_editable_lens` where `trigger = trigger_to_keystroke(lens.trigger)` (bindings.rs:762-771) and may be None. NO trigger filter in on_page_selected; sort/dedup keys on description+name only (759-782). So currently-unbound editable bindings (trigger=None) and Standard-action bindings (None off-Mac) ARE included, rendering with an empty keystroke cell (render_summary `None => Empty::new().finish()`, keybindings.rs:265-266). Confirmed this catalog enumerates ONLY editable bindings: editable_bindings() (warpui_core core/app.rs:1788) returns editable-only, and its doc at editable_bindings_for_view (1810-1813) explicitly says it returns bindings 'regardless of whether or not they have associated key bindings' — i.e. unbound editables are first-class. It never enumerates FixedBindings.\n\nCHEATSHEET / resource center (app/src/resource_center/keybindings_page.rs:136-154): built from `ctx.get_key_bindings()` chained with get_additional_keybindings(), then `.filter(|a| a.trigger.is_some() && !description.is_empty())` (140-146). get_key_bindings (warpui_core core/app.rs:1699, doc 1697) returns 'Editable bindings ... followed by any fixed bindings' = fixed+editable. So every triggerless action vanishes from the cheatsheet. Additional nuance the answer did not state but which only reinforces it: from_lens (bindings.rs:749-758) ALSO returns None for any binding with no description, so description-less fixed bindings are dropped even before the explicit filter.\n\nSEARCH (filter_bindings_including_keystroke, bindings.rs:625-687): empty query yields every binding including trigger=None (634-635); non-empty computes keystroke score via `binding.trigger.as_ref().and_then(...)` (638) so None never scores on keystroke, and fuzzy-matches `binding.description.in_context(...)` (650-653). I confirmed the function never reads `binding.name` (only description and trigger fields are referenced) — this also answers A5-Q15. Net: triggerless bindings remain searchable by description text only. (Edge case not affecting the conclusion: an unbound editable binding with an empty description would be included in the settings catalog but unfindable by text, since the settings path has no description-empty filter.)\n\nCONFLICT (ConflictMap, keybindings.rs:104-145): keyed on Keystroke. FromIterator<Option<Keystroke>> uses `iter.into_iter().flatten()` (139) so every None is dropped; has_conflict(&None) short-circuits to false (130); update (110-121) only mutates Some entries. Populated from `bindings.iter().flatten().map(|b| b.trigger.clone()).collect()` (795-800). So triggerless bindings never conflict and never get the warn border — confirming the question's hypothesis. Corollary holds: multiple distinct unbound actions (all None) do not collide with each other, because None is excluded entirely rather than collapsed to one shared key.\n\nUNBOUND FILTER: none exists. Settings view's only filtering is the free-text query (filter_bindings, keybindings.rs:536-549). I re-ran the greps: `trigger.is_some()` in app/src returns exactly 4 hits — keybindings_page.rs:141 (the cheatsheet catalog filter), keybindings.rs:397 (get_edit_button_row button-state, NOT a catalog filter), and two debug logs (controller.rs:1467, terminal/view.rs:9813). `grep -rni unbound app/src crates` returns only unrelated async unbounded channels and new_unbound editor models — no keybinding filter. R7 Unbound chip is unimplemented; data exists (trigger.is_none()), so it is a small additive filter, not an engine change.\n\nDESIGN GOTCHA holds: no single surface lists ALL bindable actions. Settings includes unbound editables but excludes all fixed; cheatsheet includes fixed+editable but excludes all triggerless. A triggerless FIXED binding (e.g. Standard actions on Linux) appears in neither. So catalog-v1 reusing the cheatsheet build path inherits the trigger.is_some() incompleteness; reusing the settings path misses all fixed bindings (A5-Q4 nameless-fixed problem).

_Evidence:_
- Settings catalog built from editable_bindings via from_editable_lens with no trigger filter (sort/dedup on description+name); rows include triggerless editables, rendered with empty keystroke cell (None => Empty::new().finish()) — `app/src/settings_view/keybindings.rs:748-784, 265-266` _(code)_
- editable_bindings() returns editable-only and includes bindings regardless of whether they have an associated keystroke (so unbound editables are first-class); settings catalog never enumerates fixed bindings — `crates/warpui_core/src/core/app.rs:1788, 1810-1813` _(code)_
- from_editable_lens sets trigger = trigger_to_keystroke(lens.trigger), an Option<Keystroke> that may be None — `app/src/util/bindings.rs:762-771` _(code)_
- trigger_to_keystroke returns None for Trigger::Empty, Standard actions resolved via mac_only_keystroke (None off-Mac), and Custom actions whose custom_tag_to_keystroke yields None — `app/src/util/bindings.rs:234-258` _(code)_
- mac_only_keystroke returns None on non-Mac, so Standard actions are triggerless on Linux/Windows — `app/src/util/bindings.rs:938-944` _(code)_
- Cheatsheet catalog built from get_key_bindings (which returns editable then fixed) chained with get_additional_keybindings, then explicitly drops triggerless/empty-desc actions via .filter(|a| a.trigger.is_some() && !desc.is_empty()) — `app/src/resource_center/keybindings_page.rs:136-154` _(code)_
- get_key_bindings returns editable bindings first followed by fixed bindings (i.e. fixed+editable) — `crates/warpui_core/src/core/app.rs:1697-1701` _(code)_
- from_lens (cheatsheet path) also applies trigger_to_keystroke and returns None when the binding has no description, dropping description-less fixed bindings before the explicit filter — `app/src/util/bindings.rs:749-758` _(code)_
- Search yields all bindings on empty query (incl. None trigger) at lines 634-635; otherwise None trigger yields no keystroke score (638) and fuzzy-matches description (650-653); binding.name is never read — `app/src/util/bindings.rs:625-687` _(code)_
- ConflictMap keyed on Keystroke; FromIterator<Option<Keystroke>> flattens away None (139), has_conflict(&None) returns false (130), update mutates only Some entries (110-121) — `app/src/settings_view/keybindings.rs:104-145` _(code)_
- Conflict map populated from bindings' triggers via .map(|b| b.trigger.clone()).collect() into ConflictMap — `app/src/settings_view/keybindings.rs:795-800` _(code)_
- No Unbound filter exists; settings view only filters by free-text query (filter_bindings) — `app/src/settings_view/keybindings.rs:536-549` _(code)_
- grep trigger.is_some() in app/src = 4 hits: keybindings_page.rs:141 (cheatsheet catalog filter), keybindings.rs:397 (button-state in get_edit_button_row, not a filter), controller.rs:1467 + terminal/view.rs:9813 (debug logs) — `grep -rn 'trigger.is_some()' app/src/` _(code)_
- grep -rni unbound app/src crates returns only unrelated async unbounded channels and new_unbound editor models; no keybinding filter — `grep -rni unbound app/src/ crates/` _(code)_

- _Unblocks:_ Lets the design doc state, with code backing, that (a) unbound editable actions are already first-class in the settings catalog/search and correctly never conflict, so the truthful-conflict work does not need to special-case them; and (b) the catalog cannot simply reuse the cheatsheet build path (its trigger.is_some() filter makes the catalog incomplete) — catalog-v1 must choose/merge a source that retains triggerless entries, and the R7 Unbound chip is a small additive `trigger.is_none()` filter rather than an engine change.
- _Residual / needs decision:_ Whether catalog-v1 should source from editable_bindings (includes unbound, excludes fixed) or get_key_bindings (includes fixed but the existing build_bindings drops triggerless) is a product/architecture decision, not settleable from code. The exact semantics of an "Unbound" filter — literal trigger=None vs. also bindings unbound from a default but still default-shadowed, and whether Standard-on-non-Mac should count — is a design choice tied to A5-Q10 platform scope. The completeness gap for triggerless FIXED bindings depends on A5-Q4 (nameless fixed bindings lack a stable key).
- _Verification:_ I independently opened all cited files. Every line reference is accurate: ConflictMap 104-145 (flatten at 139, has_conflict None at 130), render_summary None->Empty at 265-266, on_page_selected/build 748-784, conflict-map populate 795-800, filter_bindings 536-549, from_editable_lens 762-771, trigger_to_keystroke 234-258, mac_only_keystroke 938-944, filter_bindings_including_keystroke 625-687, cheatsheet build_bindings 136-154 with the trigger.is_some()+!desc.is_empty() filter at 140-146. I re-ran both greps myself: `trigger.is_some()` in app/src returns exactly 4 hits and only keybindings_page.rs:141 is a catalog filter (keybindings.rs:397 is button-state in get_edit_button_row; the other two are debug logs) — the answer's characterization is exact. The `unbound` grep returns only unrelated unbounded channels / new_unbound models — no keybinding filter. I also verified two uncited claims the answer relied on: get_key_bindings (warpui_core core/app.rs:1697-1701) returns editable+fixed per its doc, and editable_bindings (1788) is editable-only with editable_bindings_for_view doc (1810-1813) confirming unbound editables are included — so the fixed-vs-editable scope split is correct, not overgeneralized. I confirmed filter_bindings_including_keystroke reads only description and trigger, never name (validating the A5-Q15 side claim). The only things the answer omitted (immaterial, and both reinforce its conclusion): (1) from_lens also returns None for description-less bindings, so the cheatsheet drops those even before the explicit filter; (2) an unbound editable with an empty description would be in the settings catalog but unfindable by text since the settings path lacks a description-empty filter. Neither changes any conclusion. No shifted line refs, no app/-vs-crates confusion, no off-by counts found.

### A5-Q12 — CONFIRMED · confidence: high

**Q.** What stable key and snapshot format does the persisted action manifest (R9, for the change-keybinding agent) use, and how does it reference fixed bindings? Is the key the editable `name`, and are nameless fixed bindings simply unreferenceable until Q4 is resolved?

**A.** Confirmed. The only content-stable key in the code today is the editable binding `name: &'static str` (a compile-time `namespace:action` string), which is already the persistence key for the existing keybindings.yaml manifest (CustomKeybindings = HashMap<String, PersistedTrigger>, value a normalized keystroke string). BindingId (process-global atomic counter) and description (dynamically resolvable against live AppContext) are both volatile and unusable as durable keys. Fixed bindings carry no name field; their lens name is Default::default() (empty &str), so they are unreferenceable by any name-keyed manifest and unrebindable today — keying them is gated on Q4. No R9 "action manifest" concept exists in code yet (grep for manifest/snapshot/action_id/action_catalog returns nothing); its schema/regeneration trigger/on-disk location are unbuilt design decisions.

I re-grepped the question block (line 843 of the spec) and independently opened every cited file plus uncited corroborating locations. All ten evidence claims hold at the exact line numbers given; no shifts, no overgeneralization, no app/-vs-crates confusion, and the one count (4 name arrays) is correct.

Verified facts:

1) BindingId is NOT durable. crates/warpui_core/src/keymap.rs:262 `pub struct BindingId(pub usize)`; :264 `static NEXT_BINDING_ID: AtomicUsize = AtomicUsize::new(0)`; :266-272 `BindingId::new()` does `fetch_add(1, Relaxed)`. Counter restarts at 0 each process and depends on registration order, so it cannot survive rebuilds/versions. (Answer cited 261-272 — accurate, slightly wider to include the struct.)

2) The editable `name` is the only stable key and is ALREADY the manifest key. EditableBinding.name: &'static str (keymap.rs:294); lens exposes it (keymap.rs:308; from_editable_lens carries `name: lens.name.into()` at app/src/util/bindings.rs:767). On-disk: app/src/keyboard.rs:167-169 `struct CustomKeybindings(HashMap<String, PersistedTrigger>)`, doc-commented (155-166) as "top-level YAML map of (Editable Binding Name) -> Keybinding" with examples `"editor:delete_all_left": cmd-shift-A`; PersistedTrigger is a normalized keystroke string (171-187). Writes go by name: write_custom_keybinding(name,...) does `map.0.insert(name, ...)` (keyboard.rs:64-74); set_custom_keybinding(binding_name,...) -> write_custom_keybinding(binding_name.into(),...) (bindings.rs:491-499). Name lookup/override is editable-only: editable_bindings_by_name index (keymap.rs:30, built 414-417), get_binding_by_name (379-386), update_custom_trigger filters `b.name == name` over editable_custom_action_bindings and editable_bindings only (422-433).

3) Fixed bindings have NO name. struct FixedBinding (keymap.rs:277-286) has fields trigger/action/command_description/context_predicate/enabled_predicate/group/id — no `name` (only EditableBinding has it). FixedBinding::as_lens() sets `name: Default::default()` at keymap.rs:633 (impl block 630-642, distinguishable from EditableBinding's impl which starts at 645; it reads self.command_description, a FixedBinding-only field). So a fixed binding is neither name-keyable nor reboundable today (type doc: "This action can't be reconfigured with a custom key binding trigger", keymap.rs:275).

4) Description can go stale. DynamicDescriptionResolver = Arc<dyn Fn(&AppContext) -> Option<String> + Send + Sync> (keymap.rs:69); BindingDescription.dynamic_override (89); with_dynamic_override (148-154); resolve() applies the live resolver against &AppContext (164-172); also per-context `custom` overrides (83). materialize_description only resolves when has_dynamic_override() else clones (app/src/util/bindings.rs:785-791). A frozen description string would capture a point-in-time label — confirming it must not be the key.

5) No manifest exists yet. grep -niE "manifest|snapshot|action_id|action_catalog" over app/src/util/bindings.rs, app/src/keyboard.rs, app/src/settings_view/keybindings.rs, app/src/resource_center/keybindings_page.rs returns nothing. The only persisted artifact is keybindings.yaml (name -> trigger string).

6) Static discoverability catalog is editable-NAMES only. app/src/resource_center/utils.rs has exactly 4 `pub const ... : &[&str]` arrays — BLOCKS_KEYBINDINGS (8-25), INPUT_EDITOR_KEYBINDINGS (27-78), TERMINAL_KEYBINDINGS (80-106), FUNDAMENTALS_KEYBINDINGS (108-114) — all editable names; fixed bindings absent. (There is also a get_additional_keybindings() helper at 118+ for mac-menu-only bindings, which the answer didn't mention but which does not contradict the claim.)

Net: stable key = editable `name` (already proven durable as the keybindings.yaml key, value a normalized PersistedTrigger string); BindingId and description are volatile; fixed bindings are nameless and thus out of scope for a name-keyed manifest until Q4 assigns them stable names. The R9 manifest schema/regeneration trigger/location are genuine design residue, unbuilt in code.

_Evidence:_
- BindingId is a process-global atomic counter minted at registration (fetch_add Relaxed, starts at 0 each process); non-deterministic across runs/versions — `crates/warpui_core/src/keymap.rs:262-272` _(code)_
- FixedBinding (struct fields trigger/action/command_description/context_predicate/enabled_predicate/group/id) has no name field; only EditableBinding has name: &'static str — `crates/warpui_core/src/keymap.rs:277-286,294` _(both)_
- FixedBinding::as_lens() sets name: Default::default() (empty &str); the impl is FixedBinding's (reads self.command_description, a FixedBinding-only field), distinct from EditableBinding's impl starting at :645 — `crates/warpui_core/src/keymap.rs:630-642` _(code)_
- Name-based lookup/override covers editable bindings only: editable_bindings_by_name index, get_binding_by_name, update_custom_trigger filter b.name==name over editable collections — `crates/warpui_core/src/keymap.rs:30,379-386,414-417,422-433` _(code)_
- Existing persisted manifest keybindings.yaml is HashMap<String,PersistedTrigger> keyed by editable binding Name, value a normalized keystroke string — `app/src/keyboard.rs:155-187` _(code)_
- set_custom_keybinding persists by binding name via write_custom_keybinding, which does map.0.insert(name, ...) — `app/src/util/bindings.rs:491-499; app/src/keyboard.rs:64-74` _(code)_
- Descriptions can be dynamic (Arc<dyn Fn(&AppContext)->Option<String>>) and are resolved against live ctx via resolve(); also per-context custom overrides — a frozen description string can go stale — `crates/warpui_core/src/keymap.rs:69,83,89,148-172` _(code)_
- materialize_description only resolves when has_dynamic_override(), else clones — `app/src/util/bindings.rs:785-791` _(code)_
- Static discoverability catalog is exactly 4 &[&str] arrays of editable NAMES only (BLOCKS/INPUT_EDITOR/TERMINAL/FUNDAMENTALS); fixed bindings absent — `app/src/resource_center/utils.rs:8-114` _(code)_
- No manifest/snapshot/action_id/action_catalog concept exists in the keybinding code today (grep over bindings.rs, keyboard.rs, settings_view/keybindings.rs, resource_center/keybindings_page.rs returns nothing) — `app/src/util/bindings.rs + app/src/keyboard.rs + app/src/settings_view/keybindings.rs + app/src/resource_center/keybindings_page.rs` _(code)_

- _Unblocks:_ Lets the design commit to keying the R9 action manifest on the editable binding `name` (the same key already proven durable by keybindings.yaml), store the keystroke as a normalized PersistedTrigger string, treat description as a regenerated non-key label, and explicitly scope fixed bindings OUT of the manifest until Q4 assigns them stable names — avoiding a BindingId- or description-keyed schema that would break across rebuilds.
- _Residual / needs decision:_ The concrete R9 manifest is a design decision absent from code: its schema (e.g. name -> {description-snapshot, keystroke, group, platform}), regeneration trigger (build-time codegen vs runtime dump on keymap build), and on-disk location (alongside keybindings.yaml in config_local_dir vs a generated source artifact) are unspecified. Referencing fixed bindings is blocked on Q4 (whether FixedBinding gains a stable &'static str name + a by-name index); until then a name-keyed manifest can only cover editable bindings. Whether to store the (volatile) description as a non-authoritative human label vs omit it is a product call. I did not exhaustively grep the entire app/ tree for an alternate stable-id mechanism beyond the four keybinding-relevant files, but those are the canonical persistence/discoverability sites.
- _Verification:_ I opened every cited file at the exact offsets and confirmed each line reference (no shifts). Specifically re-verified: (a) BindingId atomic at keymap.rs:262-272; (b) FixedBinding struct 277-286 genuinely lacks a name field while EditableBinding has name at 294; (c) the as_lens at 630-642 setting name: Default::default() belongs to FixedBinding's impl (it reads self.command_description, EditableBinding's impl starts at 645) — so the "fixed lenses are nameless" claim is correctly scoped, not an overgeneralization; (d) editable_bindings_by_name field at :30 and update_custom_trigger's b.name==name filter at 422-433 operate on editable collections only; (e) CustomKeybindings = HashMap<String, PersistedTrigger> at keyboard.rs:169 with the doc comment confirming Name->Keybinding format; (f) materialize_description at 785-791 only resolves on has_dynamic_override. I independently RE-RAN the grep for manifest/snapshot/action_id/action_catalog across the four keybinding files — it returns nothing (confirming claim 5), and counted the name arrays in utils.rs — exactly 4 (matching the answer). I also checked an uncited corroborating location: from_editable_lens (bindings.rs:762-771) carries name: lens.name.into() into CommandBinding, reinforcing that the name is the through-line identifier for editable bindings, whereas CommandBinding::placeholder and the fixed lens both use Default::default() for name. The code anchor's question "does filter_bindings_including_keystroke read name?" — verified NO: it matches on description.in_context and trigger keystroke (bindings.rs:625-687), which does not contradict any answer claim. Found no errors, no overreach, no scope confusion. The answer's residual_unknowns correctly frames the R9 schema/trigger/location as unbuilt design decisions.

### A5-Q13 — CONFIRMED · confidence: high

**Q.** Regression strategy for conflict truthfulness and catalog completeness vs ~265 imperative register sites: a golden test that non-overlapping contexts (id!(Vim) vs !id!(Vim), actions.rs:51-60) produce NO conflict while a same-context collision DOES, and a test that every get_key_bindings() binding appears in the catalog with a resolvable id. How to keep these from rotting across 109 files?

**A.** CONFIRMED. No conflict-truthfulness test and no catalog-completeness test exist today. The settings conflict detector (ConflictMap, app/src/settings_view/keybindings.rs:104-145) is context-blind: it is a HashMap<Keystroke, usize> keyed solely on the trigger and never reads the context predicate, so the proposed non-overlapping-contexts golden test (id!("Vim") vs !id!("Vim")) would FAIL against today's code until detection is made context-aware. The catalog is split between an auto-derived Resource Center list (which filters out no-trigger / empty-description bindings and dedups by description) and hand-maintained static name-only lists with no resolvability test; the only existing debug-time net is per-binding validators (PTY-compliance / cross-platform), not conflict or completeness checks. Every evidence claim's file:line was independently verified and every count re-ran exactly.

I independently opened every cited file plus uncited corroborating locations (matcher.rs validate_bindings call path in core/app.rs, get_key_bindings delegation, validator cfg-gating). All material claims hold.

(1) CONTEXT-BLIND CONFLICT DETECTION — CONFIRMED. app/src/settings_view/keybindings.rs:104-145: ConflictMap { map: HashMap<Keystroke, usize> }. update (110-121), has_conflict (123-132, true when count > 1) and FromIterator (135-145) all key exclusively on Keystroke; the context_predicate is never consulted. Populated at 794-800 from binding.trigger.clone(); consumed at 1064 via view.conflict_map.has_conflict(&row.binding.trigger). Two editable bindings on the same keystroke in non-overlapping contexts would be a false-positive conflict. The golden non-overlap assertion therefore requires making detection context-aware first; it is not merely a missing test. Verified by reading lines 104-145, 748-800, 1050-1074.

(2) LIST BUILT FROM EDITABLE ONLY, DEDUPED BY NAME+DESC — CONFIRMED. keybindings.rs:754 builds from ctx.editable_bindings().collect(); 782 dedup_by(|a,b| a.name==b.name && a.description==b.description). So fixed bindings never enter the conflict map.

(3) actions.rs VIM FORK IS FIXED, NOT EDITABLE — CONFIRMED. app/src/code/editor/view/actions.rs:50-110 is app.register_fixed_bindings([FixedBinding::new(...)]) with `& !id!("Vim")` vs `& id!("Vim")` forks (lines 54/59, 64/69, 74/79, 84/89, 104/109). Being fixed, the Vim fork is excluded from the editable-only settings conflict map — the answer's nuance (these specific example bindings escape incidentally; the real false-positive risk is editable same-keystroke pairs) is correct.

(4) NO CONFLICT TEST — CONFIRMED. crates/warpui_core/src/keymap/matcher_tests.rs has only test_matcher (5), test_editable_binding_matching (89), test_bindings_for_context (179), plus a helper fn test_keystroke (230, not a #[test]). `grep -rn conflict crates/warpui_core/src/keymap/` returns nothing. app/src/settings_view/keybindings.rs has no `mod tests` / `#[test]`.

(5) NO COMPLETENESS TEST, TWO DRIFTING CATALOGS — CONFIRMED. Auto-derived: app/src/resource_center/keybindings_page.rs:136-154 builds from ctx.get_key_bindings(), chains get_additional_keybindings() (139), FILTERS to a.trigger.is_some() (141) AND non-empty Default description (142-146), then dedup_by description (152). A registered action with no default keystroke or empty description silently never appears, with no coverage test. Hand-maintained: app/src/resource_center/utils.rs:8/27/80/108 (BLOCKS_/INPUT_EDITOR_/TERMINAL_/FUNDAMENTALS_KEYBINDINGS, name-only) with a top comment (lines 1-2) that it "should always align with documentation"; app/src/command_palette.rs:2 (PRIORITIZED_KEYBINDINGS). `grep ... | grep -i test` over all of app/ and crates/ returns nothing — no test references any of these constants.

(6) ONLY DEBUG-TIME NET = PER-BINDING VALIDATORS — CONFIRMED. crates/warpui_core/src/keymap/matcher.rs:159-203: register_binding_validator / set_default_binding_validator / validate_bindings are all #[cfg(debug_assertions)]; validate_bindings (182-203) panics on any failure. Corroborated by core/app.rs: register_binding_validator and set_default_binding_validator each have a debug def + a release stub (1568/1578, 1588/1600), and validate_bindings is debug-real (1609) vs release no-op (1616), invoked from platform/app.rs:85. Registered validators: is_binding_cross_platform as default at app/src/lib.rs:1395; is_binding_pty_compliant per view at root_view.rs:262 (RootView), pane_group/mod.rs:318 (PaneGroup), workspace/mod.rs:82 (Workspace), terminal/view/init.rs:83 (TerminalView). These check individual-binding properties, never pairwise conflicts or catalog membership.

(7) CLOSEST COMPLETENESS ANALOG — CONFIRMED. app/src/util/bindings_tests.rs:76-116: test_orchestration_cycle_bindings_are_editable and test_terminal_page_scroll_bindings_are_editable each use App::test + ctx.editable_bindings().find(name) on two hard-coded names apiece (4 names total), spot-checks not live-set coverage.

(8) get_key_bindings IS THE LIVE SET — CONFIRMED. core/app.rs:1699 get_key_bindings delegates to keystroke_matcher.get_bindings() (matcher.rs:303), which returns editable bindings (custom-modified) then fixed bindings in reverse-add order.

(9) SCALE — RE-RAN, EXACT. register_fixed_bindings call sites = 147; register_editable_bindings = 120; total = 267. Files with either register call = 111 (2 of which are the keymap definition files, consistent with the spec's "~265 / 109"). FixedBinding::new = 397; EditableBinding::new = 451. All match the answer exactly.

_Evidence:_
- ConflictMap is HashMap<Keystroke,usize>; update/has_conflict/FromIterator key only on Keystroke, context predicate never read — `app/src/settings_view/keybindings.rs:104-145` _(code)_
- Conflict map populated from binding.trigger.clone() (794-800) and consumed via has_conflict(&row.binding.trigger) (1064) — `app/src/settings_view/keybindings.rs:794-800,1064` _(code)_
- Settings list built from ctx.editable_bindings() (754) then dedup_by name && description (782) — `app/src/settings_view/keybindings.rs:754,782` _(code)_
- actions.rs Vim fork is FixedBinding::new via register_fixed_bindings with & !id!("Vim") vs & id!("Vim"); fixed, so excluded from the editable-only conflict map — `app/src/code/editor/view/actions.rs:50-110` _(both)_
- No conflict test: matcher_tests has only test_matcher(5)/test_editable_binding_matching(89)/test_bindings_for_context(179); grep conflict over keymap crate returns nothing; settings keybindings.rs has no test module — `crates/warpui_core/src/keymap/matcher_tests.rs:5,89,179` _(both)_
- Resource Center catalog auto-derives from ctx.get_key_bindings(), filters out trigger.is_none() (141) and empty Default description (142-146), dedups by description (152) — `app/src/resource_center/keybindings_page.rs:136-154` _(code)_
- get_key_bindings (core/app.rs:1699) delegates to matcher get_bindings (matcher.rs:303) = the live registered set the catalog draws from — `crates/warpui_core/src/keymap/matcher.rs:303` _(code)_
- Static name-only catalog lists (BLOCKS_/INPUT_EDITOR_/TERMINAL_/FUNDAMENTALS_KEYBINDINGS at 8/27/80/108; comment says align with docs) and PRIORITIZED_KEYBINDINGS (command_palette.rs:2); no test references any of them — `app/src/resource_center/utils.rs:1-114; app/src/command_palette.rs:2` _(code)_
- Only debug-time net is per-binding validators: register/set_default/validate_bindings all #[cfg(debug_assertions)], validate panics on failure; release stubs in core/app.rs:1568-1616 — `crates/warpui_core/src/keymap/matcher.rs:159-203` _(code)_
- Validators registered: is_binding_cross_platform default (lib.rs:1395); is_binding_pty_compliant for RootView/PaneGroup/Workspace/TerminalView — `app/src/lib.rs:1395; app/src/root_view.rs:262; app/src/pane_group/mod.rs:318; app/src/workspace/mod.rs:82; app/src/terminal/view/init.rs:83` _(code)_
- Closest completeness analog: two tests each spot-check two hard-coded names via App::test + editable_bindings().find(name) — `app/src/util/bindings_tests.rs:76-116` _(code)_
- Re-ran counts: 147 register_fixed_bindings + 120 register_editable_bindings = 267 sites across 111 files; 397 FixedBinding::new + 451 EditableBinding::new — `grep over app/ crates/: register_fixed_bindings=147; register_editable_bindings=120; files=111; FixedBinding::new=397; EditableBinding::new=451` _(code)_

- _Unblocks:_ Lets the design doc commit to a concrete regression harness: it must (1) replace the context-blind settings ConflictMap with context-aware detection BEFORE the golden non-overlap test can pass, and (2) add a completeness test over editable_bindings()/get_key_bindings(), most cheaply wired into the existing cfg(debug_assertions) validate_bindings panic path, asserting every registered name resolves and appears in the catalog, so the ~267 register sites across 111 files cannot silently drift.
- _Residual / needs decision:_ The regression STRATEGY itself is a design choice code cannot settle: (a) enforce via the existing cfg(debug_assertions) validate_bindings panic path vs a separate nextest/CI assertion; (b) the conflict-overlap algorithm — true context-predicate satisfiability/intersection vs a heuristic, since predicates are arbitrary id!() boolean expressions; (c) what counts as a canonical resolvable id and whether the Resource Center filters (drop no-trigger/empty-description) are intended exclusions or coverage gaps; (d) whether to delete or test-pin the hand-maintained static lists (utils.rs, PRIORITIZED_KEYBINDINGS). I did not exhaustively prove there is NO other context-aware conflict surface anywhere in app/ beyond the settings ConflictMap, but the keymap-crate `grep conflict` is empty and the matcher resolves by context only at dispatch (push_keystroke), surfacing no user-facing conflict signal — so the settings ConflictMap is the only conflict detector, and it is context-blind.
- _Verification:_ I tried to refute and could not. I opened every cited file at the cited lines and re-ran every count myself.

VERIFIED EXACTLY: ConflictMap structure/keying (104-145), conflict-map population at 794-800 and consumption at 1064, list build at 754 and dedup at 782, the actions.rs Vim FixedBinding fork (50-110, id!(\"Vim\") vs !id!(\"Vim\")), the three matcher_tests fns at 5/89/179, the empty `grep conflict` over the keymap crate, the absence of any test module in settings keybindings.rs, the Resource Center filter+dedup (137-153), the four utils.rs static lists + the align-with-docs comment + PRIORITIZED_KEYBINDINGS, the matcher.rs:159-203 cfg(debug_assertions) validators, and all five validator registration sites verbatim. The two bindings_tests at 76-116 each check two names (4 total), matching the answer's \"~4 names.\"

COUNTS RE-RAN AND MATCHED EXACTLY: 147 fixed + 120 editable = 267 register sites; 111 files; 397 FixedBinding::new; 451 EditableBinding::new. (Note: 111 files include the 2 keymap definition files; subtracting those gives ~109 call-site files, consistent with the spec's \"109 files\" — no discrepancy.)

CORROBORATED UNCITED LOCATIONS: get_key_bindings at core/app.rs:1699 delegates to matcher get_bindings (matcher.rs:303), confirming the catalog draws from the live registered set; and core/app.rs shows debug/release split for register_binding_validator (1568/1578), set_default_binding_validator (1588/1600) and validate_bindings (1609 real / 1616 no-op), reinforcing the debug-only claim. The grep for these constants filtered by 'test' returned nothing, confirming no test pins them.

ONE NON-MATERIAL NIT (does not change verdict): the answer's forward-looking text says a completeness check could be wired into validate_bindings over \"get_bindings()/editable_bindings()\". In fact validate_bindings (matcher.rs:182-203) iterates bindings_for_context(...) for per-context validators and get_bindings() for the default validator; it does not iterate editable_bindings() specifically. This is strategy prose, not an evidence claim, so it does not affect the code facts. Every JSON evidence entry's file:line is accurate. No overgeneralization, no app/-vs-crates scope confusion, no shifted line refs found.

---

